# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is the Agda formalisation accompanying the paper *"A Complete and Natural Rule Set for Multi-Qudit Clifford Circuits in All Odd Prime Dimensions"*. It is being prepared for submission to the Agda standard library. Tested with **Agda 2.8.0 + stdlib 2.4**, which is what the tagged submission artifact is verified against (earlier versions of the library were reported to work with Agda 2.7 + stdlib 2.2).

## Typechecking

```bash
# Typecheck via WSL (Agda 2.8, resolves dependencies automatically).
# On this branch the root states only the qupit chain, so that is all it
# reaches: the symplectic development and its simplified rules, the
# ProjectivePauli factor, the semidirect construction that assembles
# them, and the Clifford rule sets on top (Simplified-V1 → Paper-V0 →
# Paper-V1, plus Shared/PauliBase).
wsl --exec /home/onest/.cabal/bin/agda MainTheorems.agda
```

Everything else in the tree is **not** reached from the root here, and needs typechecking separately if you touch it: the symmetric, trivial and cyclic groups, the wreath product, the Clifford+T and U₃(ℤ[½,i]) amalgamations, `ProjectiveClifford/Qupit/Simplified-V2`, `ProjectiveClifford/Qubit/` and `Clifford/Qupit/`.

Use WSL Agda 2.8 (`wsl --exec /home/onest/.cabal/bin/agda`) for all files. The WSL install uses its own stdlib at `/home/onest/.agda/lib/agda-stdlib/`. The `.agda-lib` file (`qupit.agda-lib`) includes `.` and depends on `standard-library`.

**Always re-typecheck `MainTheorems.agda` after any edit to library files.** From PowerShell, invoke WSL directly (Git-Bash mangles the Linux path).

## Architecture

The library is layered bottom-up:

### Layer 0 — Notations (`Notations.agda`)
Numeral patterns `₀`–`₉`, successor patterns `₁₊`/`₂₊`/`₃₊`/`₄₊` (overloaded over ℕ and Fin), and the `auto` pattern (= `Eq.refl`).

### Layer 1 — Free monoid (`Word/`)
- **`Word/Base.agda`**: the `Word X` type (free monoid over generators `X`): constructors `[_]ʷ`, `ε`, `_•_`; powers `_^_`/`_^'_`; `wmap`, `wconcat`, `wconcatmap` and its postfix notation `_ʷ` (so `(f ʷ)` extends `f : X → Word Y` to words); folds `wfoldr`/`wfoldl`; the stateful traversals `_ᵗ`/`_ᵗ'` that drive coset enumeration; conjugation helpers `_ʰ`/`_ⁿ`/`_ʰ'`/`_ⁿ'`; `WRel X = Rel (Word X) 0ℓ`.
- **`Word/Properties.agda`**: `wmap`/`wconcat` fusion laws, `lemma-ʷ-∘`, `lemma-fʷ-w^n`, `wfoldr-cong`/`wfoldl-cong`, and `≡-dec` (decidable equality of words).

### Layer 2 — Group presentations (`Presentation/`)
- **`Base.agda`**: parameterised by `Γ : WRel X`. `_===_` is the raw relation; `_≈_` the monoid congruence it generates (refl/sym/trans/cong/assoc/left-unit/right-unit/axiom); `refl'` lifts `_≡_`; combinators `cleft_`, `cright_`, `_reversed`; `Alphabet = X`.
- **`Properties.agda`**: `≈-isEquivalence`, `word-setoid`, magma/semigroup/monoid structures and bundles; the associativity solvers (`to-list`/`from-list`, `mod-assoc`, `by-assoc`, `by-assoc-and`, and the pattern-guided `Pattern-Assoc.by-passoc`); word-power lemmas (`lemma-^-+`, `lemma-^^`, `word-comm`, …); `wfoldr`/`wfoldl` congruence lemmas.
- **`Definitions.agda`**: `_IsPresentationOf_` (group), `_IsMonoidPresentationOf_`, and `module SubPresentation` (`Soundness` & `Completeness` of a semantics ⟦_⟧ : Syn → Sem, i.e. ⟦_⟧ is a setoid embedding).
- **`GroupLike.agda`**: `Grouplike` (every generator has a left inverse) and `Group-Lemmas` (`_⁻¹`, cancellation, uniqueness of inverses, the group `•-ε-group`).
- **`Morphism.agda`**: parameterised by presentations `Γ`, `Δ`. Builders turning generator-level data into `IsMonoidHomomorphism`/`Monomorphism`/`Isomorphism` and the group versions, for both `(f ʷ)` and `wmap f`.
- **`Independence.agda`**: independence of axioms. A `Select Γ` is a predicate on axiom instances and `Γ ∣ P` the sub-relation of the selected ones; `Derivable Γ P a` / `Independent Γ P a` say whether an instance `a : Γ w v` is (not) in the congruence the selection generates; `≈-mono`/`≈-∣` embed that congruence into larger ones. `Model Γ P mon ⟦_⟧₀` reads words into a monoid (via `StarInterp.Extend`) and `Model.Sound sound-ax` turns per-axiom soundness of the selected instances into `sound` for the congruence and `independent : ¬ (⟦ w ⟧ ≈ ⟦ v ⟧) → Independent Γ P a`. An axiom is shown independent by a model in which the others hold and it fails.

### Layer 3 — Constructions (`Presentation/Construct/`)
- **`Base.agda`**: amalgamated product `_⊕_` and related combinators on `WRel`.
- **`Properties/DirectProduct.agda`**, **`SemiDirectProduct.agda`**, **`NDirectProduct.agda`**, **`SugarProduct.agda`**: lift normal-form witnesses through the constructions. `SemiDirectProduct` takes a **word-valued** action `conj : H → N → Word N` (the relation `ConjRelʷ conj`); an earlier element-valued version, `conj : H → N → N`, was deleted once nothing imported it.
- **`Properties/Amalgamation.agda`**: amalgamated free product with coset normal form (`AmalDataNF`, `ANF`).

### Layer — Circuits (`Circuit/`)
- **`Base.agda`**: parameterised by `Gate : ℕ → Set`. Wire-indexed generators `Gen`, `Circuit n = Word (Gen n)`, shifts `_↑`/`_↥ᵏ_`, and `Lift-Relation` extending any gate relation with the **four** structural rules `cong↑`, `comm₁`/`comm₂` and `ω↑=ω`. `Lift-Relation` also proves `lemma-cong↑` and `comm-gate₁-w↑`/`comm-gate₂-w↑↑`, the comm rules extended from a single generator to a whole circuit. There is **no `comm₀` rule**: a scalar's centrality is the theorem `Central-Scalars.comm₀` (with `comm-gate₀-w`), derived from the other rules given the one hypothesis `scalars-comm` that scalars commute with one another — for a gate set with several 0-ary gates that is the gate-specific relation's job to state; `Single-Scalar (one : ∀ h h' → h ≡ h')` discharges it when `Gate 0` is a subsingleton. Clients with one scalar write `open LR.Central-Scalars (λ { ω-gate ω-gate → PB.refl }) public using (comm₀ ; comm-gate₀-w)` and use `comm₀ h g` where they used to write `PB.axiom (comm₀ h g)`; a function by cases on `_VRel,_===_` no longer owes a `comm₀` clause.
- **`Independence.agda`**: the four structural rules are independent of one another. `Structural SRel` names them (`data Rule`, constructors overloaded with those of `_VRel,_===_`), `Uses r a` says whether rule `r` occurs in an axiom instance (through `cong↑`'s premise), `Without r` is the selection of the others, and `a IndependentOf r` / `a DerivableWithout r` are `Presentation.Independence`'s notions at that selection; `Lift` lifts a restricted congruence one wire up. `Soundness P P-cong↑ mon ⟦_⟧₀` is the engine for an arbitrary gate relation: a monoid per width, `Rules` takes soundness of each rule guarded by the selection and gives `sound`/`independent`, and `Shift σ σ-hom ⟦↥⟧` discharges the `cong↑` obligation from a shift homomorphism. `Pure` instantiates the empty gate relation and proves, over any `Gate`, `cong↑-independent₀/₁/₂`, `comm₁-independent`, `comm₂-independent`, `ω↑=ω-independent` (models: `2 ^ depth` in (ℕ, +); depths of one-/two-wire gates in `List ℕ`; depths beyond the reach of the bottom-stated rules) and `scalars-comm-needed`: two distinct scalars need not commute in the full pure theory (the scalars in order, in `List (Gate 0)`), so `Central-Scalars`' hypothesis is not redundant. `MainTheorems` indexes these under `Structural-Theorems`.

### Layer — Normalization (`Normalization/`)
- **`NormalForm/Setoid.agda`**: setoid-valued normal-form witnesses on the stdlib `Function.Bundles` — `NormalFormInjective` = `Injection`, `BijectiveNormalForm` = `Bijection`, `NormalForm` = `RightInverse` (maps `word-setoid ⟶ₛ NF`) — plus `WeakNormalForm`. Uniqueness is **not** here.
- **`NormalForm/Uniqueness.agda`** (and `Uniqueness/Propositional.agda`, the same at the discrete setoid): `UniqueNormalForm`, `by-normalization` (soundness + unique NF ⇒ completeness) and the converse `by-completeness` (completeness + exact section `nf ∘ inv-nf ≗ id` ⇒ unique NF). `UniqueNormalForm` is indexed by the **section** `inv-nf`, not by a whole `NormalForm` record — so a client that already has the record passes `SNF.NormalForm.inv-nf nfp`, and `by-normalization` needs the record given explicitly (`{nfp}`), nothing else determining it. A second copy indexed by the record used to live in `Setoid.agda`; it is gone.
- **`NormalForm/Propositional.agda`**: `Normalization.NormalForm.Setoid Γ (setoid B)` re-exported for a plain carrier set `B` (an explicit module parameter) — the witnesses land in `≡` on `B` and all derivations are inherited. Types read `NormalForm Γ B` / `NormalFormInjective Γ B`. Because the re-export fixes the codomain to `setoid B`, the witnesses are function-aliases here, not record names: **opening or projecting a witness value goes through `Normalization.NormalForm.Setoid` directly (imported `as SNF`)** — e.g. `open SNF.NormalForm nfp renaming (…)` — while `Propositional` is used only for the `Γ B` types and record construction.
- **`Reidemeister-Schreier.agda`**: the injectivity/surjectivity engine. `Star-Injective-Simplified` proves `(f ʷ)` injective given a left inverse on generators; `Star-Injective-Full` (and its setoid variant) does coset enumeration and provides the Schreier section, right/left normal forms.
- **`CosetNF.agda`**: coset normal forms via Reidemeister–Schreier: `lemma-ᵗ-act` (letters-to-words action law), `module SingleLevel` (one level; its `Transfer.Unique` derives `nf' ∘ gg ≡ id` for the transported NF from base exactness + coset exactness of the table on sections, via cancellation and R–S injectivity), `CosetTable` / `PackedCosetTable` (coset tables with a distinguished identity coset), `CosetTower` (iterate up an ℕ-indexed family).

### Layer 4 — Specific groups (`Examples/Groups/`)
- **`Cyclic/`**: ℤ/nℤ. `Syntactics` (the relation `_Cn,_===_` over a one-element alphabet), `Presentation`, `Normalization`, `Theorems`.
- **`Trivial/`**: the trivial group, two presentations proved isomorphic. Everything is derived once from the single hypothesis that every generator is `≈ ε` (`gen≈ε`), then instantiated twice, so the directory is a **generic layer plus two instances**.
  - Generic, at the root, each parameterised by `(Γ, gen≈ε)`: `Normalization` (`w≈ε`, `nf`/`nfp`/`nfp'`), `Interpretation` (`⟦_⟧₀`, `GS`, `fʷ-cong-ax`, `grouplike`), `UniqueNormalForm` (`unfp`). `Semantics` is the shared target (`Terminal.group`, as `gp`) and takes no parameters.
  - **`Presentation1/`** is `⟨ ⊥ ∣ ⟩` (`EmptyRel`), **`Presentation2/ A`** is `⟨ A ∣ w = ε ⟩` (`TrivialRel`). Each has the same five modules: `Syntactics` supplies `pres` and `gen≈ε` (the only real content); `Normalization`, `Interpretation`, `UniqueNormalForm` just instantiate their generic namesakes; `Presentation` assembles `subpresentation`/`presentation`. Clients wanting the trivial group as a base case take `nfp`/`nfp'`/`gp`/`presentation` off `Presentation1.Presentation` (`NDirectProduct` does, at width 0).
  - `Presentation-Equivalence B` gives the monoid isomorphism between the two — a syntactic statement that never touches the semantics.
  - Note `gp` is re-exported, not abstracted over the module parameter, so it is `Presentation2.Presentation.gp`, never `.gp A`; `presentation` does take `A`.
- **`Symmetric/`**, **`Symplectic/`**, **`Clifford/`**, **`ProjectiveClifford/`**, **`Pauli/`**, **`ProjectivePauli/`**: see Layer 5.

There is no longer a `Presentation/Groups/`: it held a second Sₙ and a hand-rolled ℤ/4ℤ ≀ Sₙ, both over an inductive alphabet rather than circuit generators, and both are gone. The wreath product now comes from `Examples/Construct/SemiDirectProduct/SnD.agda`, which supplies `pres`, `nfp` and `nfp'` over the `Examples/Groups/Symmetric` alphabet.

### Layer 5 — Examples (`Examples/`)
- **`Groups/Symmetric/`**: completeness of the circuit presentation of Sₙ. There is no `Theorems.agda` façade — `MainTheorems` takes the five results (presentation, unique normal form for each semantics, soundness, completeness) straight from the modules that prove them. The layout follows the two chains: the **permutation** one is at the top level (`Semantics`, `Interpretation`, `Soundness`, `UniqueNormalForm`, `Surjectivity`, `Presentation`) and reaches a full presentation — `Semantics` is the target group alone and mentions no syntax, `Interpretation` is `⟦_⟧`/`⟦_⟧ᵍ`/`⟦↑⟧`, and `Soundness` is that `⟦_⟧` respects the relations; the **endofunction** one is `SubPresentation/` (`Semantics`, `Interpretation`, `UniqueNormalForm`, `SubPres`) and stops at a setoid embedding, since endofunctions are not all denotations. `Presentation` splits like Symplectic's: `subpresentation` needs only normalization, and `Surjectivity` is what promotes it. Shared support: `Syntactics`, `Cosets`, `Normalization`. `Completeness.agda` and `IndexedAction.agda` typecheck but nothing imports them.
- **`Groups/ProjectiveClifford/Qupit/`**: the paper's own subject. For an odd prime p, the qupit Clifford circuits read **modulo scalars** present `Pauli n ⋊ Sp(2n, ℤ/pℤ)`. Four rule sets over the same alphabet (Symplectic's `H`, `S`, `CZ`) — Simplified-V1, Simplified-V2, Paper-V0, Paper-V1 — each shown to present that group by transport from the one below it, over a base (`SemiDirect/`, on a different alphabet) that does the real work. Every layer above the base is an isomorphism plus a composition, which is why the stack is cheap to extend and why no rule set re-proves what another already has.
  - **`SemiDirect/`** is the base and defines `Pauli⋊Sp`. `Presentation` feeds the generic `SemiDirectProduct` machinery its two factors — `Symplectic.Simplified` for the symplectic part, `ProjectivePauli` for the Pauli part — together with `ConjAction`'s two well-definedness proofs for the conjugation action. Every input is a theorem, so it is unconditional.
  - **`Simplified-V1/`** is where the mathematics lives: the Pauli calculus (`Lemmas`, `LemmasXZ`, `LemmasCZ`, `SemiM`), the Ex-conjugation rules (`ExRules`), `Forward`, `Soundness`, a rewriting tactic layer (`Tactics`), and `Iso` onto the semidirect relation.
  - **`Paper-V0/`** is Figure 1 with the multiplier spelled over `R = S • Z^½`, as `RHR x x⁻¹`; 16 group-specific rules. Its `Iso` proves it isomorphic to Simplified-V1 and exports **`v1⇒pap`**, which transports Simplified-V1 theorems into its theory — which is why its `Lemmas` derives Selinger's c10–c15, the lower-wire rules and the swap calculus natively.
  - **`Paper-V1/`** is the same rules with the multiplier spelled over `S`, as `SHS' x⁻¹ x`, and 15 rules rather than 16: `(S • H) ^ 3 = ε` is a theorem here, not an axiom, because under that spelling `M₁` *is* that word and `M-power` at `k = 0` already gives it. `Iso` is the identity on words onto Paper-V0. `Lemmas/` carries only what that needs (`OneWire`, `GroupLike`, `XZ`) — the Ex-conjugation and three-wire layers it inherited from Paper-V0 were removed, since the isomorphism transports all of it.
  - **`Shared/PauliBase.agda`** is the relation-agnostic Pauli calculus, instantiated at whichever rule set needs it. `Calculus` rests on the axioms both spellings share; `BridgeCalc` adds `X • Z ≈ Z • X` as a *hypothesis* — the one fact whose proof genuinely differs between them — and builds the bridge between the R- and S-spellings of the multiplier, plus `MulZ`/`SemiMR` for carrying a Pauli across it.
  - **`Simplified-V2/`** transports the V1 theorem once more along an identity isomorphism. Nothing imports it.
- **`Groups/ProjectiveClifford/Qubit/`**: the qubit analogue (`CliffordExtension`, `Cocycle`, `ExtensionPresentation`, a `Selinger/` subtree). Self-contained — nothing outside the directory imports it.
- **`Groups/Real-Clifford+CH/`**: Clément's *A Complete Equational Theory for Real-Clifford+CH Quantum Circuits* (arXiv:2602.06644) — the gate set {H, Z, CZ, CH}, its Figure 4 with the swap rules of Remark 1, matrix semantics over ℤ[1/√2], soundness in full, completeness on 0, 1 and 2 qubits up to the one theorem the paper imports (Theorem 4.4, [17]), and completeness on n ≥ 3 qubits from the paper's Section 8 — its generators, rules, encoding and decoding all defined, the encoding proved to preserve the semantics at every width, and its remaining results as hypotheses: Lemmas 8.7 and 8.8, and Theorem 4.10 — which is also proved, from the imported Theorem 4.4 at every width, by the Reidemeister–Schreier method of Appendix A (`Auxiliary/Theorem410Proof`, used by `Completeness.Theorem-8-9′`). Appendix D has begun (`ThreeQubit/`: Lemmas D.1 and D.2 proved from completeness on two qubits; `FourQubit/`: Lemma D.5, all ninety-nine equations (150)–(248), from completeness on three; `FiveQubit/`: Lemma D.7, all twenty equations (249)–(268), from the same two and the schema (19); `GeneralN/`: eight of Lemma D.8's thirty-nine, (269)–(273), (281), (286) and (303), on any number of wires, and (274), (275), (285) modulo the keystone; not yet used). Self-contained: nothing outside the directory imports it, and it is not reached from the root — typecheck `Examples/Groups/Real-Clifford+CH/Completeness.agda` (the relator checks and the box matrices, which were most of its ten minutes from scratch, now take seconds; run Agda under `ulimit -v 8000000` and a `timeout`), `Auxiliary/Soundness.agda` and `Auxiliary/Checks8.agda` (Figure 8 decided on three qubits, about twenty minutes and under 1.5 GB: `Checks8/Base` holds the decision procedure, `Checks8/R20`…`R42` and `H01`…`H67` the evaluations — a few thousand instances per module, because Agda frees the memory of an evaluation only when its module is done and all of them in one module exhaust 10 GB — `Checks8/Sound` an abstract name for the statement, and `Checks8` assembles `sound`) and `Auxiliary/DecodingChecks.agda` (Definition 8.3 decided on three qubits for every sign pair and signed exchange and on samples of the other letters, about three minutes), which nothing imports. Never run two Agda processes on these at once: they race on the interface files.
  - `Syntactics`: the gates over `Circuit.Base`, the shortcuts of the paper (`°CZ`, `Ex`, `CZ₂₀`, `PP`, `CCZX`/`CCXZ`, the multi-controlled box `Λ□` of Definition 2.4), the raw relation `_SRel,_===_` (Figure 4 plus the swap rules), its lift `_VRel,_===_` through `Lift-Relation`, and `n ⊢ w ≈ v` for the congruence. Wire 0 is the paper's bottom wire; a two-wire gate sits on wires 0 and 1 with the control of CH on wire 1; bit vectors list wire 0 first.
  - `Semantics/`: `Ring` (ℤ[√2] as a `data` of two integers, ring laws by the ℤ ring solver, `√2^_`, `strict`), `Algebra` (operators `Op n = Bits n → Bits n → 𝔽` under pointwise `_≐_`; stored tries `Mat k` with `strict` entries, `mulM`/`tenM`/`idM`, `mat-dec`), `Local` (`emb` a k-wire matrix on the bottom wires, `up`/`upk` shifts, the tensor calculus), `Scaled` (`Scaled n = ℕ × Op n` read as (1/√2)^ℓ · M, the localisation relation `_~_`, `Scaled-monoid`). `Semantics.agda` re-exports these and gives the four gate matrices, each √2 times the gate.
  - `Interpretation`: `⟦_⟧ᵍ`, `len` and `⟦_⟧ₒ` by direct recursion on words, `⟦ w ⟧ = (len w , ⟦ w ⟧ₒ)`; `Ext.⟦_⟧ᴱ` is the `StarInterp` reading with `⟦⟧ᴱ-def : Ext.⟦_⟧ᴱ n w ≡ ⟦ w ⟧` and the transports `⟦⟧ᴱ-~`/`~-⟦⟧ᴱ`; the stored reading `⟦_⟧M`; `localise`, `by-matrix` and `by-relator`, which turn one equation of tries at the width a relator is drawn on into the axiom at every width.
  - `Soundness/` — `Relators` (the relators named, `Same` identities by `refl`, and the word equations `pNˡ/pNʳ : rN ↓ᵏ n ≡ …` also by `refl`), `Operators` (diagonal, permutation and controlled operators, `ctrl-⊙`), `BoxMatrices` (`abstract` stored 8 × 8 matrices and the two `refl` identities of the box's recursive step), `Box` (`Λ□-sem`: the box is (1/√2)^ℓ · a diagonal sign; `box-Z-sound`, the schema (19)) — and `Soundness` (Proposition 3.1: `axiom-sound`, `sound`, `axiom-soundᴱ`).
  - `OneQubit` (Lemma 7.1: a sixteen-state automaton normal form, uniqueness decided on stored matrices, `MonoidSem.GetSubPresentation`), `Auxiliary/Syntactics` (Figure 7 over `−1[_]`, `X[_,_]`, `H[_,_]` — the name is spelled with the Unicode minus, `-1` being a literal) and `Auxiliary/Soundness` (Figure 7 is sound for the 4 × 4 matrices, every rule at every choice of distinct indices by `refl` on tries: it pins the transcription and the written-order reading of auxiliary words), `BackAndForth` (the generic encode/decode completeness argument, by `Star-Injective-Simplified`), `TwoQubit` (Definitions 7.2 and 7.3, `e-sem` by `refl`, `by-triesY`, and `Lemma-7-6` parameterised by Theorem 4.4 and Lemmas 7.4/7.5), `Completeness` (Theorem 8.9: widths 0 and 1 outright, width 2 from `TwoQubit` and `TwoQubit/Decoding`, width ≥ 3 from `Section8.Complete` with the encoding's semantics proved and the three Section 8 results as module parameters; `Theorem-8-9′` takes Theorem 4.4 at every width in place of Theorem 4.10, which it then proves; `subpresentation` at every width). No postulates.
  - `TwoQubit/` is Appendices B and C, proved: `Conjugation` (the `Tools` module — `front`/`back`/`mid`, the cancellation helpers, `conj-sym`/`conj-comm`/`conj-invol` — one-wire involutions, `comm-↓↑` for gates on disjoint wires, the swap moving a one-wire gate across, and `module Conj c c²`: conjugation `⟪ w ⟫ = c • w • c` by an involution, with `⟪⟫-•ₖ` distributing over k factors, `⟪⟫-≈` transporting an equation, `⟪⟫-fix`, `⟪⟫-invol`, `⟪⟫-sandwich`; instantiated as `N↑` (X on the upper wire), `N↓` (X on the lower wire) and `S` (the swap), with the action of each on every shortcut — `N↑-°CZ`, `S-°CH`, …), `Auxiliary` (Figure 12, Equations (81)–(110), each family derived once and transported: `eq84`, …, `eq110`, plus the intermediate forms named by content, `CH-°CZ`, `CX-alt : CX ≈ CH • CZ • CH`, `CH-Z↑`, …; (109) and (110) go through the one-wire facts `□-HZ`/`HZ-□` about the box Z • Z°), `Decoding` (Lemma 7.4 on the six generators; Lemma 7.5 on every rule of Figure 7 at N = 4 — the conjugation tables `N↑-dH`/`N↓-dH`/`S-dH`/`…-dZ` say how each conjugation renames a decoded generator, `cX-Z`/`cX-H`/`cX-H′` are Table 1 organised as: conjugating by D(X_[b,c]) is conjugating by CX between two renamings (`via-N↑`/`via-S`/`via-N↓`/`via-E`), and (d3*) is (110) after commuting °CH past Z° on its control). Stated at width `₂₊ n` (Decoding at 2) so the equations are available on the bottom two wires of any circuit.
  - Section 8, on n = m + 3 qubits, all `--safe`: `Auxiliary/Gray` (Definition 8.1's reflected Gray code, `code n`/`index n` mutually inverse — the width is explicit everywhere because `2 ^ n` is not invertible by unification), `Auxiliary/Bitstrings` and `BitstringsLemmas` (wire operations `lookupℕ`/`insertℕ`/`removeℕ` with their commutation lemmas under bounds, `allBits`, the Boolean membership `_∈ᵇ_` — level 6, so parenthesise it under `∧`/`∨` — and `Nodup`), `Auxiliary/Matrices` (the operators of the letters on 2ⁿ basis vectors: `negOp`, `swapOp`, `hadOp`, `⟦_⟧ᴳ`), `Auxiliary/P` (the alphabet P of Definition 4.9 — `−1−1`, `−1X`, `XX`, `HH`, with irrelevant distinctness proofs — and `⟦_⟧ᴾ`), `Auxiliary/Figure8` (rules r20–r46, side conditions as hypotheses, the paper's standing convention that an X or H letter has distinct indices made explicit), `Encoding` (Definition 8.2 through the deciding letter wrappers `zz`/`zx`/`xx`/`hh`, the basis vectors `str₁`/`str₂` of a context, `E-H`/`E-Z`/`E-CZ`/`E-CH` and `e`; Appendix A.4.1's `E-swap`, `E-X`, `ℰ` — the negation of a control, X where the bit is 1: the paper prints ℰ^{1−x}, but Checks8 decides (36)/(37) and they hold only this way round — and `W₁`/`W₂`; Definition E.1's `Σ`/`Σ′`; the H-pattern `hpat`; `view`, a generator as a gate on a wire), `MultiControlled` (Definition 2.4 by layouts: `Λ□`/`ΛZX`/`ΛXZ`/`ΛH` placed by the swap networks `shiftDown`/`shiftUp` and negated by X on the white controls), `Decoding` (Definition 8.3: `dZZ`, `dZX`, `dXX`, `dHH`, `dP`, and `d = rev ∘ dPʷ`), `Reverse` (`rev-cong`: reversal is the syntactic inverse for self-inverse generators), `Section8` (the statements `Theorem-4-10`, `E-sem`, `Lemma-8-7`, `Lemma-8-8` per width and `Complete`, an instance of `BackAndForth`). Conventions: our operator reading of a P-word is the paper's written-order matrix product (its `∘` on P-words is concatenation), the paper's circuit composition `A ∘ B` is our word `B • A`, bit vectors list wire 0 first and the paper's top-first string `x β y` is `y ++ β ∷ x`.
  - `EncodingSemantics/` proves `Section8.E-sem` at every width (`EncodingSemantics.e-sem`): `Auxiliary/WireForms` writes each gate's operator on bitstrings (`hadWire`, `chWire`, `zsgn`, `czsgn`, `form`, `gen-form`); `Products` reads a product of letters (`∏-ℓ`, `∏-M` — pass the word and `{m = m}` explicitly, the width is otherwise unsolvable) and evaluates diagonal products over an enumeration (`∏ᶠ-one`, `∏ᶠ-unique`); `Strings` reads a context and pairing bit back off a basis vector (`ctx₁`/`pr₁`/`str-ctx₁`, `ctx₂`/…); `Diagonal` does Z and CZ (sign letters, one context carries the sign at a vector); `Hadamard` does H and CH by an invariant `part p cov f` over the contexts processed so far (`Pair.step`: one two-level Hadamard on an uncovered pair; `Encode.sem`: any gate whose letters are Hadamards on pairs, instantiated twice). `Auxiliary/Checks8` decides on three qubits that `e` preserves semantics and that every rule of Figure 8 is sound at every admissible index — the check that pins the transcription of Figure 8, Definition 8.2 and A.4.1 (it found the missing distinctness conventions of (30), (34), (42) and the negation parity of W₁/W₂). Its shape is dictated by how Agda compares decisions: each evaluation module exports its rule pointwise (`∀ a … → (guards ⇒ decide u t) ≡ true`, from one `refl` on the closed conjunction through `all8ₖ-true` with the predicate explicit), `sound` writes every word out, pins `toℕ {8}` where the evaluation modules elaborate it so, and states its cases through the abstract `Sound` — a `decide u t` with free indices that is not *syntactically* the expected one is compared by unfolding both, which is exponential in the word. `Auxiliary/DecodingChecks` does the same for the decoding `d`: every sign pair and every signed exchange on eight indices (`zz-all`, `zx-all`) and samples of the exchange pairs and of the Hadamard pairs, on and off the H-pattern and with repeated indices — about three minutes, by the boolean check on the row-operation reading (the dense `⟦_⟧M` compared by `mat-dec` cost about 1.27ⁿ in the n gates and reached only five letters under 43 gates; a Hadamard pair off the H-pattern has some 1800).
  - Appendix D, the auxiliary equations on n ≥ 3 qubits, follows the paper's own architecture: an n-qubit derivation uses completeness one width down as a black box (its Lemma 5.1; "by Lemma 7.6" inside a three-qubit proof, "by Lemma D.3" inside a four-qubit one). `Weakening` replays a derivation on n ≤ 4 wires on the bottom wires of any wider circuit (rule by rule; the bound excludes the schema (19), which starts at five wires and whose box holds every wire of its width), and `TopWeakening` removes the bound without looking at the rules: `top w` is w with one more idle wire on top, the cycle `σ n` of swaps carries `w ↑` to `top w` (`nat`, from the swap rules (f1)–(f4) one gate at a time), so `top-cong : n ⊢ w ≈ v → (1 + n) ⊢ top w ≈ top v` is cong↑ between σ and σ⁻¹ — in particular (19) holds on the bottom wires of any wider circuit (`box-Z-top`; `box-Z₅ m` for the five-wire box in the `↓ᵏ m` spelling, where `top (w ↓ᵏ m)` and `w ↓ᵏ suc m` agree definitionally because the word is closed — for a symbolic width they differ by `+-suc`, which is why the general statement is over `topᵏ`); `SemanticSteps` turns one evaluation `Same u v` — `Eq.refl` on one boolean (`Semantics.Decide.eqM`) over the row-operation reading `Evaluation.⟦_⟧R`, under a second for an 80-gate three-wire word — into a derivation at width k, on the bottom k wires (`Below.by-sem`) or one wire up (`by-sem↑`), given completeness at k ≤ 4 as a module parameter. `ThreeQubit/` is parameterised by `complete₂` (completeness on two qubits) and nothing imports it yet: `Figure13` (Lemma D.1, six two-wire equations, each one `by-sem`), `Blocks` (a two-wire circuit on the lower, upper or outer pair — `L`, `U`, `O u = Ex ↓ • U u • Ex ↓` — rewritten by evaluation with `L-sem`/`U-sem`/`O-sem`; disjointness `L-comm`/`U-comm`/`Z↑-O`…; `O-•`, `O-invol`, `O-bot`/`O-top`; `U-shift`, the swap rules (f1)–(f4) for a whole block, and `O-L : Ex ↑ • L u • Ex ↑ ≈ O u`) and `Auxiliary` (Lemma D.2, all of (117)–(147); typecheck it directly, under a minute). Its method: `WordAlgebra`, facts about words over any relation, its own module so that the four- and five-qubit stages can share it (`comm-abab`, `merge`, `pass-pqpq`, `pass-xy`, `comm-•`, `unwrap`, `invol-comm`, `twist`, `inv-unique`), conjugation modules for every involution in play (`S↑`/`S↓` the swaps, `N₁`/`N₂` X on wire 1/2, `PP↓` P ⊗ P below, `K` the lower CZ, `Zc`, and a local `R` = X H X on wire 0, which turns H into −H), and equations transported along them with `⟪⟫-≈`. (139)/(140) and (144)/(145) come from which gates exchange CCZX and CCXZ; (146) and (147) each say that a word of involutions equals its reverse, i.e. is an involution — (146) because P ⊗ P and (14) turn it into a palindrome, (147) from (146) by `twist` after identifying the discrepancy as a gate that passes `(a A a) A` for the reason that H and −H commute, which reaches the axioms as (17) through P ⊗ P and the swaps. These two replace some sixty diagram steps of the paper. `ThreeQubit/sim.py` is a pure-Python simulator (WSL python has no numpy; 2ᴺ × 2ᴺ float matrices, words as gate lists in our time order, `eq`/`comm`/`invol`) used to test every intermediate claim before writing Agda — set `N` for the four- and five-qubit appendices.
  - `FourQubit/` is Appendix D.3 (Lemma D.5, Equations (150)–(248), all proved; `FourQubit/LemmaD5.agda` re-exports them as `eq150` … `eq248`, an equation with several instances carrying a suffix — `eq228₁`/`eq228₀`, a prime for the mirrored statement), parameterised by `complete₂` and `complete₃` — Lemma D.3 is the induction hypothesis, completeness on three qubits. `Blocks` re-exports ThreeQubit.Blocks and adds the four triples of wires (`L₃` 012, `U₃` 123, `O₃` 023, `M₃` 013, each with `-sem`/`-comm` by one evaluation on 8 × 8 tries), the pairs `P₂₃`/`P₁₃`/`P₀₃` beside `L`/`U`/`O`, the swap conjugations `S₀₁`/`S₁₂`/`S₂₃`, the fusion lemmas (`L₃-L`…, as `≡`, and `M₃-L`/`M₃-U`/`M₃-O`), coherence (`τ-P₀₃`: Definition 2.4's τ₀₂-spelling of a gate from wire 3 to wire 0 is `P₀₃`), disjoint pairs (`P₂₃-L`, `P₁₃-O`, `P₀₃-U`) and the twelve `comm-ij-kl`: two pairs with a common wire commute when they do on their triple. `Auxiliary` ((150)–(153)): `CZ₃₀`/`CH₃₀`, `box₃ = Λ□ 3 ↓ᵏ n`, and the paper's key trick as a lemma — `W-split`, CCZX as five factors each on at most three wires (controlled H Z from wire 1, CNOT, controlled Z H, CNOT, controlled H Z from wire 2), so that a gate on the pair 03 passing those factors passes CCZX and CCXZ (`W-pass`/`V-pass`). `Box` ((154)–(163), (165), (166)): the box `W c V c` passes Z, H, X on its box wire (`WordAlgebra.box-pass`, from `V c W = W c V`), the CZ and CH gates onto wire 0 (`box-pass₂`), and is symmetric in its controls — wires 1, 2 by (127), wires 2, 3 by `WordAlgebra.comm-swap`: the CNOT from wire 3 to wire 2 carries the relation CCZX² = CZ ↑ of a = CH ↓ and b = CZ₂₀ to one of a and b c, and the three relations force the commutator of a b a and c to be that of b and a c a; then (165) and the involution (166). `Controlled` ((164), (167)–(171); (170) merges the box with its version negated on wire 2 into the CZ of wires 1 and 3, and (171) is its image under P ⊗ P for the doubly controlled H). `Rotations` ((172)–(178)): the doubly controlled ZX from wires 2, 3 (`CCZX₂₃ = O₃ CCZX`) against the lower CZ, P ⊗ P under CCZX ↑, the box with its box wire on wire 1 (`box₃′ = S₀₁.⟪ box₃ ⟫`) against a controlled ZX on that wire and against the negated CCXZ — (139)/(140) through the lower swap — and the two decompositions of the box over `box₃′`. `Colours` ((179), (180), the first of the black-control-against-white-control family; `L₃-top`: the lower triple passes anything on wire 3; `N₃`, X on wire 3). `ControlledH` ((181), the first long one; its three-wire evaluations live in `Evals181`, 8 min, the derivation itself 12 min): the doubly controlled H with its H on wire 0 and box wire 3 (`ΛH₂′`, ΛH 2 under the three swaps) is a rotation on its box wire around the CH from that wire, R e R⁻¹ e (`ΛH₂′-form`, by a `shape` lemma that conjugates a word (r t r t) u (t r t r) u letter by letter, through the swaps, P ⊗ P on the pair 03, and X on wire 2); the box on wire 1 is V₁ °q W₁ °q (`box₃′-form`); W₁ = CCZX ↑ passes R and e, and °q passes R and — the rule (17) on the wires 0 ← 3 ← 1 — the conjugate e s e. `Colours2` ((182)–(189), seconds): (181), or (181) with the colours on the shared wire exchanged by X (`eq181ᵇ`), transported along wire permutations with `tr`; the gates are spelled as the conjugates this produces (the header tabulates which H(h, t; controls) and Box(u; controls) each is — (188) is drawn identically to (181)), not yet identified with Definition 2.4’s own swap network for the same placement; (190) there too — the H gate on the box wire of a box of the other colour: (177) negated on wire 2, then the H gate passes the box on wire 1 by (181), the lower CH factor by factor and the white CZ from wire 2 by the rule (17) on the wires 0 ← 3 ← 2, with the H gate in its second form `ΛH₂′-rot′` (rotation controls in the other roles; `°CZ₂₀-ΛH₂′` and `CZ°₂₃-ΛH₂′` are exported for later colour arguments). `Ev` restates the evaluation lemmas of `Blocks` with implicit words (`O₃-ev ev`, `M₃-comm-ev ev`, …): **a stored `Evaluated` fact must be applied through these**, because `L₀`/`U₀`/`O₀` live in a parameterised module and a re-typed word refers to another instantiated copy, which the conversion checker compares by re-evaluating both (it cost ControlledH ten minutes). `Permutations`: the braid relation is the swap rule for the swap itself (`U-shift Ex`), `braid-conj`/`braid-conj↑`, swaps on disjoint pairs (`far`), the transpositions (1 3) and (0 3) as conjugation modules `T₁₃`/`T₀₃` with their nestings (`T₁₃-nest`, `T₀₃-nest`, `T₀₃-nest′` = (1 3)(0 1)(1 3)), the action of (1 3) on a two-wire circuit placed on any pair (`T₁₃-L`, `-O`, `-U`, `-P₂₃`, `-P₁₃`, `-P₀₃`), on X (`S₂₃-X₂`, …, `T₁₃-N₂`) and on the gates (`T₁₃-box₃`, `T₁₃-°box₃`, `ΛH₀₁ = S₀₁.⟪ ΛH 2 ⟫` with `T₁₃-ΛH₂′`, `S₂₃-ΛH₀₁`, `S₁₂-ΛH₂′`), and `N₂-N₃`. `Colours3` ((191)–(200)): transports of (190) and (196); a second white control comes from (170) — the box with two white controls is the box with one times a CZ negated on the shared wire, which the H gate passes ((195), (197)); (196), box and H gate on the same box wire, moves the box wire of the box to wire 3 by (178) under the cycle 0 → 1 → 3 → 0 (`box-13`) and is then (175) and (181). `HGates` ((201), (202)): a doubly controlled H is a box between two P ⊗ P on its H wire and its box wire; on a triangle of wires the three P ⊗ P form a Klein four-group ((130); `klein`, `klein-ca`, `klein-ab`, `klein-ba`), so that two H gates on one triangle commute iff one box commutes with one H gate — conjugate by the third P ⊗ P; (201) is (171) carried to the H gate with its box wire on wire 3 (`eq171ᶜ`: the two colours merge into CH ↓). `HGates2` ((203)–(207)): (204), (205) are (181), (189) through `klein`, (206) is (179) between the same P ⊗ P, (207) is X between P ⊗ P being Z H Z; (203) — two H gates on the same box wire, where the third P ⊗ P sits on two controls of a box and makes no H gate — moves both boxes' box wires to wire 1 by (177) under (0 3), so that P ⊗ P does make H gates, and the four kinds of factor commute pairwise: rotation against rotation by the one new evaluation (`Evals203`, 328 gates, 5½ min), rotation against H gate by `ev-°R-CCZX` of (181) (the H gate on wires 0 1 is `CCXZ ↑ • CH ↓ • CCZX ↑ • CH ↓`), H gate against box by (196). This replaces the paper's three pages. `Rotations3` ((208)–(215)): the triply controlled ZX/XZ `ZX₃ = ΛZX 3 ↓ᵏ n` = a B a B; a is the product of the two colours of the H gate (171) and the white one passes B (181) — (210); B is the CZ of wires 0 3 times its white version (170), which the H gate passes — (212); inverses by `inv-unique`; each form is fixed by one swap of controls ((214), (215)). `Crossed` ((216)–(218)): the triply controlled ZX on wire 1 (from 0, 3 and, negatively, 2) against CCZX — a *crossed* colour statement, the target of each gate a control of the other, which no decomposition makes factor-by-factor (the figure of the paper's proof did not parse: as drawn it is not semantically valid). As for (139) on three wires: CCZX = p q p q by `symm-controls` (p = CH₂₀, q = CZ ↓) and `pass-pqpq`; p passes the gate K in the form °G′ c′ °G′ c′ (from (212); p against the white H gate is (164) transported through its P ⊗ P form); q K q = Z₁ K Z₁ (the definition a′ °B a′ °B; q and Z₁ pass the box) = c′ K c′ = K⁻¹, because K passes Λ(°3)(Z₁) = c′ Z₁ — in the form G₃ °c₃ G₃ °c₃, (212) for the other control with the box wire of the H gate on wire 2, where X passes it ((207)), and then `°CZ₂₀-ΛH₂′` transported. Colour statements are thus of two kinds: uncrossed (decompose until factor-by-factor) and crossed (`pass-pqpq` through `symm-controls`). `Merges` ((219)–(226)): (219) and (220), a white H gate against a rotation — (219) the XZ in the form B G B G with the box wire of its H gate on wire 2 (`XZ₃-form₄`), whose letters the H gate passes by (193) and (203); (220) the rotation from wires 1 3 as p q p q by `symm-controls` on the triple 0 1 3, where between the P ⊗ P of the H gate p meets a box of the other colour ((164)) and q becomes a CH onto the box wire ((163); `Evals219`); and the merges (221)–(226): in the forms G B G B and °G °B °G °B of (210) every black letter passes every white one ((181) twice, (201), (170)), so the product shuffles (`shuffle`) into (°G G)(°B B)(°G G)(°B B) = a c a c, the ZX from wires 1 3 (`merge₀`, `merge₀′`); the rest by inverses (`inv-unique`, `unwrap′`) and the symmetry (214). `PConjugates` ((227), (228)): between P ⊗ P on the wires 1 2 the H gate H(0, 2; 1, °3) is, by the Klein four-group on 0 1 2, the white box on wire 2 between P ⊗ P on 0 1 (`Y-form`); it passes every letter of both colours of the triply controlled ZX in the form G₂ B G₂ B — B by (189), °B by X on the box wire, G₂ by `klein` and (198) — hence their merge, the ZX from wires 1 3; (228) is then (227) and (220) letter by letter, a white control on wire 1 adding by (171) the CH from wire 3 negated there. `Rotations4`/`Rotations5` ((229)–(238); `Evals229`): gates that pass the triply controlled rotations letter by letter in the right form; (234) reuses the (216) machinery (`CH₂₀-S₀₁°ΛH₂′`); (236)–(238) are the paper's own short arguments — H is the CH from wire 1 in both colours, the black one a letter of a B a B, which it turns over, the white one passing by (232) under (215); Z likewise with the CZ from wire 3, (213) and (231); X = H Z H. `Families` ((239)): t c t c with t the white CH from wire 2 and c the CZ from wire 3; the white c passes ZX₃, the black one exchanges ZX₃ and XZ₃ and `pass-pqpq` applies. `Families2` ((241)) and `Families3` ((240)) are the first families with parametrised control colours, sixty-four cases each, stated over `Bool` (`N₁ᵇ`/`N₃ᵇ`/`N₀ᵇ` a control of either colour, `rot` the sign; black is `true`): X on a wire that controls both gates exchanges both colours there (`flip₁`, `flip₃` — stated as `N⟪ … (not δ) … ⟫ ≈ … δ …` by cases, since `not (not δ)` does not reduce), X on the target of one gate exchanges the other's colour there and turns the first over ((238), `N₀-rot`), ZX and XZ are inverse (`all-rot`, `comm-inv`), which leaves (241) with four cases — the merge in both orders; the merge on wire 3 plus `pass-pqpq` on B a B a against the white doubly controlled ZX ((176)); the same under (1 3); and one three-wire evaluation (`Evals241`) — and (240) with two: (216) one wire up (the first gate as P q P q with P the H gate on the wires 0 1, (212) under (1 3); P passes K by (204) and a CH onto a box wire), and the merge on wire 3 plus (216) mirrored. `PForms` ((246), and the forms behind (242)–(245)): (246), P ⊗ P on the pairs 2 3 and 0 1 is P ⊗ P on 1 3 and 0 2, is two Klein four-groups sharing P ⊗ P on 1 2; the rotation of (241) between P ⊗ P on the wires 0 1 (`Aᴾ α β a`) is again a word of two H gates, G′ P G′ P (`Aᴾ-form`: the box becomes an H gate, the H gate another one by the Klein four-group on 0 1 3), and a white control on wire 1 splits off, by the merge on that wire (`merge₁`), the doubly controlled ZX from the wires 2 3, which P ⊗ P turns over ((174) under the lower swap, `Aj-CCZX₂₃`; `Aᴾ-white`). `PFamilies243` ((243)) and `PFamilies242` ((242)): `Aᴾ` against the second gate of (241), resp. (240) — X on wire 3 and inverses reduce colours and signs, both black is letter by letter ((203), (192), (202), (196), (197); resp. (201), (182), (204), (194), (195)), a white control is a merge whose doubly controlled factor passes by (239), resp. as the merge of two gates of (240). `PFamilies247` ((247), (248); `Evals247`): between P ⊗ P the white CZ is a CH onto wire 1 (or 0), which passes the rotation in the form (212) letter by letter ((164), (230), (232), (233)). `PFamilies245` ((245)) and `PFamilies244` ((244)) — gates of opposite colours on wire 3, semantically trivial, syntactically not: for (245) the rotation G₂ B G₂ B between P ⊗ P on the wires 1 2 is Yᵇ Jᵇ Yᵇ Jᵇ, the letters of (227) and (205)/(199) with the colours on wire 3 exchanged, and a white control on either gate is split off by the merge on its wire and passes by (228); for (244) the first gate has its *target* between the P ⊗ P, so X on wire 1 — Z H Z between P ⊗ P, exchanging ZX and XZ by (236)/(237) — flips the colour of the second gate's control there for free (`flipF`, `flipS`), both black is [Kᵇ, Y₁] letter by letter ((206), (184) bridged to `J′`, and for Y the Klein four-group with (198), and (183): `Y-G₃`, `Y-box₃` in `PConjugates`), and white on wire 2 is the merge (221), whose factor — the doubly controlled ZX on wire 1 from the wires 0 3, its own inverse between P ⊗ P by (174) — passes the second gate by (216) under the lower and the upper swap. Before transporting, look for an X that is a symmetry of the *family*: it costs a few lines where a merge costs a page. The paper's route for each equation is cheapest to find from the equation numbers cited under its proof (`grep "Proof of Equation"` in the text of the PDF), not from the figures. Identifiers must not contain combining characters (b̂, V̂ are two code points; use precomposed ẑ, Ŵ). Typecheck `FourQubit/LemmaD5.agda`: under a minute on top of the three-qubit layer (it was about an hour, ControlledH alone forty minutes, nearly all of it three-wire evaluations, before `Same` was decided by one boolean on the row-operation reading). **A lemma must never take `Same (… u …) …` with a free word variable u as a hypothesis**: the conversion checker unfolds `Same` and normalises trie products with a neutral factor, which exhausts 8 GB; take `SemanticSteps.Evaluated` (a record, compared by its two words; `evaluated Eq.refl`) and project `.same` where the evaluation lemma is applied. `_•_` (infixr 7) and `_↓ᵏ_` (infixl 7) do not mix: parenthesise `(ΛH 2 ↓ᵏ n)` or name it (`box₃`).
  - `FiveQubit/` is Appendix D.4 (Lemma D.7, Equations (249)–(268), all proved; `FiveQubit/LemmaD7.agda` re-exports them: `Auxiliary` has (249)–(252), `Colours` (253)–(260), `Rotations2` (261), `Rotations` (262), `PFamilies263` (263)–(266), `PFamilies267` (267), `PFamilies268` (268)), at width `₁₊ (₄₊ n)`, parameterised like `FourQubit/` (the paper's Lemma D.6, completeness on four qubits, is cited only in the proofs of (249), (256) and (261), and (249) did not need it). Five wires is where the schema (19) enters, and (249) *is* (19): the five-wire box is W B V B with W the doubly controlled ZX on wire 0 and B the four-wire box on the wires 0 1 3 4; Z on wire 0 exchanges W and V and passes B, so (19) says V B W B = W B V B, i.e. W² = CZ ↑ passes B (`square-passes`) — the box passes a CZ between its box wire and the fifth wire — and the transposition τ₀₂ turns that into (249). (251), the box does not depend on its box wire: the swap is a word in CZ and H, (8), and the box passes each letter ((249), (155), disjointness). (250), P ⊗ P on the box wire and the fifth wire: it turns the letters of W c V c into those of the form (153) mirrored ((174); the CZ from wire 4 spelt from the pair 1 2 upwards so that only the rule (18) is met; `flip-WcV`). (252), the same for the doubly controlled H: moving its box wire changes its P ⊗ P by the one on the wires 0 1 (Klein four-group), which (250) absorbs. `Shift` carries (251)/(252) from the frame of the wires 1–4 to the gates of the four-qubit development, which sit on the wires 0–3: `shift` is `TopWeakening.nat` for the cycle c₄ under further idle wires (for a family `f m : Circuit (₄₊ m)` with `top (f m) ≡ f (suc m)` — `λ m → Eq.refl` for a closed word under `↓ᵏ`), along P = c₄ φ (φ the cycle of the wires 0–3) the swap of the wires 0 1 becomes that of the wires 3 4 (`Ex-P`) and a gate F one wire up becomes F under the three lower swaps (`F-P`), and `transport` carries an equation along a word that is not an involution; so the swap of the wires 3 4 passes the box and the doubly controlled H with their box wire on wire 3 (`Ex₃₄-box₃‴`, `Ex₃₄-ΛH₂′`), hence their images under X and swaps away from the wires 3 4. `Colours` ((253)–(260): a black H gate on the wires 0–3 against a box or H gate on the wires 0 1 2 4, white on wire 2, spelled `S₃₄.⟪ four-wire gate ⟫`): each is an equation of Lemma D.5 once a box wire has moved so that both gates sit on the same four wires — the first gate's from wire 3 (`move`: (192), (182), (201), (203)), the second's from wire 4 (`move′`: (204), (202)), in (255) both from wire 1, under the cycle ψ that carries wire 1 to wire 4 and the braid relation ((186)); (256), where the two gates need all five wires and the paper uses four-qubit completeness, is the box in its form (177) negated, °W B′ °V B′, with B′ the box of (255) and °W passed letter by letter (`rot-comm`: the H gate is an involution of the form V a W a, so it passes a). `Rotations` ((262), (174) one level up): between P ⊗ P on the wires 0 1 the CH of a B a B becomes the CZ (rule (18)) and the box, by the Klein four-group and (250) under the middle swap, the H gate on wire 1, which is the XZ in its form (213). With the old dense evaluator a four-wire evaluation was not an option (36 gates did not finish in ten minutes), so the paper's uses of Lemma D.6 were replaced by structure; now that `Same` is decided on the row-operation reading, four-wire evaluations are cheap (760 gates in about four seconds, `GeneralN/Base335`). `Rotations2` ((261), F = Λ(4, °2, 1)(ZX₀) against the doubly controlled XZ from the wires 2 3, four uses of Lemma D.6 in the paper): F is °G °B₄ °G °B₄, (210) negated on wire 2 with the H gate's box wire back on wire 3; the H gate passes the XZ in the form c h c h with the controls' roles exchanged ((14): c the CZ from wire 2, of the other colour, h the CH from wire 3, around which the H gate is a rotation — `rot-comm` again), and the box, under the cycle that carries wire 1 to wire 4 (`cycle₁₄`, from (255)), is the box on wire 2 against the XZ on the wires 0 1 2: (176) negated, under the middle swap. **Read the control dots at 400 dpi**: (261) was first misread as F against the *triply* controlled XZ₃ — that statement is proved too (`F₂₆₁-XZ₃`) and is harder: with a the CH below, F = a °B₄ a °B₄ and XZ₃ = B a B a, and the four letters B, a B a, °B₄, a °B₄ a commute pairwise as soon as B = box₃′ passes °B₄ and F (a °B₄ a = F °B₄); F = °G c °G c by (212) — B passes °G by (181) and c = CZ(4,0) because B is invariant under the transposition (1 4) (`τ`) and passes CZ ↓ ((160)); °B₄ = B₄ c by (170), and B passes B₄ because both are Ŵ d V̂ d over the same rotation with commuting CZs, each passing the other's (`two-boxes`) — (249) along P (`CZ-P : CZ ↓ • P ≈ P • CZ ↑ ↑ ↑`, one three-wire evaluation; `e249`, (249) for the box on wire 3 of the wires 0–3) and then (1 3). `Shift` also exports the five-wire swap calculus (`far₀₃`, `far₁₃`, `braid₃₄`, `conj-swap`, `braid-conj₃₄`, `push-φ`). `PFamilies268` ((268), which is (243) with the second gate's control moved from wire 3 to wire 4): both black, `Aᴾ` = G′ P G′ P is a word in the first gates A₁, A₀ of `Colours` and the second gate, °G₄ °B₄ °G₄ °B₄, one in its second gates, so the letters pass by (258), (253), (260), (255); a white control of the second gate on wire 1 is the merge on that wire, whose doubly controlled factor passes `Aᴾ` by (261) under the swap of the wires 3 4, negated on wire 2, and then between P ⊗ P ((174), `PP-R₁′`); a white control of the first gate on wire 1 is `Aᴾ-white` and (261) itself (X on wire 1 for the other colour of the second gate); on wire 3, X, which the second gate does not see (`X₃-S₃₄`, from `L₄-top`: a circuit on the wires 0–3 passes anything on wire 4). `PFamilies267` ((267), likewise (242)): both black by (257), (254), (259), (256); **every colour is then a symmetry of the family**, as in the paper — X on wire 3; Z H Z on wire 1, which between P ⊗ P is X on the first gate's control and on the second gate's target exchanges ZX and XZ ((236), (237)); X on wire 0, which between P ⊗ P is Z H Z on the first gate's target and changes the colour of the second gate's control — so no merge is needed. `Pairs`: (262) for the gates on the wires 0–3 — along P a symmetric two-wire circuit on the wires 0 1 becomes the same on the wires 3 4 (`pair-P`, generalising `CZ-P`; `transport₂` carries x y = z x), so P ⊗ P on the target of a rotation and the fifth wire turns it over (`PP₀₄-rot`, with white controls `PP₀₄-S`, `PP₀₄-V`; target on wire 2, `PP₂₄-J`). `PFamilies263` ((263)–(266): a rotation on the wires 0 1 2 4 between P ⊗ P on the pairs 2 3 and 0 1 against one white on wire 4): under the swap of the wires 3 4 everything sits on the wires 0–3 and wire 4 carries only P ⊗ P, on the pairs 2 4 and 0 1, which by (246) (the other way: `e246′`) are the pairs 0 4 and 1 2; P ⊗ P on a target and the idle wire turns a rotation over, and what is left is (244) ((264)), (245) ((266)), (244) under the transposition of the wires 0 2 ((263), `e244θ`), and (244) negated on wire 3, under the middle swap and between P ⊗ P ((265), `e244ᴾ`) — `conj-inv-comm`: if x turns y over and f passes y turned over, x f x passes y. With the old dense evaluator a four-wire evaluation was not an option (36 gates did not finish in ten minutes), so the paper's uses of Lemma D.6 were replaced by structure; now that `Same` is decided on the row-operation reading, four-wire evaluations are cheap (760 gates in about four seconds, `GeneralN/Base335`). `Rotations2` ((261), F = Λ(4, °2, 1)(ZX₀) against the doubly controlled XZ from the wires 2 3, four uses of Lemma D.6 in the paper): F is °G °B₄ °G °B₄, (210) negated on wire 2 with the H gate's box wire back on wire 3; the H gate passes the XZ in the form c h c h with the controls' roles exchanged ((14): c the CZ from wire 2, of the other colour, h the CH from wire 3, around which the H gate is a rotation — `rot-comm` again), and the box, under the cycle that carries wire 1 to wire 4 (`cycle₁₄`, from (255)), is the box on wire 2 against the XZ on the wires 0 1 2: (176) negated, under the middle swap. **Read the control dots at 400 dpi**: (261) was first misread as F against the *triply* controlled XZ₃ — that statement is proved too (`F₂₆₁-XZ₃`) and is harder: with a the CH below, F = a °B₄ a °B₄ and XZ₃ = B a B a, and the four letters B, a B a, °B₄, a °B₄ a commute pairwise as soon as B = box₃′ passes °B₄ and F (a °B₄ a = F °B₄); F = °G c °G c by (212) — B passes °G by (181) and c = CZ(4,0) because B is invariant under the transposition (1 4) (`τ`) and passes CZ ↓ ((160)); °B₄ = B₄ c by (170), and B passes B₄ because both are Ŵ d V̂ d over the same rotation with commuting CZs, each passing the other's (`two-boxes`) — (249) along P (`CZ-P : CZ ↓ • P ≈ P • CZ ↑ ↑ ↑`, one three-wire evaluation; `e249`, (249) for the box on wire 3 of the wires 0–3) and then (1 3). `Shift` also exports the five-wire swap calculus (`far₀₃`, `far₁₃`, `braid₃₄`, `conj-swap`, `braid-conj₃₄`, `push-φ`). Open: (263)–(266), a rotation between P ⊗ P on the pairs 2 3 and 0 1 against one white on wire 4 (the paper cites (262), (246), (244), (245)), and (267), (268), which are (242), (243) with the second gate's control moved from wire 3 to wire 4 — both black they are letter by letter (259), (256), (257), (254), resp. (260), (255), (258), (253), since `Aᴾ` is a word in the first gates A₁, A₀ of `Colours` and the second gate one in its second gates; a white control of the first gate on wire 1 is `Aᴾ-white` and (261). Typecheck `FiveQubit/LemmaD7.agda`, which re-exports `eq249` … `eq268` (half a minute on top of `FourQubit/LemmaD5`).
  - `GeneralN/` is Appendix D.5 onwards (Lemma D.8, Equations (269)–(307), begun), the equations on any number of wires, by induction on the number of controls of `Λ□ k` (box wire 0, controls 1 … k; `Λ□ (₃₊ k) = W • B • V • B` with `B = τ₀₂-conj (Λ□ (₂₊ k) ↑)` is (269), by `refl`). The paper calls an (n − 1)-qubit instance of a later equation "Lemma 5.1"; here it is the induction hypothesis, so no completeness at width ≥ 4 is assumed (and none could be used: see the four-wire evaluation above). `Box`: `Z[ i ]`, Z on wire i, over `Fin`; (270), Z on a control passes the box — Z on wire 1 or 2 passes W and V ((119)–(122)) and becomes under τ₀₂ Z on the smaller box's box wire (`box-Z₀`: (19), (154), or disjointness) or on a wire it does not touch, Z higher up passes W, V and τ₀₂ (`L₃-top`) and is on a control of the smaller box; (272), the box one wire up passes the CZ of the wires 0 1, which *is* the schema (19) at every width, as (249) was on five wires — `square-passes` without assuming B an involution (B is cancelled on the right by `Basis-Change.bbc`, every word having its reverse as inverse); (271), the CZ of the first control and the extra wire, from (168) for the rotation and (272) one size down for the smaller box, met on its box wire between transpositions of the wires 1 3 which that CZ passes. (286), the box is also V B W B (`mirror`: the content of (19) before the square is taken; (124) and `flip-WcV` on four wires), whence `d-B□`, the CZ of the wires 1 2 passes the letter `B□ k`; (273), the CZ of the box wire and the control on wire 2 — the letter b of W = a b a b, with b W = W d b by (128) and both d and b passing B (`box-pass₂`; b passes B because under τ₀₂ it is (271) for the smaller box). **The rest of Lemma D.8 is a mutual induction on the width**, read off the equation numbers cited under each proof: (285)ₙ (H on the box wire passes the box) ← (284)ₙ (the box with a multi-controlled H in place of B, the general (153)) ← (283)ₙ ← (285)ₙ₋₁, (303)ₙ₋₁, (274)ₙ, (277)ₙ, (282)ₙ; (274)ₙ (box wire independence) ← (285)ₙ₋₁, (272); (276)ₙ ← (284)ₙ₋₁, (174), (272); (277)ₙ ← (276), (274); (299)ₙ (the box is an involution) ← (273), (298)ₙ ← (285)ₙ, and (286); and a few proofs reach into later lemmas one or two widths down ((312), (317), (355)). Without (299) B is not known to be an involution, which is why `square-passes` cancels B by its reverse. `HGate`: (281) and (303), H on the H wire resp. the box wire of the multi-controlled H — λH (k + 2) is the box between P ⊗ P on the wires 0 1, H there is Z, (113), and Z passes the box by (270) resp. the schema (19) (`through`, a generic lemma: h is z between p, and z passes q, so h passes p q p); and `box-CZ↓`, the box passes the CZ of its box wire and the control on wire 1, by `box-pass` with `K-W`/`K-V` (that CZ exchanges W and V), the induction hypothesis under τ₀₂, and `flip□` — (286) with one B cancelled.

    **The shape of the rest.** Lemma D.8 and everything after it is *one induction on the width*, not a linear development: the paper's standing convention (§D.5, first paragraph) is that a citation of a later equation with no subscript means its (n − 1)-qubit instance, "just a call to Lemma 5.1", while `(XX)ₙ` is a genuine back-reference at the current width. Read that way the citation lists (`scratchpad/cites.py`, which prints them per equation) give, at a fixed width, the order 274 → 276 → 277 → 278 → 279 → 280 → 282 → 283 → 284 → 285 → 298 → 299 → …, each step using the previous width freely. **Every one of these passes through (283), and (276), (278), (283) are the only three proofs in which Lemma 5.1 is used *semantically*** — ten steps in all, where the transformation is no numbered equation but an equality of (n − 1)-qubit circuits that holds because both sides denote the same operator. So the general-n development needs completeness at n − 1 as a module parameter, which is the paper's own global induction (`complete<`), and an operator-level proof of each of the ten steps at symbolic width (`Soundness/Operators`: `diag`, `ctrl`, `ctrl-⊙`, `emb-ctrl`, `perm-conj`, and `Soundness/Box.Λ□-sem`, which already gives the box's denotation at every width). There is no way round them by harvesting easier equations first: (287)–(307) all cite (298) or (299), which sit above (285).

    The one place where a structural shortcut is still worth trying is the keystone itself. (284) — the general (153), the box with a multi-controlled H in place of B — is, given that B and E = PP₀₁ B PP₀₁ are involutions, equivalent to *E B passing V*: the four-wire proof (`FourQubit/Auxiliary.eq153`) is exactly this, with B = CZ₃₀, E = CH₃₀ and `V-pass` for the two-wire gate E B = P₀₃ (CH • CZ) on the pair 0 3. (285) then follows, since H ↓ passes E (between P ⊗ P it is Z on a control of B) and exchanges W and V. Read at 400 dpi, (284) is exactly `Λ□ (₃₊ k) ≈ CCZX • E • CCXZ • E` with `E = PP ↓ • B□ k • PP ↓`, the multi-controlled H with its H on wire 0, box wire 1 and controls 3 … k + 3; and (285) is exactly H on the box wire commuting with the box. Since `B□ k` denotes Λ(3…k+3)(Z₀) and E denotes Λ(3…k+3)(H₀), the content of "E B passes V" is that **Λ(3…k+3)(ZH₀) commutes with Λ(1,2)(XZ₀)** — same target wire, disjoint controls, commuting payloads. The four-wire case is `eq152`'s `ec-V`, by `W-split`; what blocks the general case is that E • B has no spelling avoiding wires 1 and 2, so `W-split`'s disjointness step does not apply. And (299), the box is an involution, is the four-wire `eq165`/`eq166` verbatim once B² = ε one size down and *B passes Λ□* are known; with W² = d, V = W⁻¹ and d passing B (`d-B□`), that last one is equivalent to **(B • W)⁴ = ε**, which is where the route currently stops. All of these equivalences were checked numerically at five wires (`scratchpad/t284.py`) before being written down — the repo's standing discipline. `BoxWire`: (274), the box wire is an identity wire, from the keystone — the swap IS a word in CZ and H, since Equation (8) is how `Ex` is spelled in `Syntactics` and `_↓` is the identity, so `Ex ↓` unfolds definitionally to `CZ ↓ • H ↓ • H ↑` three times over, and the box one wire up passes each letter: the CZ by (272), H on the idle wire 0 by `comm-gate₁-w↑`, H on the box wire by (285), which is a module-level hypothesis `Eq285 k` (at k = 0 it is the four-qubit (155), `eq285-0`, and in the paper's induction it arrives from the previous level, which is why the paper cites it as (285)ₙ₋₁). `BoxWire` also has (275), the controls on the wires 1 2 can be exchanged: the box is W B V B, the swap of those wires fixes W and V by (127) and carries the letter B to the same letter with its box wire moved, which is (274) one size down — transported by the braid relation alone, `Ex ↑ • τ₀₂ ≈ τ₀₂ • Ex ↓`, since τ₀₂ is `Ex ↓ • Ex ↑ • Ex ↓`; the four-qubit case `eq275-0` is (157).

    **(284) is the single keystone.** Tracing the reductions: (285), H on the box wire, is equivalent to the *mirror* of (284) — H ↓ exchanges W and V and passes E (between P ⊗ P it is Z on a control of B), so `H ↓ • (W E V E) ≈ (V E W E) • H ↓` and (285) says the two orders agree, exactly as (286) does for the B-form. (276), P ⊗ P on the box wire and the idle wire below, is the general form of the five-qubit (250) and needs (284) at the box's own size plus (174), the frame shift between `PP ↓ • B ↑ • PP ↓` and `(PP ↓ • B • PP ↓) ↑` being the Klein four-group on the wires 0 1 2 together with (274) and `comm-gate₂-w↑↑`. And `flip-e` for the E-form — the last hypothesis `box-pass` needs — comes down to **CH ↑ passing the letter B**, i.e. a CH between the box wire and an idle wire passing the box, which by (274) is interchangeable with CH ↓ and is *not* reachable from what the box already passes: Ex, CZ and the one-wire gates generate only the real Clifford group on two wires, and CH is the non-Clifford generator. So there is no elementary way in below (284); the paper's own order — (283) first — stands.

    `EForm` therefore names the keystone and reduces everything below it to exactly two hypotheses: `E□ k = PP ↓ • B□ k • PP ↓` is the multi-controlled H with H on wire 0, box wire 1 and controls 3 … k + 3 (at k = 0 it is the CH from wire 3, so `Eq284 0` is (153)), and `Eq284 k` / `Eq284′ k` are (284) and its mirror. From those, `eq285` is `box-pass`: H ↓ exchanges W and V by (123), passes E by `through` — between P ⊗ P it is Z on a control of B, `Z↓-B□`, which is (270) under the transposition — and the flip V E W ≈ W E V is the two halves with one E right-cancelled by `bbc`. (274) and (275) then follow in `BoxWire`. So this whole part of Lemma D.8 now rests on the two halves of (284) and on nothing else unproved. Typecheck `GeneralN/HGate.agda`, `GeneralN/BoxWire.agda` and `GeneralN/EForm.agda` (a minute on top of `GeneralN/Box.agda`, which is under five minutes with five three-wire evaluations).
  - **Theorem 4.10 (Appendix A.3, A.4) is the one piece left that Appendix D does not block**, and it is a Reidemeister–Schreier computation, which `Normalization/Reidemeister-Schreier.agda` already implements. The route: instantiate `Star-Injective-Full.Reidemeister-Schreier-Full` with Γ = the Figure 8 relation, Δ = the Figure 7 relation `_G,_===_`, C = the four cosets of Definition A.2 — ε, (−1)[1], H[0,1], H[0,1](−1)[1] — and I = ε; `f` is the paper's σ, which is already in the repo as `Auxiliary/P.asG`, and `h` is Definition A.2's τ. The module's two hypotheses are *exactly* the paper's conditions: `h=⁻¹f-gen` is (a), one equation per P-letter, and `h-wd` is (b), one per Figure 7 rule per coset — thirteen rules times four cosets. Those obligations are A.3.2's raw output, and deriving them from Figure 8 is what A.4 does, so instantiating Γ with Figure 8 directly collapses A.3.2 and A.4 into one pass. The pay-off is `(f ʷ)` injective modulo the presentations, which with Theorem 4.4 (Figure 7 complete, already a hypothesis) is Theorem 4.10 and removes one of `Completeness`'s three hypotheses. `Auxiliary/Cosets`, `CosetAction` and `RS` supply the data, and `Auxiliary/DE` the obligations. Item (a) is proved in its main case for all four letter shapes — `DE-ZZ`, `DE-ZX-all` (nine cases), `DE-XX` with `DE-XX-o₁`/`DE-XX-o₂` (three), `DE-HH` (the case where the pair 0 1 is free) — and item (b) has begun (`WD-a1`, `WD-a2`, `WD-c1`).

    Getting there needed the mixed-letter fragment of Figure 8 at *arbitrary* index pairs, where the figure states it only for consecutive ones. Each is the same Gray-code induction on the gap between the indices, (25)–(28) shrinking a pair by one index at a time according to the parity of the gap: `flip` (the exchange of the two X indices, the general (33)), `conj-zz` (a letter conjugates a sign pair by transposing its X indices, the general (31)) and `zx²` (the square, the general (23)), from which `zx-inv` (the general (29)) is five lines. The recursion is on a fuel bound for the gap, since the indices are `Fin`s at a width with no literals; the even step needs the consecutive letter invertible on *both* sides, which (29) gives on one side and (23) supplies on the other (`zz-past-L`), while the odd step decomposes over the pair at the top, already in the shape (31) is stated for, and needs no such thing. **That general theory turns out to be the paper's own Figure 9** (Lemma A.3, derived in A.4 before item (a) is attempted): (47) is `LR`, (48) `L-cube`, (51) `zx-inv`, (52) `zx²′`, (56) `zx-own`, (58)–(62) instances of `conj-any`, (63) `zz-comm`, and (49), (50), (54), (55) are `flip-step`/`flip-step′` packaged as equations rather than as steps of an induction. Only (64), that two mixed letters on disjoint index pairs commute, has no counterpart yet.

    Both items were mapped numerically before any of it was written: `scratchpad/tA2.py` checks the coset action itself (`rep c · y = asG w · rep c′` for every coset and generator at eight indices, 480/480) and `scratchpad/tB.py` runs every Figure 7 rule through the action at every coset and every admissible index, checking both sides semantically and reporting the size of each obligation — no failures, and 48 small obligations (two to four letters of P a side) against two large ones, (d3*) at eight to twelve and (d4*) at twelve to eighteen. **One obligation has no visible route yet**: (a3) at the coset ε asks for `hh a b 0 1 • hh 0 1 a b ≈ ε`, which by (42) at `c d := a b` is the single letter `hh a b a b`, denoting H₍ₐ,ᵇ₎ H₍ₐ,ᵇ₎ = I; but (35) wants four distinct indices, (36) and (37) want the two `hpat` positions to differ — and `hpat` is `nothing` here, since `diff (code a) (code a)` is empty — and (42) relates the letter to its bridged form rather than to ε. Figure 9 has no Hadamard equations at all, so the answer is in the later part of A.4.2 — and **that part is not more equations but a normal form**: Lemma A.4 gives a unique normal form for words over P with no Hadamard letter, Corollary A.5 reads off that two such words with the same semantics are equal, and Corollaries A.7, A.9, A.10 extend that to words with at most one Hadamard letter; A.4 then derives the Reidemeister–Schreier output by appealing to those rather than writing derivations out. Figure 9's own (64) is not proved that way — its proof is a page: the base is (32), the steps are (54) and (51), and the rest is a six-way analysis of how the two intervals interleave, of which the disjoint case is `disj-int`. `scratchpad/tC.py` measures what the Hadamard-free fragment would buy: of item (b)'s obligations only (a1*) and (b1*) are Hadamard-free at every coset and index, (a2) and (c1) are except where an index meets 0 or 1, and every rule of Figure 7 mentioning H produces Hadamard letters at every coset — so `WD-a1`, `WD-a2`, `WD-c1`, `WD-b1`, with `WD-a2-K` and `WD-c1-K` at the other two cosets, are about what that fragment reaches, and the rest of item (b) waits on Lemma A.4.

    `Auxiliary/NF` proves **Lemma A.4's existence half in full**: every word over P with no Hadamard letter is equivalent to one of the form (B). Its normal form is a sign part and a permutation part: the sign part an even-sized subset of the indices, kept as the sorted list of them, and the permutation part a **Lehmer code**, the vector (d₁,…,d_{N−1}) with dᵢ ≤ N − i ranging over ∏(N−i+1) = N! values, each giving a product of letters on *consecutive* pairs. That this is a normal form was checked by enumeration first (`scratchpad/tNF.py`): at N = 2, 3, 4, 5 the words number exactly 2^(N−1)·N! and no two have the same matrix. The permutation part is indexed by natural numbers through `zxℕ`, since its indices are literals and `Fin N` has none at a symbolic width; the sign part is indexed by `Fin N` through `zz`, which is total and so needs no `Maybe` case analysis — the first draft had both over ℕ and every commutation lemma would have taken sixteen cases on `toFin`.

    Existence is the two halves the paper gives it. *Every Hadamard-free generator is a product of letters on consecutive pairs* (`AsSxs`, a record carrying the list of indices, its bound and the equation; `AsSxs-ε`/`AsSxs-•`/`AsSxs-≈` make it compositional): a sign pair is a mixed letter squared, (52) = `zx²′`; a letter whose sign is on neither index of its pair sheds it by (30); (33) and `flip′` move the sign to whichever index one likes; and a letter on a pair at distance more than one shrinks by (25) or (27) according to the parity of the gap — the same induction as `flip-fuel`, whose scaffolding (`next`, `prev`, `∸suc`, `pred∸`, …) `DE` now exports rather than keeping private. What is left on a consecutive pair is `sx a` or its cube, the cube because `sx a` squared is the sign pair, (23). A letter of P carries its distinctness side condition *irrelevantly* and the rules of Figure 8 need it relevantly, so `AsSxs-gen` decides it again, the irrelevant proof ruling out the branch where the indices coincide (`Data.Empty.Irrelevant.⊥-elim`). *And any product of such letters is of the form (B)* (`reduce`), by pushing one letter at a time into a normal form from the right. Where a letter lands is decided by the run it meets: it commutes past a run starting two or more above it (`sx-below`), extends one starting just above it, squares into a sign against one starting at it (`sx-absorb`, (23)), and passes through one starting below it, coming out one lower (`sx-inside`, one braid (24) and commutations). The runs of (B) have tops N−i−1, which decrease, so a letter entering the i-th run always has index at most that top and must land by the last run — that is the hypothesis `suc j ≤ k` of `absorb-fuel`, k being the number of runs left, and at k = 0 there is no such letter. The sign a letter leaves behind travels back out to the left: past the later runs by `run-past`, since a mixed letter transposes the two indices of a sign pair ((58)–(62), `conj-any`) — *which* pair does not matter, the side conditions being kept by toggling any two indices — and past the sign part by `sx-past`, which recurses on `signOf` two indices at a time rather than on `atoms` one at a time, because an atom is a pair through the index 1, which the letter may move, whereas a pair always comes out a pair and with it the parity. Two traps: the side condition on the sign part must be `Incr₀`, with *no* bound on the first element, since (B) allows a sign on the index 0 — `Incr 0` silently loses half the normal forms; and the case splits are `with`s, through which the termination checker does not see a recursive call, so `absorb-fuel` recurses on fuel and `sx-through` on the *gap* between the letter and the run's first index.

    **The model is built** (`Auxiliary/SignedPerm`): a signed permutation is a record of a sign function and a permutation of the 2ⁿ indices, composed with the left factor acting first, and Equations (20)–(34) all hold in it (`sound-ax`, one clause per rule, no catch-all), hence the whole congruence of the fragment does (`sound`). The whole of Figure 8 could not be modelled this way — (41) says a Hadamard pair squared is the identity — which is why the fragment is a relation of its own: `Auxiliary/Figure8Free` restates (20)–(34) with the *same constructor names*, so `DE` and `NF` moved to it by changing one line each (`open Tools (m PF,_===_)`) and not one derivation, and `free⇒full` / `free⇒full-≈` carry everything back into Figure 8. Only `DE-HH` uses a Hadamard rule; it moved out to `Auxiliary/DEH`. Two lemmas do the soundness work: `prod-L`, the product of two letters, and `conj-L`, a letter conjugated by a letter with an arbitrary sign on the third factor — which covers the braid (24) *and* all four of (25)–(28), whose parity and ordering side conditions turn out to play no part, only the distinctness of their indices. Every factor of `hop`/`fuse`/`drop` is explicit: `_⊙_` computes to a record literal, so a meta `f ⊙ g` is never a unification pattern and nothing would be solved. `NF.cor-A5` is then **Corollary A.5** — two Hadamard-free words with the same signed permutation are equivalent — modulo one named statement, `NF.Unique`: that a word of the form (B) is determined by the signed permutation it denotes.

    `Auxiliary/Unique` **proves that**, so **Lemma A.4 is complete and Corollary A.5 is unconditional** (`Unique.A5` in the fragment, `Unique.A5-full` in Figure 8). The two parts of (B) are read back separately. A sign part permutes nothing (`signOf-prm`), so the permutation of a normal form is that of its Lehmer code alone; the i-th run sends its first index to N − i (`runAt-send`) and every later run leaves N − i alone, since the runs have decreasing tops (`permFrom-hi`), so `code-at` says the i-th entry of the code *is* the index the permutation sends to N − i — and `prm-inj`, the permutation of any word being injective, turns that into `d i ≡ d′ i`. The first run is then cancelled from the equality of permutations by `runBack`, the same letters in the other order, which inverts it on both sides because each letter is an involution; the rest is the induction. With the codes equal the signs agree index by index, and the sign of a sign part at an index is, mod two, how often that index occurs — which for a strictly increasing list is membership, so `incr-unique` recovers the list. The whole of `Unique` is 330 lines and takes 10 s.

    `Auxiliary/SPMatrix` connects that development to the repo's own semantics: the matrix of a Hadamard-free word **is** the matrix of its signed permutation (`word-op`, with `word-scale` saying it carries no power of 1/√2), and the reading is faithful (`spOp-inj`, a signed permutation matrix having exactly one non-zero entry in each row and that entry being ±1). `SignedPerm`'s composition has the left factor acting first and `(M ⊙ N) x y` sums `M x z * N z y`, so the two agree on the nose and `Σ-δˡ` collapses the sum. Hence **`SPMatrix.A5-mat`: Corollary A.5 exactly as the paper states it** — two Hadamard-free words with the same matrix semantics are equivalent.

    `Auxiliary/Theorem410` is the Reidemeister–Schreier instantiation: Γ the theory of P, Δ the theory of the auxiliary generators, C the four cosets with ⟨ε⟩ distinguished, f = `P.asG`, h = `RS.act`. It names the engine's two hypotheses as `Item-a` and `Item-b` — which *are* the paper's conditions — and `Complete` exports `reidemeister-schreier`, so **given those two, nothing else about the method remains**. `Auxiliary/ItemA` then proves item (a) for the three Hadamard-free letter shapes: each is exactly one of `DE`'s theorems (`DE-ZZ`, `DE-ZX-all`, `DE-XX` with its two variants), the coset action having been transcribed so that they would be. What is left of item (a) there is the Hadamard shape, `Item-a-HH`, and **`Auxiliary/ItemAHH` discharges it** (below), so item (a) is complete given (65). Two traps, both met here: the action and the traversal are defined by `with`, and a `with` in a proof does **not** reduce a *stuck call* to them in the goal — so each letter's action is computed once in its own lemma, `run₂` puts two of them together, and the case splits are `Dec`-taking helpers rather than `with`s; and the width must be pinned on every letter (`−1−1 {₃₊ m} a b`, `asG {₃₊ m}`), since `2 ^ ?n = 2 ^ (3 + m)` is not solvable by unification.

    **What gates the rest of Front B is Lemma A.6**, and it is genuinely semantic: for a Hadamard-free w commuting with `H[0,1]H[3,2]`, w is ≈ `((−1)[0](−1)[1])^α ((−1)[3](−1)[2])^β (X[0,3]X[1,2])^γ Q` with Q on the indices ≥ 4. Corollary A.7 is A.6 plus A.5, (65) — Figure 10's form of (35) without the `hpat` restriction — is A.7, and *every* equation of Figure 10 goes through (65); Corollary A.9 (at most one Hadamard letter) is Figure 10 plus A.4's existence, and A.10, A.11 and the Hadamard obligations of item (b) are A.9. In particular (70), `H[a,b]H[a,b] ≈ ε` — the (a3) obligation at the coset ε — is (42), (66), (65), A.5, (41), A.5. `SPMatrix` is the first half of what A.6 needs.

    The second half is begun. `Auxiliary/HadamardPair` computes the matrix of `H_[p,q] H_[r,s]` for **any** two disjoint index pairs — the general form (65) wants, not only the paper's 0, 1, 3, 2 — by writing a row of a two-level Hadamard as a combination of two deltas (`had-i`, `had-j`, `had-o`) and collapsing the product with `Σ-add` and `Σ-δˡ` (`row-two`, `row-one`, and `HH2-p`/`-q`/`-r`/`-s`/`-o`). The pair is √2 times a two-level Hadamard on each of its pairs and twice the identity elsewhere, so its **diagonal** is +√2 on the first index of each pair, −√2 on the second and 2 everywhere else; those are three different elements of ℤ[√2] (`√2≢2`, `-√2≢2`, `√2≢-√2`, all by `()`), and `diag-is-√2` / `diag-is--√2` / `diag-is-2` read an index's position off it. `Auxiliary/Commutant` then proves **Lemma A.6's structural core**: for a signed permutation commuting with the pair, `image-p`, `image-q`, `image-r`, `image-s` place each of the four indices, `image-o` says an index away from both pairs stays away, and `images-pr`/`images-qs` reduce it by injectivity to *either fixed pointwise or exchanged*. The argument is one comparison: a signed permutation has one entry per row, so one product is just the pair's row at the image, while the other leaves a single surviving delta; comparing at the image gives the pair's diagonal entry there, and ±1 cancels (`sign-cancel`). The off-diagonal entries then pin the rest: the pair's entry at (p , s) — one index from each of its own pairs — is zero while the comparison there is √2 times a sign, which is not, so `together` and `together′` say the two pairs move as one, and `sign-pq`/`sign-rs` say the two signs of each pair agree. **That is A.6's characterisation, complete at the level of signed permutations.**

    Two further pieces go with it. `Auxiliary/Passes` collects the words Figure 8 can carry across the pair — a letter on a consecutive pair at index 4 or above by (38), the sign pair on 0 and 1 by (39), the double exchange by (44) — closed under products and under equivalence; those are exactly the generators of the form A.6 gives, which is why it is stated over them. And `Unique.low-runs` supplies what turns a *permutation* fact into a *word* fact: **a word whose permutation fixes every index below a bound has a normal form all of whose runs start at or above it** (every run starting below is empty). That localises, which is not obvious — the i-th run starts at the index the permutation sends to N − i, so if the permutation fixes that index the run is empty; and a run that is empty, or starts above the bound, fixes everything below it, so the hypothesis passes to the runs after it (`low-start`, `runAt-fix`, `low-tail`).

    **The assembly is done for the branch that matters**, as `Passes.passes-low`: a Hadamard-free word that neither moves nor signs any index below 4 can be carried across the pair. Its two halves are `Unique.NFHigh` — from "the normal form neither moves nor signs a low index", `nf-low-runs` (every run starting low is empty, by `low-runs`) and `nf-high` (every index of the sign list is high: the permutation part carries no low sign, `permFrom-sgn`, so the sign at a low index is membership in the list, and an increasing list whose head occurs cannot have a low head) — and `passes-signOf` / `passes-permFrom`, which pass those two parts letter by letter, a sign pair on two high indices going through (22) to consecutive pairs and then (23) and (38).

    **Lemma A.6 and Corollary A.7 are proved**, unconditionally. The other branches of A.6 come from dividing out the generator that makes them: `Passes.passes-factor` says a factor that passes and is its own inverse may be divided out, `passes-fixed` divides out the sign pairs (`zz 0 1` and `zz 3 2`, which toggle exactly their own two signs and move nothing) and then calls `passes-low`, and `passes-swapped` divides out the double exchange, which renames the four indices back. `Auxiliary/LowGens` supplies the three generators and the arithmetic they need: the four indices as `fin8 ₀`…`fin8 ₃`, the deciding wrappers (`zz01≡`, `zz32≡`, `xx0312≡`, `hh0132≡`), the three involutions by Corollary A.5, and the one fact Figure 8 does not state — **the sign pair on 3 and 2 passes**, being the sign pair on 0 and 1 conjugated by the double exchange, which is A.5 again after pushing the two signs left through the two exchanges (each renamed by the exchange it crosses) and cancelling what is left, two involutions on disjoint pairs. `Auxiliary/A6` is the bridge: it reads `sp w` on bitstrings (`sgw`, `pmw`), checks that `Commutant`'s `P` and `SPMatrix`'s `spOp` are the same matrix (`sgnOf-if`), instantiates `Commutant.Pair` at the codes of the four indices, and turns `places`/`signs` back into statements about `prm`/`sgn` by injectivity of the code — so `A6` takes the matrix commutation and `A6-word` the localised equation `⟦ w Λ ⟧ ~ ⟦ Λ w ⟧`. `Commutant` gained `sign-pq′`/`sign-rs′` (the exchanged branch: the entry read off is the pair's at (r , s), which is the same as at (p , q)) and the packaged `places`/`signs`.

    `Auxiliary/A7` is Corollary A.7: for A, A′, B, B′ Hadamard-free with `⟦AA′⟧ = ⟦BB′⟧ = I` and `⟦A hh A′⟧ = ⟦B hh B′⟧`, `A hh A′ ≈ B hh B′`. The proof is the paper's: B′A commutes with the pair semantically — `comm-lemma`, which is the calculation `(b′a)M = b′(aM(a′a)) = b′((aMa′)a) = b′((bMb′)a) = (b′b)M(b′a) = M(b′a)` — so by A.6 it passes, and then `A hh A′ ≈ (BB′) A hh A′ ≈ B ((B′A) hh) A′ ≈ B hh (B′ (A A′)) ≈ B hh B′` is associativity and the two cancellations. Where the paper spells A.6's normal form out and moves it across letter by letter with (38), `Passes` has done that once and for all. Two things this needed: `SignedPerm.sp-inj` (the `prm` of any `sp w` is injective — `SP` stores a bare function, so this is proved letter by letter) and `SignedPerm.⊙-inverse`, that a one-sided inverse is two-sided, which needs no counting: `g` is injective, so `g (f (g x)) = g x` cancels to `f (g x) = x`, and the signs then follow at the transported index. Two traps: `comm-lemma` must be stated over **abstract** operators, since with the matrices in place the conversion checker expands every `⊙` into its sum over bitstrings and a five-fold product exhausts memory; and `by-assoc` is no use on word *variables*, its flattening not computing through them, so the re-bracketing is `assoc` by hand.

    What is left is (65) and the rest of Figure 10. (65) is A.7 applied to `hh a b c d` written as `A hh A′` — which is what (35), (36) and (37) give, in cases on `hpat` — against `Σ hh Σ′`, given that (65) is *sound* for all a, b, c, d. Its semantic half is `Auxiliary/Conjugate`, which is done: **conjugating any matrix by a signed permutation and its inverse pulls it back along the permutation and conjugates it by the signs**, `(F M G) x y = σ x · M (π x) (π y) · σ y` (`Conj.conj`), there being one non-zero entry per row on each side — the left delta collapses by `Σ-δˡ`, and the right one does too once `g`'s delta at w is recognised as the delta at `π y` (`right-δ`, which needs `prm f ∘ prm g = id`, hence `⊙-inverse`). `had-pull` says a two-level Hadamard pulls back to the two-level Hadamard on the pre-images (an injective π renames its `eqB` tests and preserves `δb`), `conj-mult` says conjugation is multiplicative (over **abstract** operators, for the memory reason above), and `had-sign` says a two-level Hadamard does not see the signs at all, as long as **the two signs along its own pair agree**: its non-zero entries are on that pair and on the diagonal, and at every one of them the two signs multiply to 1. That is the right hypothesis, since Definition E.1's word is four `(−1)[m] X[x,y]` letters and each carries a sign of its own, so `sp Σ` is *not* sign-free. `Pull.conj-pair` puts them together: a permutation whose signs agree along each of the two pairs carries the standard pair on `π a, π b, π c, π d` back to the pair on `a, b, c, d`. **Equation (65) is now sound at every width**, which is the half the paper's proof assumes. `Auxiliary/Sigma` analyses Definition E.1's four transpositions over ℕ: with `π₃ = (d 2)`, `π₂ = (c , ρ₃ 3)`, `π₁ = (b , ρ₂ 1)`, `π₀ = (a , ρ₁ 0)` and `ρₖ = πₖ ∘ ρₖ₊₁`, each step sends its numeral where it should (`τ-i`) and leaves the earlier images alone because ρ is injective and the numerals differ — so `ρ₀` sends 0, 1, 3, 2 to a, b, c, d and the word's own permutation, `π₃π₂π₁π₀`, is `ρ₀` backwards. The four signs sit at `ρₖ 4`, which is none of those images for the same reason. `Auxiliary/SigmaPerm` reads that off the word (`prm-Σ-a` … `sgn-Σ-d`), crossing two things: the letters are `zxℕ`, whose three indices are decided by `toFin` (one `zxℕ-zx` each, `lit`), and a letter whose two X-indices coincide is the **empty word**, not a letter — harmless, since an empty letter neither permutes (nor does the transposition it stands for) nor signs. It also proves **Σ′ is Σ's inverse**, and cheaply: Σ′ has the same four transpositions in the other order, each carrying the *next* sign up (`mₖ = πₖ mₖ₊₁`, `m₃ = π₃ 4`), so the letters pair off **by transposition** — the same degeneracy condition on both sides — `(−1)[u] X[x,y] (−1)[u′] X[x,y] = ε` whenever `u = (x y) u′` (`zx-pair`), and the product telescopes (`tele4`). `Auxiliary/Eq65` then puts `SigmaPerm` and `Conjugate` together: `Σ-conj` says the conjugate of the standard pair is the pair on a, b, c, d, `sound-65` that both readings of (65) have the same matrix at the same scale, and `eq65` reduces (65) itself, through A.7, to *any* writing of the letter as `A (H[0,1]H[3,2]) A′` with A, A′ inverse Hadamard-free words — a `Writing` record. `eq65-35` discharges that where the codes have no H-pattern: there Equation (35) **is** (65). What is left is the H-pattern branch, where (36) and (37) write the letter with a swap network (`W₁`/`W₂`), and its two obligations are semantic: that the network's two halves are inverse and that the writing is sound. Then (66)–(77) are short (the page of Corollary A.8 shows each in three or four steps over (65), A.7 and A.5), **(70) is `H[a,b]H[a,b] ≈ ε`, the (a3) obligation at the coset ε**, and A.9, A.10, A.11 and item (b)'s Hadamard obligations follow. Traps met here: an implicit that occurs only under a `τ`/`if` chain is never solved, so `ρₖ-inj` and the `zxℕ`-conversions take their numerals explicitly; and every implicit of `~-trans`/`~-sym` must be given, `√2^ ?ℓ` not being a unification pattern.

    **Figure 10 is begun, over one hypothesis.** `Auxiliary/Figure10` takes `Eq65` — (65) at every tuple of four distinct indices — and derives **(66)**, that the two pairs commute, and **(70)**, that a pair squared is ε. (66) is (65) at both tuples with A.7 between them, the two sides denoting the same operator by `Auxiliary/PairComm`: each factor of a pair is √2 times the identity away from its own two indices, so on each of the five kinds of row the two products have the same entries. (70) is the paper's own chain — (42) splits the letter over two indices fresh to a and b, (66) turns the second factor round, (65) writes both as Definition E.1's words around the standard pair, and then the inner Σ′Σ cancels, the two standard pairs cancel by (41), and the outer ΣΣ′ cancels. **(70) is `H[a,b]H[a,b] ≈ ε`, the (a3) obligation of item (b) at the coset ε** — the one that for a long time had no visible route. Both cancellations are `Eq65.Tuple.ΣΣ′-≈` and `Σ′Σ-≈`, Corollary A.5 applied to Definition E.1's two words, the second order coming from `⊙-inverse`. (42) needs the two fresh indices to be indices: `Encoding.free` is private, so `Figure10` repeats the search (the two agree by reduction, the fuel being a numeral) and proves that with only *two* forbidden values three tries always suffice, so they are below 6. **The performance trap here is real**: composing two `_~_`s makes the conversion checker expand each side into a sum over bitstrings *per letter*, and a nine-letter word took 700 s and did not finish; composing the scale and the matrix separately (`conj-scale`, `conj-op`) takes 15 s. **And this pins what is left of item (a)**: `DE-HH` gives `H[a,b]H[c,d] ≈ (H[a,b]H[0,1])(H[0,1]H[c,d])` only when the four indices are *not* all distinct — that is (42)'s own side condition — and 0, 1 are free; the all-distinct case of that same equation is exactly **Figure 10's (71)**, whose proof needs the paper's `T_{a,b,c,d,e,f}`, a six-index analogue of Definition E.1's Σ (footnote 13: "constructed in a similar way"). So `Item-a-HH` and (71) are one piece of work, and `Sigma`/`SigmaPerm` are the template for it. **(74) is there too**, and with it the general fact behind it, `pair-comm`: *a Hadamard-free word that neither moves nor signs any of a pair's four indices commutes with the pair.* By (65) the pair is Σ Λ Σ′, so conjugating the word by Σ gives one that neither moves nor signs an index below 4 — exactly what Lemma A.6's `passes-low` asks — and the two conjugations are Corollary A.5, the signed permutations being equal by construction (`Σ′ (u Σ)` on one side, `(u Σ) Σ′` on the other). `comm-ε`, `comm-•` and `comm-zz` close that under products and add the sign pairs, so it is a kit rather than one equation. `comm-xx` completes the kit's three letter shapes. The next equation of that family, (75), does **not** follow: there the mixed letter's indices *are* the pair's first two, the conjugate `Σ′ u Σ` acts on the low indices as `(−1)[0] X[0,1]` — that is `sx 0` — and what would be needed is `Λ (sx 0) Λ ≈ (sx 0)⁻¹`, which none of (38), (39), (40), (44) gives: (38) starts at index 4.

    **The six-index word `T` is built** (`Auxiliary/Telescope`, `TeleWord`, `SixWord`), which is the construction footnote 13 waves at and the thing (71) needs. It is done *generically*, over a **plan** — a list of pairs (target index , numeral) — since Σ and T are the same construction at four and at six: `ρ [] v = v` and `ρ ((x,k) ∷ p) v = (x , ρ p k)` applied to `ρ p v`, each step transposing its target with wherever the rest of the plan sends its numeral, and `P` the same transpositions in the other order. `P p ∘ ρ p = id` needs nothing but that a transposition is an involution; that ρ really sends each numeral to *its own* target (`ρ-img`) is the half needing `Ok p`, distinct targets and distinct numerals, and it is one three-line induction where the concrete four-index version took twenty lemmas. `TeleWord` builds the word — one `(−1)[ρ p s] X[x , ρ p′ k]` letter per step, the sign being the image of a **spare** numeral the plan does not use — and proves its permutation is `P`, that it signs no index the plan reaches from another numeral (the k-th sign is at `ρ (drop k) s` and the k-th letter is reached at `ρ (drop k) j`, so ρ-injectivity settles it), and that the companion word with the signs shifted one step up is an inverse, by the same `zx-pair` telescope as Σ′. `SixWord` instantiates it at `[(a,0),(b,1),(c,3),(d,2),(e,4),(f,5)]` with 6 spare, and `T-conj` delivers what (71)'s first appeal to A.7 consumes: `T (H[0,1]H[3,2]) T′` denotes `H[a,b] H[c,d]`. Definition E.1's Σ is the same word at `[(a,0),(b,1),(c,3),(d,2)]` with 4 spare, differing only by a trailing ε. **A trap:** `τ` unfolds to an `if`, so `τ y z _` is not a neutral application — `Eq.cong (τ y z)` fails to match and the step has to be an `Eq.subst`.

    With T in hand, (71) is the chain I read off page 40 and completed by hand: (65) on both letters, then A.7 twice to rewrite them as `T Λ T′` and `T V Λ V′ T′` with `V = Σ_{3,2,4,5}` (a *concrete* instance of `SigmaPerm`), then `T′T ≈ ε`, then (65) backwards to `T Λ (H[3,2]H[4,5]) T′`, then **(43)** — `Λ · H[3,2]H[4,5] ≈ X[0,3]X[1,2] · H[3,2]H[4,5] · X[0,3]X[1,2]`, a rule of Figure 8 — and (65) once more, leaving `W Λ W⁻¹` with `W = T · X[0,3]X[1,2] · V`, whose permutation sends a, b, e, f to 0, 1, 3, 2 (check: a ↦ 0 ↦ 3 ↦ 0, b ↦ 1 ↦ 2 ↦ 1, e ↦ 4 ↦ 4 ↦ 3, f ↦ 5 ↦ 5 ↦ 2), so one last A.7 against `Σ_{a,b,e,f} Λ Σ′_{a,b,e,f}` finishes it. **`Auxiliary/Eq71` carries that out in full.** `T-conj` and two more calls to **`A6.conj-at`** — the reusable form of the computation, that any signed permutation sending four indices to 0, 1, 3, 2 without signing them carries the standard pair onto those four — give all three conjugation facts, for `T`, for `T V`, and for `W = T (X[0,3]X[1,2]) V`, each with its permutation traced index by index and its inverse by a `peel` telescope; `chain` carries (71)'s whole syntactic content over **abstract** words; and `step₁`, `step₂`, `step₃` join them by A.7, with `eq71 = trans (cong step₁ step₂) (trans (chain …) step₃)`. **The one thing that matters about typing that in is that every argument of `A7` must be a named definition with its own type.** Inlined, the `same` and `invʷ` applications leave *their* implicit words as metavariables to be solved against A.7's, and unifying two unsolved readings of a nine-letter word is what makes the conversion checker expand them — 600 s was not enough, while the same three lines written out as definitions take a second. Two earlier diagnoses were wrong and cost a session between them: it is not the transparency of `Encoding.Σ` (an abstract alias for it, `Auxiliary/Opaque`, made no difference and has been deleted), and it is not Agda's module application (`Eq65.Tuple` is instantiated four times in `Eq71` and that is free). `TeleWord.Wrd`'s opacity is worth keeping for the plan's own word — without it 200 s and 2 GB were not enough — but it was not the blocker either.

    **(71) holds for any six indices, and with it item (a) is complete.** Figure 10 states (71) with no side condition at all; `Eq71` is the case where the six are distinct, and **`Auxiliary/Fuse`** covers the rest — which is what `Item-a-HH` needs, its middle pair being the fixed 0, 1. The route is not the paper's, which conjugates each degenerate configuration by a signed permutation built for the purpose. Three facts suffice: **`hh-invol`**, a letter squares to ε — (65) at its tuple, then the inner Σ′Σ, (41) and the outer ΣΣ′ cancel from the inside out, the chain `Figure10` runs for (70) but at an arbitrary tuple; **`hh-cancel`**, so a letter and its reverse cancel, by (66); and **`hub`**, so the middle pair of a product of two letters may be replaced by any pair fresh to the four outer indices — insert the cancelling pair, re-bracket, and the six-index case applies to each half. `fuse-fresh` is then (71) whenever the middle pair misses the outer four: where those four are distinct it is `Eq71`, and where they are not, **(42)** splits the right-hand side over the two smallest naturals they miss and `hub` carries the middle pair onto those, in one hop where it misses them and in two through a fresh pair otherwise. `fuse` is (71) outright, and needs no case analysis: a pair fresh to all six splits both letters, the two inner halves cancel by (66) and `hh-invol`, and what is left is `fuse-fresh`. **(73)** is then five lines. `Auxiliary/ItemAHH` discharges `Item-a-HH` — the action sends H[a,b] from ⟨ε⟩ to H[a,b]H[0,1] and H[c,d] from ⟨K⟩ to H[0,1]H[c,d], so the obligation *is* (71) at (a, b, 0, 1, c, d) — and exports **`item-a : Eq65 → Item-a`**.

    **The freshness all of that needs is a pigeonhole, and a search with fuel does not prove it.** `Auxiliary/Avoid` does it by deletion instead: start from the list 0 … 7, delete each blocked value in turn (`flt`), and read the first survivors off. Deleting one value shortens a **strictly increasing** list by at most one (`flt-len` — that is the whole of the counting, and the increase is what rules out a repeat), and a survivor is by construction in the original list and different from everything deleted. `avoid` gives two survivors from six blocked values, `avoid4` two from four with the first at most 4 and the second at most 5. `Auxiliary/Small` uses `avoid4` for what every use of (42) needs and nothing so far had: that `Encoding.twoSmallest a b c d` really does miss a, b, c, d, and is below 8, hence an index at every width. Its search is repeated locally (`free` is private to `Encoding`; the two agree by reduction), `fr-low` says everything the search passed was blocked, `fr-stop` that where it stopped early it stopped on an unblocked value, and `fr-ok` puts them together — an unblocked value in the window bounds the answer, which is itself unblocked. Where a degenerate configuration repeats an index, three of the four cover what all four block (`Cov`, `cov`), which is what keeps the blocked list down to the six entries `avoid` takes. Throughout, each decision is taken by a helper *taking the `Dec`* rather than by a `with`, so that a proof can case on it and see both branches reduce; `with` on a variable bound in a parent clause is rejected outright.

    **Carrying a Hadamard-free word *across* a pair is one lemma too** (`Auxiliary/Move`). Figure 10's (68), (69), (76) and (77) all move a mixed letter past `H_[a,b] H_[c,d]`, renaming the pair's indices by the letter's own exchange, and the paper proves each the same way: A.5 appends the letter's inverse, (65) writes the pair around the standard one, A.7 absorbs the letter into Definition E.1's word, and (65) reads the result back. None of that uses the word being a *letter*, so `hh-conj` states it once — **a Hadamard-free word with an inverse carries a pair onto the pair on the pre-images of its four indices, provided its signs agree along each of those two pre-image pairs** — and `hh-pass` turns that into `u · H_[p′,q′]H_[r′,s′] ≈ H_[p,q]H_[r,s] · u`. `pull-pair` is the matrix half (`A6.conj-at` at an arbitrary target instead of the standard pair) and `reassoc` the operator re-bracketing, over **abstract** operators for the reason `A7.comm-lemma` is. **(76)** — and with it **(68)**, which is (76) at e = a + 1 — is then one instance: the letter `(−1)_[a] X_[a,e]` exchanges a and e, signs only a, and a is not among the pre-images, so the signs agree trivially. `xx-pass` is the other instance, at a **double exchange** on two disjoint pairs: it carries no sign and is its own inverse (two exchanges on disjoint pairs commute, so the product squares away — `SWP-conj` with `swapF-o`), so only the four images are left to give. That is the shape item (b) will meet, the coset action producing an `X_[z,w] X_[x,y]` beside a Hadamard letter at three of its four cosets. `zz-pass` is the third letter shape: a **sign pair on the Hadamard pair's own two indices** commutes with it — the general form of Equation (39), and the only interesting case, since away from all four indices it is already `Figure10.comm-zz` and one index in with one out is where the signs genuinely disagree. So all three Hadamard-free letter shapes now pass a pair. `Auxiliary/CorollaryA8` indexes what Figure 10 now has under its equation numbers — (66), (70), (71), (73), (74), (76), each taking (65) as its first argument — together with the kit (`hh-invol`, `hh-cancel`, `hub`, `fuse-fresh`, `hh-conj`, `hh-pass`, `xx-pass`), and says which are still open. (67) is the one the rest wait on. It says `H_[b,a] H_[c,d] ≈ (H_[a,b] H_[c,d]) ((−1)_[b] X_[a,b])`, and Corollary A.7 does **not** reach it — the two halves of the right-hand side are then not inverse, their product being the mixed letter itself. Nor does `hh-conj`: conjugating by that letter has its two signs *disagreeing* along the pre-image pair, which is exactly the hypothesis `Conjugate.had-sign` needs. It is also equivalent to **(72)**, `H_[a,b] H_[b,a] ≈ (−1)_[b] X_[a,b]`, given (71) and `hh-invol` — so both wait on the paper's own route, Equation **(40)**, which carries a sign pair across the standard pair at the cost of a mixed letter, together with the bookkeeping of where Definition E.1's word sends the numerals 3 and 4. And `Auxiliary/ItemAHH.Complete` now states where Front B stands in one line: **Theorem 4.10 follows from (65) and item (b), and from nothing else.**

    **(75), (67) and (72) are proved, all from Equation (40)** (`Auxiliary/Eq75`, `Auxiliary/Eq67`). Earlier notes said (75) did not follow, because it needs Λ to conjugate `(−1)[0] X[0,1]` to its inverse and no *passing* rule says so. That was true of (38), (39), (44) and wrong about the figure: **(40)**, which carries the sign pair on 3, 4 across Λ at the cost of `(−1)[2] X[2,3]`, is exactly the rule that says what Λ does to a sign on one index of a pair. Conjugated by the double exchange (44) it becomes `eq40′`, the same on the first pair; from there Λ conjugates `(−1)[1] X[1,0]` to `(−1)[0] X[0,1]` (A.5 twice, word algebra over abstract words), Λ-conjugation is an involution, and that is **(75) at the standard pair** (`eq75₀`); `L-by` — a letter crosses any signed permutation that signs its two exchanged indices alike, landing on their images — transports it by Definition E.1's word to every tuple. (67) is harder only in that its two letters have the same pairs in different orders, which conjugation by the pair's own exchange would relate if that exchange did not sign the two indices unequally. At the standard pair, `(−1)[4] X[2,3]` turns Λ's second pair round (`hh-conj`, the fresh sign on 4 making both pairs agree). That letter splits as the sign pair on 3, 4 times `(−1)[3] X[2,3]`; the second crosses Λ by (75) on the second pair and the first by (40), and what is left is `(−1)[2] X[2,3]` by A.5 — so `H_[0,1] H_[2,3] ≈ Λ ((−1)[2] X[2,3])` (`eq67₀`). Definition E.1's word for (c, d, a, b) then carries it anywhere, with (66) putting the pairs back in order. (72) is (67) with a fresh pair, (66), (71) and `hh-invol`. Every step of both routes was checked numerically first (`scratchpad/t75.py`). **(77), and with it (69), is proved too** (`Auxiliary/Eq77`), so **Figure 10 is complete over (65)**: every one of (66)–(77) is a theorem taking `Eq65` as its first argument, indexed in `Auxiliary/CorollaryA8`. (77) needs (40) at an *arbitrary* tuple, and `Eq77.Frame` supplies it: Definition E.1's word for (a, b, c, d) sends the numeral 4 to an index `e₀` fresh to the four (`e₀ = prm (sp T′) f₄`, with `e₀a` … `e₀d`), `std` is (40) carried there, `zz a e₀ • hh a b c d ≈ hh a b c d • (zz a e₀ • zx b b a)`, and (77) is then (76) twice with A.5 in between — the fresh index cancels. **Item (b) is proved for every rule of Figure 7 but (d3*) and (d4*)** (`Auxiliary/ItemB.Assemble`, which takes those two as hypotheses `D3`, `D4`), and not by two hundred case splits: **the coset action is conjugation of a uniform embedding by four fixed words** (`Auxiliary/Frame`). Pick three indices e, f, g away from 0, 1 and the rule (`ItemB.frame-for`, by `Avoid.avoid3`) and send −1[a] to (−1)_[a](−1)_[g], X[a,b] to (−1)_[g]X_[a,b], H[a,b] to H_[a,b]H_[e,f] (`Φ`): the tags commute with everything the rule touches, and `Frame.tr` proves `act c y ≈ T c · Φ y · T⁻ c′` for every coset and generator, with `T ⟨ε⟩ = ε`, `T ⟨M⟩ = (−1)_[1](−1)_[g]`, `T ⟨K⟩ = H_[0,1]H_[e,f]` and `T ⟨KM⟩` their product — about forty branches, one per clause of the action, all checked numerically first (`scratchpad/tF.py`, 480 generator cases). Then `Frame.run` (the traversal of a word is the conjugate of its embedding), `run-cos` (the coset reached is the Klein-group sum of the generators' tags, `_⊕_`, `tag`, `cos`) and `by-frame`: **item (b) for a rule is the rule's image under Φ**, uniform in the indices and the coset. Those images are a line each — a sign pair squared, sign pairs commuting, `hh-invol`, `comm-zz`, (71)+(66) for (b6*), and four decided by the frame normaliser below. Three tools make this work. `Auxiliary/Template`: a Hadamard-free word written over `Fin k` and read at `Fin N` through an injection ι has, at ι x, ι of the template's own signed permutation and elsewhere the identity, so `A5-tmpl w w′ refl` is Corollary A.5 decided by computing tables on literals (templates are trees `⌜ l ⌝ •ᵗ …` mirroring the goal's bracketing; `Distinct` builds ι from a vector). `Auxiliary/Twist.hh-twist`: `Move.hh-pass` without the sign-agreement hypothesis — when the signs along a pre-image pair disagree, conjugation leaves `(−1)_[p]X_[p,q]` behind (`κ`); proved by flipping the sign with a sign pair and paying with (40) at the tuple, `std-any`/`std-any₂` (`Eq77.Frame.std` with any fresh partner). `Auxiliary/FrameNorm.Norm`: with a frame pair e f fixed, H_[x,y]H_[e,f] behaves as a single Hadamard, and a word of such half-letters and Hadamard-free letters is normalised — Hadamard-free letters pushed right by `hh-twist` (pre-images through a template inverse `tinv`), half-letters turned round by (67), equal neighbours cancelled — to a list of ordered pairs and a tail; `by-norm w w′ refl refl` decides an equation, and nearly every branch of `tr` is one call. **Two traps, both costly.** At the concrete `RS.z₀`, `RS.o₁` every with-abstraction unfolds `Gray.fin8` and the proof it carries — thirty branch lemmas of the action took 2 GB and ran out of memory; stated over the action's parameters as variables (`FrameAct.Branches z o z≢o`, and `Frame` itself generic in z o) they take 700 MB for sixty. And `rewrite` with an equation `act c y ≡ (w , c′)` ran out of memory where `Eq.subst` is instant (`Frame.via-act`). Figure 7's starred rules sit at literal indices at `suc`-widths, which 2^(3+m) never matches, so `ItemB.item-b-gen` takes a rule at any width K with `K ≡ N` and transports its words (`trW`); the images never need the transported literals to be anything in particular, only apart (`lit≢`) or equal to 0 and 1 (`lit-z`, `lit-o`). Template-letter constructors `tz`/`ty`/`tx` shadow variables of those names in patterns — a variable named `tz` is read as the constructor. **(d3*) is proved too** (`Auxiliary/ItemBd3.d3`): with the frame e, f, g = 7, 6, 4 its image under Φ is Figure 8's (45) conjugated by the exchange of 2 and 3 (`zx 4 2 3`) — (d3*)'s H_[0,2]H_[1,3] are (45)'s H_[0,3]H_[1,2] renamed, the frame is (45)'s H_[7,6] — and each side is one `by-norm` against the corresponding side of (45); `core` states it over index variables with the numerals as hypotheses. **Item (b) is proved in full, and with it Theorem 4.10 from Theorem 4.4 alone.** (d4*)'s image is Figure 11's (80), and `Auxiliary/Eq80.eq80` proves (80) from Figure 8's (46) by the paper's own chain of twenty-seven lines (conjugation by `(−1)[6]X[2,3](−1)[2](−1)[3]`, A.10 moves decided by `by-norm` with a frame pair of whichever two or three indices the move leaves free, `Move.hh-pass` for the two that touch seven, (70), (73), (66)): the words are lists (`⟪_⟫`) and each line replaces one segment (`step`, chained by `Chain`/`run`), so only the replaced letters are ever written — the chain was transcribed from the rendered page and checked line by line first (`scratchpad/t80.py`; the text extraction of that page is garbled and gave one false step). On three qubits (d4*)'s six indices leave room for a frame pair (6, 7) but not a sink too, so the sink is the rule's own index 2: `Frame.by-frameAt` transports a word whose Hadamards are all read at ⟨ε⟩ or ⟨K⟩ with only the pair avoiding them (`AvAt`, `AvRun`, `runAt`) — true of (d4*) from those two cosets, its two signs being adjacent — and `Auxiliary/SignSym.obl-M` gets ⟨M⟩, ⟨KM⟩ from them: the action at c ⊕ ⟨M⟩ is the action at c conjugated by Z c, a sign on 1 and a fresh index s (or `(−1)_[s]X_[0,1]` at ⟨K⟩, ⟨KM⟩), because the transition words at c ⊕ ⟨M⟩ are those at c with Z on one side (`cornerL`/`cornerR`, one `by-norm` each; `scratchpad/tSym.py`). `Auxiliary/ItemBd4.d4` assembles it, `Auxiliary/Theorem410Proof` has `item-b` and **`theorem-4-10 : Theorem-4-4 → Section8.Theorem-4-10 m`** — σ preserves the semantics on the nose (`asG-sem`), so completeness of Figure 7 at the width of P, the paper's imported Theorem 4.4, is all it takes — and `Completeness.Theorem-8-9′` is Theorem 8.9 with Theorem 4.10 discharged: its hypotheses are Theorem 4.4 (at two qubits and at every width ≥ 3) and Lemmas 8.7 and 8.8. The hypothesis (65) is gone too, proved below.

    **(65) is proved at every tuple of four distinct indices** (`Auxiliary/Eq65H.eq65-full : Eq65`), so every `e65` argument of the development is now discharged. The H-pattern branch is Corollary A.7 applied to (36)/(37)'s writing of the letter, and what that needs is the signed permutation of the swap network at symbolic width. `Auxiliary/NetSP` computes it: each encoded gate is a product over the contexts of the other wires of letters acting inside one context, so at a basis vector it is the one letter of that context (`Local`, `ZXProd`, `ZZProd`, over `allBits`' enumeration with `Nodup`); read off that way `E-Z` and `E-CZ` negate the vectors with 1s on their wires and **`E-X` and `E-swap` carry no sign at all** — the flip and the wire exchange, as bit maps (`bm`, `sp-∏` for products; the swap's four letter tables compose to (u, t) ↦ (t, u) with the signs cancelling, checked by cases). `Auxiliary/BitAlg` is the bit algebra: the network carries wires i < j to 0 and 1 keeping the others in order (`network`, from `bubble` and `pairdown`), the negations clear the controls (`negs`), composites are undone by their reverses (`tele`), and `hpat-inv`/`Flips` read the H-pattern back as four flips of one code (via `diff`'s worker, now the top-level `Bitstrings.diffFrom`). **And it found a transcription bug**: `Encoding.swaps←`/`swaps→` ran their products over i + 1 … j − 1 the wrong way round (↑ for the paper's ∏←), which carries wire j only to j − 1 — the same as the right thing only when j − i < 3, always true on three qubits, where Checks8 decides (36)/(37), so it was invisible there. So (36) and (37) as transcribed were *unsound* from four qubits on; `scratchpad/tW.py` simulates the network's signed permutation and found it (8 of the 48 H-pattern tuples at four qubits land wrong, 48 of 160 at five), and with the ranges reversed all 700 tuples at three to six qubits land right. The fix changes nothing at three qubits, where the lists coincide.

    The sign half of the reduction is done. Pairing the indices off is the canonical spelling but not the convenient one, since adding an index shifts every pair after it; (DE-ZZ) cuts each pair through the index 1, so a sign part is equally a product of one **atom** `zz 1 x` per index (`atoms`, `signOf-atoms`), and those commute and square to ε. Adding an index is then local: `toggle` inserts or deletes, `atoms-toggle` says that is multiplication by one atom, `merge-zz` merges a whole sign letter by two toggles, and `toggle-Incr` / `toggle-Even` / `toggle-Odd` keep the list sorted and flip its parity (stated mutually, since one toggle flips it). `absorb-zz` is then the reduction's sign step in one line, the word being processed from the right so each letter arrives on the left. What remains is the mixed-letter step — absorbing a letter into the Lehmer code — and uniqueness against the semantics.

    A warning worth not filtering out: a first draft of `NF` typechecked while being silently wrong, because `suc` was imported but `zero` was not, so `runFrom d zero = ε` matched a pattern *variable* named `zero` and the recursive clause was dead. Only `PatternShadowsConstructor` and `UnreachableClauses` caught it. Two transcription notes for τ: in Definition A.2 `a + b` means the one of a, b that is not 0 and `ab` the one that is not 1 (each case guarantees the other index is that unit), and `Gen N`'s `X[ a , b ]` and `H[ a , b ]` carry no distinctness while `GenP n`'s constructors do — τ must therefore answer arbitrarily (say `ε`) on a degenerate letter, which is harmless because only `f x` and the sides of Figure 7 axioms are ever run through it, and those have distinct indices by construction.
  - **Lemma 8.7 at every width, and Theorem 8.9 by induction on the width.** `GeneralN/Lemma87` proves `Section8.Lemma-8-7 m` from a record `BoxFrames.Canon m` of six facts about the box at the canonical position (involution, X on its box wire, networks on its controls (307), the merge (309), (274), (336)) and the merge (309) at every smaller width: `BoxFrames` carries a statement at the canonical position to any two wires by a swap network (`pl`, `frame-eq`, `three-boxes` — Lemma 8.4's step; `Lemma84` is Lemma 8.4), `MergeAll` merges the decoded boxes of E(Z)/E(CZ) in the box frame (`Lemma87Z`, `Lemma87CZ`), and `Lemma87H` reduces E(H)/E(CH) to them by conjugating with P ⊗ P and (113)/(18); `OneWire`/`TwoWire`/`PlaceAt`/`PlaceCalc`/`LocalPlace`/`NetWires`/`SwapCalc` are the placement calculus. `Canon3` (from completeness on two qubits), `Canon4` (Lemma D.5) and `CanonN` (width ≥ 5, from completeness below via `BoxSym`, `BoxForms`, `Keystone`) supply the record; `Lemma87Three` and `Lemma87All` assemble Lemma 8.7 per width. `CompletenessInduction.Theorem-8-9″` builds completeness width by width (`below`), discharging Lemma 8.7; its hypotheses are Theorem 4.4 (two qubits, and at the width of P) and Lemma 8.8 at each width given completeness below. **Lemma 8.8 (Appendix E.5) is begun** in `Lemma88/`: `Easy` has the rules that decode by definition ((20), (21), (25)–(28), (30), (33)–(35), (42)), `Invol` (29) and (41), and `Rule22` (22) — the decoding of a sign pair is a chain of boxes, each a placed coloured box (Lemma 8.4's `MI` at distance one), so with S x the chain from 0 to x the decoding of zz x y is S x • S y whichever is larger and the rule is S a S b S b S c = S a S c. It rests on the colour-general box commutation, now proved at every width: `GeneralN/Gadget318` ((318) all black), `Gadget319` ((319) in every colour on the wires 1 2: one semantic step one width down writes the box, wire 3 idle, as a product of at most four boxes — both controls, one, the other, neither — and ZX₃ passes each by (291), (290), (290) under a swap, and (308)), `Box320` ((318) in every sign and colour on wire 1, and (320) from (295)), `Colours` (a colouring `col s w = negsB s • w • negsB s` peeled wire by wire, `norm-ZX`/`norm-K`: a coloured rotation white on wire 2 is a gate of (241)/(240), `col-place`), `BoxComm` (**(335) and (336) at the canonical position in every colour**, `C335`/`C336`: the paper's induction on the width, with the base on four wires decided — `Base335`/`Base336`, eight evaluations each with completeness on four qubits — and the step expanding both boxes by (320), moving a white control to wire 2 by adjacent swaps ((307), `move`), and checking that every letter of one expansion commutes with every letter of the other (`pair`, `all-pairs`): rotations by (240)/(241), a rotation and a box by (318)/(319), two boxes with wire 3 idle by the statement one width down) and `BoxAnywhere` (any two placed boxes `boxF u s` in any colours commute: a colouring passes a network with its bits permuted, `net-negs`, two compose, `negs-xor`). `GeneralN/Col` holds `col`, `B₁`, `C335`, `C336` with no parameters so that the stored evaluations and their uses spell them identically: a stored `Evaluated` compared with a differently spelled use unfolds `Same` and evaluates with the colouring bits free (8 GB). **(338)**, the box on wire 0 against the H gate on wire 0 whose box wire is wire 1 (`Col.Hg` = P ⊗ P around `B₁`), is proved at every width from five on: `Gadget322` is (322)/(323) (the triply controlled ZX between P ⊗ P, `Aᴾ α true true`, against the box with wire 3 idle, white on wire 2, box wire 0 or 1 — `Vb` — in every top colouring), the paper's induction with the base on four wires decided (`Base322`) and the step expanding the box one width down by (320) (on four wires decided too, `Base338.d320`): its rotations land on the wires 0 1 2 4 (`place3-loc`) and are the second gates of the five-qubit (267)/(268), its smaller box is the statement one width down around wire 4 (`place-34`, as Idle's `place-swap`); `Box338` is (338) with the colourings differing on wire 2 (`Sep`), the all-pairs induction of BoxComm with the H gate expanded between P ⊗ P: rotations by (242)/(243), mixed pairs by the gadget (conjugating by P ⊗ P, or by the colouring), the two smaller gates one width down; `Box338Eq` is (338) with x = y (`XY.eq338xy`) — not the paper's route over the 2ⁿ − 1 cases x ≠ y and a merge of all colourings, but BoxForms' argument for (336) between P ⊗ P, found by the analysis of the decoding of (39): R Λ R = (R W R) B (R V R) B by the E-form (284), B₁₀ = B D with D negated on wire 2, and D passes B and R W R, R V R (`g292`, a P ⊗ P version of (292), by width induction with three four-wire evaluations, `Base338Eq`) — plus X on the H gate's box wire (`X-Hg`: Z H Z on the box wire between P ⊗ P, (19) and (285)). `Rule39` is (39): the decoded sign pair on 0 1 and the decoded H_[0,1]H_[3,2] (`Decoding.gadget`) unfold at index 0 to the box and the H gate sharing the white negations of the wires 2 …, which are conjugated away, leaving the parameter `core` that `Box338Eq.XY.core39` supplies; `Lemma88/Wide` collects the rules proved at width 5 + k from completeness below ((22), (39)). The other rules — (23), (24), (31), (32), (36)–(38), (40), (43)–(46) — need further equations of Appendix D.12–D.15 ((307)–(361)) and Lemmas 8.5, 8.6; (308) is `GeneralN/Ancilla`. A map of exactly which instances each rule's decoding uses (colours, x = y or not) was made by reading every E.5 proof at 450 dpi: most x ≠ y uses are commutations of gates with opposite colours on a common control wire, and (338) with x = y is used by (39), (40), (44), (45), (46). Widths 3 and 4 of Lemma 8.8 are still open for every rule that needs completeness on four qubits. A module-level `private variable` generalised in a type comes *before* its explicit `∀ {k}` (bind it by name, `{r = r}`); names like `back`, `mid`, `unconj`, `swap-order`, `X` clash with `Tools`, Figure 4 or Syntactics when opened alongside; Lemma 8.4's induction passes its recursive call into the helper that cases, since from inside the helper it looks like an increase.
  - **Performance discipline.** Stored-matrix evaluations — every `Same u v`, CForm's `cf-~`, the literal identities (`Evaluation.by-rows`) — are decided by one boolean, `Eq.refl : eqM A B ≡ true` with `Semantics.Decide.eqM-sound`, on the row-operation reading `Evaluation.⟦_⟧R` (each gate a sum, difference or ±√2 scaling of rows; `⟦⟧R-ix`, `⟦⟧R≡⟦⟧M`). `Eq.refl` on an equation of tries makes the conversion checker compare them entry by entry and re-evaluate the whole product of dense matrices for each entry: a hundred to four hundred times slower (Soundness.Relators 525.6 s → 1.2 s, FourQubit/LemmaD5 about an hour → 54 s, one 80-gate three-wire equation 158 s → under a second). Write new evaluations the same way; give `Soundness.Relators.same-≡`/`≡-same` their words explicitly (`Same` is a function, so unification cannot read them back and evaluates with unknown words instead); and a literal-product chain `Eq.trans (Eq.cong₂ mulM …) …` is no longer needed — use `by-rows w Eq.refl`. Operators are functions, so Agda decides whether two are equal by applying them to arguments and normalising; for the product of forty gate matrices that is a sum over every intermediate index, exponential in the word, and Agda does it whenever two readings of a long word are not *syntactically* identical — which happens for reasons invisible in the source (an implicit width elaborated as `3` in one place and `₃₊ 0` in another). So `⟦_⟧ₒ` is **`abstract`**: two readings `⟦ w ⟧ₒ`/`⟦ w′ ⟧ₒ` are then compared by comparing the words, which is linear. Its defining equations are the lemmas `⟦⟧ₒ-gen`/`⟦⟧ₒ-ε`/`⟦⟧ₒ-•` (and `•₃`/`•₄`), and `localise`/`⟦⟧-ix`/`up-word`/`⟦⟧ᴱ-def` live in the same abstract blocks; proofs that need `⟦ w • v ⟧ = ⟦ w ⟧ ∙ ⟦ v ⟧` on the nose (a homomorphism law: `sound`, `BackAndForth`) use the StarInterp reading `Ext.⟦_⟧ᴱ` and transport with `⟦⟧ᴱ-~`/`~-⟦⟧ᴱ`. Three further rules: the power `√2^_` is evaluated strictly level by level (`Ring`), because the lazy `√2 ^ n` is a tree of size 2ⁿ to the conversion checker and `√2^ 27` alone exhausts memory — closed scalars are then safe to evaluate; every lemma about `_~_` gets its words explicitly (`_~_` unfolds to a scaled equation in which `√2^ ?ℓ` is not a unification pattern, so such implicits are never solved), and closed three-wire words have one spelling (`BoxMatrices.CCZX₀` etc.) so that stored-matrix identities are compared syntactically rather than by re-evaluating the tries. Stored matrices are `abstract`; the actual checks are `refl` on tries.
- **`Groups/CNOT-Dihedral/`**: Amy, Chen and Ross, *A finite presentation of CNOT-dihedral operators* (arXiv:1701.00140) — the gate set {ω, X, T, CNOT} with SWAP as the symmetry, the thirteen relations of its Figure 1, the phase-polynomial semantics, soundness, the derivable rules its existence proof rests on, and completeness assembled with the paper's imported and reordering lemmas as hypotheses. Self-contained and not reached from the root — typecheck `Examples/Groups/CNOT-Dihedral/Completeness.agda` (under a minute; the relation checks are on tables of at most sixteen entries; the one-qubit uniqueness decision needs more than 4 GB, so run under `ulimit -v 7000000`).
  - `Syntactics`: `Gate` has ω (0-ary), X, T (1-ary), CNOT, SWAP (2-ary); U and V (Definition 3.2) are words; gates on non-adjacent wires (`CNOT₂₀`, `U₀₂`, `U₁₃`, `U₀₃`, `V₀₂₃`, `V₀₁₃`) are the gate on the top-most wires conjugated by the swaps that carry its lower wires up, the paper's convention. `_SRel,_===_` is R₁ … R₁₃ plus the laws of the symmetry (an involution, the braid relation, naturality for X, T and CNOT — ω's is the theorem `comm₀`, opened from `Central-Scalars`); `Lift-Relation` adds the structural rules; `n ⊢ w ≈ v` is the congruence; `grouplike`. Wire 0 is the bottom wire; CNOT's control is wire 1, its target wire 0.
  - `Semantics/Z8` (ℤ₈ as `Fin 8`, laws by exhaustion) and `Semantics`: an operator is `Op n = Bits n → Bits n × ℤ₈`, x ↦ (f x , p x) — the phase polynomial representation; composition adds phases along the way, so there are no sums and a relation check is a `refl` on a table (`Mat k`, `matOf`, `ix`, `mat-dec`); `emb`/`up`/`split` as in Real-Clifford+CH; `Op-monoid`.
  - `Interpretation` (abstract `⟦_⟧` with `⟦⟧-gen`/`⟦⟧-ε`/`⟦⟧-•`, `Ext.⟦_⟧ᴱ` and `⟦⟧ᴱ-def`, `⟦_⟧M`, `localise`, `by-relator`), `Soundness/Relators` and `Soundness` (Proposition 3.7: `axiom-sound`, `soundᴱ`, `sound`).
  - `Derived`: the 24 rules of Figure 2 and the 4 of Lemma 5.2, each an equational derivation from the relations (`Tools` bundles the congruence, `front`/`back`/`mid` rewriting and the associativity solvers). Two lemma families do the work: naturality of the symmetry for whole circuits (`nat₁`, `nat₂`, `nat₃`, by induction from the generator laws), which yields every swap rule; and the dihedral group of the three CNOTs on three wires (`braid-CNOT`, `CNOT↑-CNOT₂₀`, …: R₆ makes `CNOT₂₀` the commutator of `CNOT` and `CNOT ↑`, which has order 2, so it is central), which lets a CNOT pass a U or V it shares a wire with (`CNOT-U↑`, `CNOT-V`, `CNOT-V↑`).
  - `OneQubit`: completeness on one wire outright (normal forms ω^k T^a X^e, an automaton respecting the relations by decision, uniqueness decided on the 128 tables, `MonoidSem.GetSubPresentation`).
  - `Completeness`: a `NormalForms n` record naming the paper's lemmas as hypotheses (Lafont's affine normal forms, Lemmas 5.1/5.3, 6.2/6.3), the separation step of Proposition 6.4 proved (D · A = D′ · A′ as operators forces A = A′ then D = D′), and the theorem and `subpresentation` assembled from them. No postulates.
- **`Amalgamations/CliffordT1.agda`**: the qubit Clifford+T gate set as an amalgamated product, ending in a monoid isomorphism.
- **`Amalgamations/QutritCliffordT1.agda`**: the qutrit Clifford+T analogue.
- **`Amalgamations/U33Di.agda`**: U₃(ℤ[½,i]) presented as a two-level amalgamated product.

## Key conventions

- `_===_` always means the raw relation (the axioms); `_≈_` always means the monoid congruence it generates (a congruence for `•`, closed under the monoid laws).
- `[_]ʷ` injects a generator into `Word`. `[_]ₗ`/`[_]ᵣ` are left/right embeddings in products.
- `(f ʷ)` extends `f : X → Word Y` to `Word X → Word Y` (postfix `_ʷ` = `wconcatmap`).
- `(h ᵗ)` extends a coset action `h : C → Y → Word X × C` to words, threading the coset.
- `nfp` (`NormalFormInjective`) and `nfp'` (`NormalForm`) are the standard names for normal-form witnesses.
- `by-equal-nf` proves `w ≈ v` from `nf w ≡ nf v`; `by-assoc` proves `w ≈ v` from `to-list w ≡ to-list v`; `by-passoc` re-brackets guided by pattern words built from `□`.
- Files follow the agda-stdlib style guide (see `style-guide.md`): 72-char banner headers with library line `-- Presentations of groups`, `{-# OPTIONS --safe #-}`, imports sorted with `using` lists, `private variable` blocks, sentence-case section separators.
- `MainTheorems.agda` is the style exemplar: it re-states the main theorems with definitions imported openly and proofs imported qualified, each section's banner naming the module the proof lives in. It is also the only façade — the per-development `Theorems.agda` layer was removed for the symmetric group, whose results MainTheorems now takes straight from `Presentation`, `UniqueNormalForm` and `SubPresentation/*`.

## Stdlib compatibility notes

- `Homomorphic₂` is imported from `Relation.Binary.Morphism.Definitions` (re-exported `Congruent`). That module is parameterised by `A B : Set`, so `A`/`B` are implicit at the call site — do **not** pass them explicitly.
- `IsMagmaHomomorphism` uses field `∙-homo` (renamed from `homo` in stdlib v3.0).
- `Data.Product.Relation.Binary.Pointwise.Dependent` is from stdlib master; it may not exist in older releases.
