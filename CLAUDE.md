# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is the Agda formalisation accompanying the paper *"A Complete and Natural Rule Set for Multi-Qudit Clifford Circuits in All Odd Prime Dimensions"*. It is being prepared for submission to the Agda standard library. Tested with **Agda 2.8 + stdlib 2.3** (also works with Agda 2.7 + stdlib 2.2).

## Typechecking

```bash
# Typecheck via WSL (Agda 2.8, resolves dependencies automatically).
# This single root reaches most of the library through the results it
# states, including CliffordT1, QutritCliffordT1, U33Di, and the qupit
# projective Clifford chain (Paper-V1 → Paper-V0 → Simplified-V1 →
# SemiDirect, plus Shared/PauliBase):
wsl --exec /home/onest/.cabal/bin/agda MainTheorems.agda
```

What the root does **not** reach, as of the last check: `ProjectiveClifford/Qupit/Simplified-V2`, `ProjectiveClifford/Qubit/` and `Clifford/Qupit/` — nothing `MainTheorems` states depends on them, so they need typechecking separately if you touch them.

Use WSL Agda 2.8 (`wsl --exec /home/onest/.cabal/bin/agda`) for all files. The WSL install uses its own stdlib at `/home/onest/.agda/lib/agda-stdlib/`. The `.agda-lib` file (`qupit.agda-lib`) includes `.` and depends on `standard-library`.

**Always re-typecheck `MainTheorems.agda` after any edit to library files.** From PowerShell, invoke WSL directly (Git-Bash mangles the Linux path).

## Architecture

The library is layered bottom-up:

### Layer 0 — Notations (`Notations.agda`)
Numeral patterns `₀`–`₉`, successor patterns `₁₊`/`₂₊`/`₃₊`/`₄₊` (overloaded over ℕ and Fin), and the `auto` pattern (= `Eq.refl`).

### Layer 1 — Free monoid (`Word/`)
- **`Word/Base.agda`**: the `Word X` type (free monoid over generators `X`): constructors `[_]ʷ`, `ε`, `_•_`; powers `_^_`/`_^'_`; `wmap`, `wconcat`, `wconcatmap` and its postfix notation `_ʷ` (so `(f ʷ)` extends `f : X → Word Y` to words); folds `wfoldr`/`wfoldl`; the stateful traversals `_ᵗ`/`_ᵗ'` that drive coset enumeration; conjugation helpers `_ʰ`/`_ⁿ`/`_ʰ'`/`_ⁿ'`; `WRel X = Rel (Word X) 0ℓ`.
- **`Word/Properties.agda`**: `wmap`/`wconcat` fusion laws, `lemma-ʷ-∘`, `lemma-fʷ-w^n`, `wfoldr-cong`/`wfoldl-cong`, and `≡-dec` (decidable equality of words).

### Layer 2 — Group presentations (`Presentation/`)
- **`Base.agda`**: parameterised by `Γ : WRel X`. `_===_` is the raw relation; `_≈_` the monoid congruence it generates (refl/sym/trans/cong/assoc/left-unit/right-unit/axiom); `refl'` lifts `_≡_`; combinators `cleft_`, `cright_`, `_reversed`; `Alphabet = X`.
- **`Properties.agda`**: `≈-isEquivalence`, `word-setoid`, magma/semigroup/monoid structures and bundles; the associativity solvers (`to-list`/`from-list`, `mod-assoc`, `by-assoc`, `by-assoc-and`, and the pattern-guided `Pattern-Assoc.by-passoc`); word-power lemmas (`lemma-^-+`, `lemma-^^`, `word-comm`, …); `wfoldr`/`wfoldl` congruence lemmas.
- **`Definitions.agda`**: `_IsPresentationOf_` (group), `_IsMonoidPresentationOf_`, and `module SubPresentation` (`Soundness` & `Completeness` of a semantics ⟦_⟧ : Syn → Sem, i.e. ⟦_⟧ is a setoid embedding).
- **`GroupLike.agda`**: `Grouplike` (every generator has a left inverse) and `Group-Lemmas` (`_⁻¹`, cancellation, uniqueness of inverses, the group `•-ε-group`).
- **`Morphism.agda`**: parameterised by presentations `Γ`, `Δ`. Builders turning generator-level data into `IsMonoidHomomorphism`/`Monomorphism`/`Isomorphism` and the group versions, for both `(f ʷ)` and `wmap f`.

### Layer 3 — Constructions (`Presentation/Construct/`)
- **`Base.agda`**: amalgamated product `_⊕_` and related combinators on `WRel`.
- **`Properties/DirectProduct.agda`**, **`SemiDirectProduct.agda`**, **`NDirectProduct.agda`**, **`SugarProduct.agda`**: lift normal-form witnesses through the constructions. `SemiDirectProduct` takes a **word-valued** action `conj : H → N → Word N` (the relation `ConjRelʷ conj`); an earlier element-valued version, `conj : H → N → N`, was deleted once nothing imported it.
- **`Properties/Amalgamation.agda`**: amalgamated free product with coset normal form (`AmalDataNF`, `ANF`).

### Layer — Circuits (`Circuit/`)
- **`Base.agda`**: parameterised by `Gate : ℕ → Set`. Wire-indexed generators `Gen`, `Circuit n = Word (Gen n)`, shifts `_↑`/`_↥ᵏ_`, and `Lift-Relation` extending any gate relation with the structural rules `cong↑`, `comm₀`/`comm₁`/`comm₂` and `ω↑=ω`. `Lift-Relation` also proves `lemma-cong↑` and `comm-gate₀-w`/`comm-gate₁-w↑`/`comm-gate₂-w↑↑`, the comm rules extended from a single generator to a whole circuit.

### Layer — Normalization (`Normalization/`)
- **`NormalForm/Setoid.agda`**: setoid-valued normal-form witnesses on the stdlib `Function.Bundles` — `NormalFormInjective` = `Injection`, `BijectiveNormalForm` = `Bijection`, `NormalForm` = `RightInverse` (maps `word-setoid ⟶ₛ NF`) — plus `WeakNormalForm`. Uniqueness is **not** here.
- **`NormalForm/Uniqueness.agda`** (and `Uniqueness/Propositional.agda`, the same at the discrete setoid): `UniqueNormalForm`, `by-normalization` (soundness + unique NF ⇒ completeness) and the converse `by-completeness` (completeness + exact section `nf ∘ inv-nf ≗ id` ⇒ unique NF). `UniqueNormalForm` is indexed by the **section** `inv-nf`, not by a whole `NormalForm` record — so a client that already has the record passes `SNF.NormalForm.inv-nf nfp`, and `by-normalization` needs the record given explicitly (`{nfp}`), nothing else determining it. A second copy indexed by the record used to live in `Setoid.agda`; it is gone.
- **`NormalForm/Propositional.agda`**: `Normalization.NormalForm.Setoid Γ (setoid B)` re-exported for a plain carrier set `B` (an explicit module parameter) — the witnesses land in `≡` on `B` and all derivations are inherited. Types read `NormalForm Γ B` / `NormalFormInjective Γ B`. Because the re-export fixes the codomain to `setoid B`, the witnesses are function-aliases here, not record names: **opening or projecting a witness value goes through `Normalization.NormalForm.Setoid` directly (imported `as SNF`)** — e.g. `open SNF.NormalForm nfp renaming (…)` — while `Propositional` is used only for the `Γ B` types and record construction.
- **`Reidemeister-Schreier.agda`**: the injectivity/surjectivity engine. `Star-Injective-Simplified` proves `(f ʷ)` injective given a left inverse on generators; `Star-Injective-Full` (and its setoid variant) does coset enumeration and provides the Schreier section, right/left normal forms.
- **`CosetNF.agda`**: coset normal forms via Reidemeister–Schreier: `lemma-ᵗ-act` (letters-to-words action law), `module SingleLevel` (one level; its `Transfer.Unique` derives `nf' ∘ gg ≡ id` for the transported NF from base exactness + coset exactness of the table on sections, via cancellation and R–S injectivity), `CosetTable` / `PackedCosetTable` (coset tables with a distinguished identity coset), `CosetTower` (iterate up an ℕ-indexed family).

### Layer 4 — Specific groups (`Examples/Groups/`)
- **`Cyclic/`**: ℤ/nℤ. `Syntactics` (the relation `_Cn,_===_` over a one-element alphabet), `Presentation`, `Normalization`, `Theorems`.
- **`Trivial/`**: the trivial group, two presentations proved isomorphic. Everything is derived once from the single hypothesis that every generator is `≈ ε` (`gen≈ε`), then instantiated twice, so the directory is a **generic layer plus two instances**.
  - Generic, at the root, each parameterised by `(Γ, gen≈ε)`: `Normalization` (`w≈ε`, `nf`/`nfp`/`nfp'`), `Interpretation` (`⟦_⟧₀`, `GS`, `fʷ-cong-ax`, `grouplike`), `UniqueNormalForm` (`unfp`). `Semantics` is the shared target (`Terminal.group`, as `gp`) and takes no parameters.
  - **`Presentation1/`** is `⟨ ⊥ ∣ ⟩` (`EmptyRel`), **`Presentation2/ A`** is `⟨ A ∣ w = ε ⟩` (`TrivialRel`). Each has the same five modules: `Syntactics` supplies `pres` and `gen≈ε` (the only real content); `Normalization`, `Interpretation`, `UniqueNormalForm` just instantiate their generic namesakes; `Presentation` assembles `subpresentation`/`presentation`. Clients wanting the trivial group as a base case take `nfp`/`nfp'`/`gp`/`presentation` off `Presentation1.Presentation` (`NDirectProduct` does, at width 0).
  - `Presentation-Equivalence B` gives the monoid isomorphism between the two — a syntactic statement that never touches the semantics.
  - Note `gp` is re-exported, not abstracted over the module parameter, so it is `Presentation2.Presentation.gp`, never `.gp A`; `presentation` does take `A`.
- **`Symmetric/`**, **`Symplectic/`**, **`Clifford/`**, **`ProjectiveClifford/`**, **`Pauli/`**, **`ProjectivePauli/`**: see Layer 5.

There is no longer a `Presentation/Groups/`: it held a second Sₙ and a hand-rolled ℤ/4ℤ ≀ Sₙ, both over an inductive alphabet rather than circuit generators, and both are gone. The wreath product now comes from `Examples/Construct/SemiDirectProduct/SnD.agda`, which supplies `pres`, `nfp` and `nfp'` over the `Examples/Groups/Symmetric` alphabet.

### Layer 5 — Examples (`Examples/`)
- **`Groups/Symmetric/`**: completeness of the circuit presentation of Sₙ. There is no `Theorems.agda` façade — `MainTheorems` takes the five results (presentation, unique normal form for each semantics, soundness, completeness) straight from the modules that prove them. The layout follows the two chains: the **permutation** one is at the top level (`Semantics`, `Interpretation`, `Soundness`, `UniqueNormalForm`, `Surjectivity`, `Presentation`) and reaches a full presentation — `Semantics` is the target group alone and mentions no syntax, `Interpretation` is `⟦_⟧`/`⟦_⟧ᵍ`/`⟦↑⟧`, and `Soundness` is that `⟦_⟧` respects the relations; the **endofunction** one is `SubPresentation/` (`Semantics`, `Interpretation`, `UniqueNormalForm`, `SubPres`) and stops at a setoid embedding, since endofunctions are not all denotations. `Presentation` splits like Symplectic's: `subpresentation` needs only normalization, and `Surjectivity` is what promotes it. Shared support: `Syntactics`, `Cosets`, `Normalization`. `Completeness.agda` and `IndexedAction.agda` typecheck but nothing imports them.
- **`Groups/ProjectiveClifford/Qupit/`**: the paper's own subject. For an odd prime p, the qupit Clifford circuits read **modulo scalars** present `Pauli n ⋊ Sp(2n, ℤ/pℤ)`. Four rule sets over the same alphabet (Symplectic's `H`, `S`, `CZ`) — Simplified-V1, Simplified-V2, Paper-V0, Paper-V1 — each shown to present that group by transport from the one below it, over a base (`SemiDirect/`, on a different alphabet) that does the real work. Every layer above the base is an isomorphism plus a composition, which is why the stack is cheap to extend and why no rule set re-proves what another already has.
  - **`SemiDirect/`** is the base and defines `Pauli⋊Sp`. `Presentation` feeds the generic `SemiDirectProduct` machinery its two factors — `Symplectic.Simplified` for the symplectic part, `ProjectivePauli` for the Pauli part — together with `ConjAction`'s two well-definedness proofs for the conjugation action. Every input is a theorem, so it is unconditional.
  - **`Simplified-V1/`** is where the mathematics lives: the Pauli calculus (`Lemmas`, `LemmasXZ`, `LemmasCZ`, `SemiM`), the Ex-conjugation rules (`ExRules`), `Forward`, `Soundness`, a rewriting tactic layer (`Tactics`), and `Iso` onto the semidirect relation.
  - **`Paper-V0/`** is Figure 1 with the multiplier spelled over `R = S • Z^½`, as `RHR x x⁻¹`; 16 group-specific rules. Its `Iso` proves it isomorphic to Simplified-V1 and exports **`v1⇒pap`**, which transports Simplified-V1 theorems into its theory — which is why its `Lemmas` derives Selinger's c10–c15, the lower-wire rules and the swap calculus natively.
  - **`Paper-V1/`** is the same rules with the multiplier spelled over `S`, as `SHS' x⁻¹ x`, and 15 rules rather than 16: `(S • H) ^ 3 = ε` is a theorem here, not an axiom, because under that spelling `M₁` *is* that word and `M-power` at `k = 0` already gives it. `Iso` is the identity on words onto Paper-V0. `Lemmas/` carries only what that needs (`OneWire`, `GroupLike`, `XZ`) — the Ex-conjugation and three-wire layers it inherited from Paper-V0 were removed, since the isomorphism transports all of it.
  - **`Shared/PauliBase.agda`** is the relation-agnostic Pauli calculus, instantiated at whichever rule set needs it. `Calculus` rests on the axioms both spellings share; `BridgeCalc` adds `X • Z ≈ Z • X` as a *hypothesis* — the one fact whose proof genuinely differs between them — and builds the bridge between the R- and S-spellings of the multiplier, plus `MulZ`/`SemiMR` for carrying a Pauli across it.
  - **`Simplified-V2/`** transports the V1 theorem once more along an identity isomorphism. Nothing imports it.
- **`Groups/ProjectiveClifford/Qubit/`**: the qubit analogue (`CliffordExtension`, `Cocycle`, `ExtensionPresentation`, a `Selinger/` subtree). Self-contained — nothing outside the directory imports it.
- **`Amalgamations/CliffordT1.agda`**: the qubit Clifford+T gate set as an amalgamated product, ending in a monoid isomorphism.
- **`Amalgamations/QutritCliffordT1.agda`**: the qutrit Clifford+T analogue.
- **`Amalgamations/U33Di.agda`**: U₃(ℤ[½,i]) presented as a two-level amalgamated product.

### Separate development — Path-sums (`PathSum/`)

Amy's path-sum calculus (QPL 2018). The core is §4 — lemmas 4.1–4.3, and corollary 4.4 for circuits over {H, S, CZ} as the paper *proves* it, about the circuit itself and syntactically (`corollary-4-4-any`, `corollary-4-4-syntactic`); its polynomial-time statement is not formalised. Around it: lemma 2.5 in general; composition (def. 2.6, prop. 2.7, remark 2.8 up to ≋); the paper's gate set {H, CNOT, R_k} (def. 2.9, props. 2.10 and 2.14); ℤ[ζ] as a ring, definition 2.4, and unitarity of every circuit over both gate sets; all of figure 2 ([Case], Boolean-valued quotients) at any variables, with decidable matching; corollary 4.4 by the paper's Gaussian-elimination route and under any rules in any order; lemma 4.1/corollary 4.4 up to a global phase; equivalence of two circuits by the miter; and the worked examples (at M₀ = 0). **Not reached by `MainTheorems.agda`**; its own root is `PathSum/Theorems.agda`, which covers the whole directory. Typecheck it with `wsl --exec /usr/bin/agda +RTS -M10G -RTS PathSum/Theorems.agda` — **always under a heap cap**: `Denotation` alone peaks near 10 GB and takes ~250–270 s CPU, and an uncapped run once exhausted the WSL VM. `Cyclotomic` takes ~2 min; everything else is cheap (a few minutes together).

- **`Polynomial.agda`** / **`Polynomial/Properties.agda`**: multilinear polynomials over the dyadic rationals. `Mon n m = Subset n × Subset m` (input and path variables), `Poly n m = Mon n m → ℤ` (numerators over 2^M), `Σsub`/`Σmon`, `eval`, `liftXor` (the lifting of a Z₂-linear form), `subst`. Properties holds lemma 2.5 (`liftXor-value`), the evaluation homomorphism, and the substitution theory: `eval-subst`, `eval-split`, `eval-subst-fixed`, `Σsub-at`/`Σmon-at` (a sum splits at *any* index, not only the head), `liftXor-split`, `hh-case`.
- **`Order.agda`**: the order of a phase polynomial (def. 2.11) as a 2-adic divisibility predicate, and lemma 2.13 (`subst-Ord≤`).
- **`Base.agda`**: `record PathSum (n k m : ℕ)`, `idPS`, `y₀`, `head-part`/`tail-part`, `Internal`.
- **`Reduction.agda`**: `⅛`/`¼`/`½`, the reducts, and `_⟶_` with constructors `elim`, `ω`, `hh` — each on the first path variable, with Z₂-linear quotients (`liftXor c S`), which is all lemma 4.3 needs. `[Case]` and general quotients are in `Reduction/General`, a second relation; this one is left alone because `Denotation` proves it sound.
- **`Mobius.agda`**: Möbius inversion modulo c — a multilinear polynomial whose value at every Boolean point is divisible by c has every coefficient divisible by c (`values⇒coefficients`, and `values⇒coefficientsᵐ` for any number of path variables).
- **`Cyclotomic.agda`**: ℤ[ζ] = ℤ[X]/(X^H+1) with `H = 2^(2+M₀)`, `N = 2H`, `c = N/8`. `Amp = Fin H → ℤ`, `_≐_`, `zpow`, `rot`, `√2·`, `scale`, `Σᴮ` (sums over assignments), `Σᴮ-at`, `scale-injective`, and the coordinate facts `zpow0-at-0`, `√2·zpow0-at-c`, `2·≢scale-zpow0`.
- **`Denotation.agda`** (checked `--call-by-name`): `amp`, `hits`, `_≋_`, the three soundness proofs, lemma 4.2, the undersized lemmas, and `semantics`. Layered in modules by *what premise they need*: `Branches ξ eqf` (the pair of y₀ branches, no premise), `Cancel ξ c S eqP eqf` (head ≈ ½·form), `ωBranches` (head ≈ ¼ + ½·form). Each re-exports the one below with `open … public`.
- **`Semantics.agda`**: the interface `record Semantics` — `_≋_` and its equivalence, `⟶-sound`, `interference`, `undersized-elim`, `undersized-ω`.
- **`Clifford.agda`**: §4.3 over that interface — `progress`, `lemma-4-3`, `corollary-4-4`, and `⟶*-sound`, proposition 3.1 along a whole chain.
- **`Circuit.agda`**: the two hypotheses of corollary 4.4, discharged. Clifford circuits over `H`, `S`, `CZ` (`Gate`, `Circuit`), and `⟦_⟧ᴿ`, the isometry restriction of §4.1 already reified — a Hadamard allocates a path variable only when a later one touches its wire, since otherwise `f (x , y) = x` forces that variable to be `x_w`. Hence `norm C` counts every Hadamard while `paths C` counts only those, and `⟦⟧ᴿ-Internal`/`⟦⟧ᴿ-Ord≤` are theorems. Also `⟦_⟧`, the unrestricted path-sum of definition 2.9 (`runᵁ`: every Hadamard allocates), with `pathsᵁ≡norm`, and the public `run-sig`/`⟦⟧ᴿ-sig` (every output of `⟦ C ⟧ᴿ` is its input). Semantics-free: it imports only `Base`, `Order`, `Polynomial` (with `Properties`) and `Reduction` (for `¼`/`½`).
- **`Assign.agda`**: shared by `AssignSum`, `CircuitAmp`, `CircuitSemantics`, `Isometry`, `Decide` and `Syntactic` — `_[_≔_]` (overwrite one bit) and its pointwise laws, `_=ᵇ_`, `same` (two assignments agree everywhere), `[_]ᶻ`.
- **`Norm.agda`**: the trace form on ℤ[ζ], `⟪ a , b ⟫ = Σ_{j<H} coeff a j · coeff b j` and `‖ a ‖² = ⟪ a , a ⟫` (= Tr(a·ā)/H). Positive definite (`‖‖²-zero`), preserved by every `rot` (`⟪⟫-rot`, via a periodic-window lemma on `coeff`), `parallelogram`, `‖‖²-√2` (the cross term vanishes because multiplication by i is antisymmetric), hence `‖‖²-scale` and `‖zpow0‖²`.
- **`AssignSum.agda`**: `Σᶻ`, integer sums over all assignments — **opaque** (a transparent one stuck on an unknown `k` leaves implicit `k`s unsolvable); use `Σᶻ-zero`/`Σᶻ-suc` and the lemmas `Σᶻ-point` (split off one assignment), `Σᶻ-at` (pair the two values of a bit), `Σᶻ-≥0`, `Σᶻ-≡0`.
- **`CircuitAmp.agda`**: `toPS`/`ampˢ`, the path-sum and amplitudes of an interpretation state, and one lemma per gate (`ampˢ-S`, `ampˢ-CZ`, `ampˢ-allocH`, `ampˢ-finalH`, `ampˢ-init`): each gate changes the amplitudes by its own unnormalised matrix.
- **`CircuitSemantics.agda`**: `Column`, `δ`, `gateᴬ`, `applyᴬ` (the circuit's matrix, gate by gate); `prop-2-10` (`amp ⟦ C ⟧ x z ≐ applyᴬ C (δ x) z`), `⟦⟧-unit-columns` (every column of `⟦ C ⟧` has trace-form norm 2^(norm C); orthogonality is `Unitarity`), `⟦⟧ᴿ-restricts` (same diagonal as `⟦ C ⟧`) and `⟦⟧ᴿ-diagonal`.
- **`Decide.agda`**: the test on a fully reduced path-sum, input by input. `id-if′` (no normalisation, the single path returns every x, phase ≡ 0 mod 2^M at every x ⇒ identity), its converse `id-no-norm`/`id-only-if′` from the refutations, together `id⇔′`; `search`, a finite search over assignments for any decidable property that respects pointwise equality; and `decide-≋-id : Dec (ξ ≋ idPS)` for `ξ` without path variables (decidability as such is elementary — finitely many entries).
- **`Syntactic.agda`**: the converse of `Identity.id-if` — a path-sum without path variables that is the identity has no normalisation, outputs the inputs mod 2 and phase 0 mod 2^M, *coefficient by coefficient* (`id-only-if`, via `Decide.id-no-norm`/`id-only-if′`, the mod-2 reading `outBit-∣`, and `Mobius`); together `id⇔syntactic`.
- **`Isometry.agda`**: lemma 4.1 for any path-sum. `WellFormed ξ` = every column has trace-form norm ≤ 2^k (implied by definition 2.4: `PartialIsometry.PartialIsometric⇒WellFormed`; strictly weaker); `Restriction-id ξ` = `amp ξ x x ≐ scale k (zpow 0ℤ)` for all x (the restricted sum is the normalised 1); `lemma-4-1`, and `diagonal-≋` for path-sums none of whose paths leaves its input.
- **`Identity.agda`**: what `Reduces` stops short of — whether a path-sum with no path variables left is the identity. Such a sum has a single path, so its amplitude is `0` or the single power `ζ^P(x)`, and the question is one about coordinates in ℤ[ζ]: `id-if` (the criterion — no normalisation, outputs the inputs mod 2, phase `0` mod `2^M`, stated coefficient-wise) and `not-id-out`, `not-id-norm`, `not-id-phase` (the three ways it fails, each at one input). Coefficient-wise vs pointwise means these four do not visibly cover every case; `Decide.id⇔′` is the pointwise characterisation, and `Syntactic.id⇔syntactic` (Möbius inversion) shows `id-if` is also necessary. The refutations rest on `zpow≢scale`, that a power of ζ is never `√2^(1+j)`, proved by rotating back to `ζ^0` and appealing to `Cyclotomic.2·≢scale-zpow0`.
- **`Corollary.agda`**: the circuit-level proofs the root restates (`circuit-≋-id`, `circuit-WellFormed`, `lemma-4-1-circuit`, `reduct≋`, `circuit-id`/`circuit-not-id`, `corollary-4-4-⟦⟧`/`-any`/`-syntactic`, `circuit-decidable`, `matrix-decidable`), kept out of the root so other modules can import them — **no module in the directory may import `Theorems`**.
- **`Polynomial/Product.agda`**, **`Polynomial/Substitution.agda`**, **`Polynomial/Boolean.agda`**: the product `_*ᴾ_` (`eval-*ᴾ`, `monoᴾ`); substitution of any polynomial for a variable (`substᴾ`, `eval-substᴾ`, `Absent`, the quarters `q₀₀`…`q₁₁` that [Case] reads); Boolean-valued polynomials (`BoolValued`, `_⊕ᴾ_`, `¬ᴾ`, `BExp`/`liftᵉ`); lemma 2.5 for every Boolean polynomial (`liftᴮ`, `lemma-2-5`, `lift-unique`, `BoolValued⇔idem`).
- **`Reduction/General.agda`**: all of figure 2 at the head as `_⟶ᴳ_` — `elimᴳ`, `ωᴳ`/`hhᴳ` with Boolean-valued quotients, `caseᴳ` (removes two variables; its premises are the three quarter coefficients) — with `⟶⇒⟶ᴳ` and `⟶ᴳ*-bounded` (no longer exact). **`Reduction/Sound.agda`**, **`Reduction/CaseSound.agda`**, **`Pairs.agda`**: `⟶ᴳ-sound`, `⟶ᴳ*-sound` (appendix A). **`Reduction/GeneralCorollary.agda`**: the circuit verdicts along `⟶ᴳ` chains (`corollary-4-4-anyᴳ`, …).
- **`Interference.agda`**: lemma 4.2 for general quotients — `interferenceᴳ` (Q odd at some input: `¬ (Q ≈[ + 2 ] 0ᴾ)`), `interference-bool` (Boolean-valued and non-zero), and `lemma-4-2-as-stated-fails` (Q = 2x₁: "non-zero, integer-valued" is not enough).
- **`Reorder.agda`**, **`Anywhere.agda`**: rules at any internal variable. `front j` lists y_j first (`/ʸ`, `∖ʸ`, `eval-front`, `Ord≤-front`); `Anywhere R` lifts a head relation (`plain` / `at j`), `_∪ᴿ_` unions them; `_⟶ᵍ_ = Anywhere _⟶_` with `elimAt`/`ωAt`/`hhAt` stating premises at y_j; exact length `⟶ᵍ*-length`, `SN`, `⟶ᵍ-SN`. The renumbering is folded into each rule — a separate reorder step would loop. **`Anywhere/Sound.agda`**: `front-≋`, `Anywhere-sound` (generic in R), `⟶ᵍ*-sound`. **`Anywhere/Clifford.agda`**: `lemma-4-3-at` (progress at *every* internal variable), strategies. **`Anywhere/Corollary.agda`**: verdicts along `⟶ᵍ` chains. **`Anywhere/Match.agda`**: one-step reducibility decided (`step?`), `normal-form`.
- **`Ring.agda`**, **`Hermitian.agda`**: `_⊛_` (negacyclic product) and `conj` on ℤ[ζ] with distributivity, `zpow-⊛`, `⊛-rot`, `‖‖²-coeff0` (the constant coefficient of `a ⊛ conj a` is `‖ a ‖²`); the Hermitian form on columns (`Σᵃ`, `inner`). **`Ring/Laws.agda`**: `⊛-comm`, `⊛-assoc`, `⊛-identityʳ`, `conj-⊛`, `Σᴮ-⊛`/`Σᵃ-⊛`, `ℤ[ζ] : CommutativeRing` (up to `≐`), `conj-isRingIsomorphism` — it exports generic names (`≐-refl`, `+ᴬ-assoc`, …): import with a using-list. **`PartialIsometry.agda`**: definition 2.4 on the unnormalised operator — `gram`, `Isometric` (U†U = 2^k·I), `PartialIsometric` (U†U idempotent up to 2^k) — with `PartialIsometric⇒WellFormed` (t² ≤ ‖G_xx‖² ≤ Σ ‖G_xx″‖² = 2^k·t, no Galois theory), `lemma-4-1-isometric`/`-partial`, invariance under `≋`. **`PartialIsometry/Strict.agda`**: `WellFormed-strict` (½·id is `WellFormed`, not a partial isometry). **`PartialIsometry/Unitary.agda`**: `cogram`, `Coisometric` (UU† = 2^k·I), `Unitary`, `lemma-4-1-unitary`. **`Unitarity/Gates.agda`**: the gate matrices of both gate sets (`hadᴬ`, `phaseᴬ`, `cnotᴬ`), their effect on `inner`, and their symmetry (`dot-*`). **`Unitarity.agda`**: `circuit-Isometric`, `circuit-transpose` (U_Cᵀ = U_(reverse C)), `circuit-coisometry`, `circuit-Unitary` over {H, S, CZ}. **`CRK/Unitarity.agda`**: the same over {H, CNOT, R_k, R_k†} (CNOT via a conditional-swap invariance of `Σᶻ`) — its `norm-++`/`circuit-*` names clash with `Adjoint`'s and `Unitarity`'s.
- **`Adjoint.agda`**, **`AmpLinear.agda`**, **`Miter.agda`**, **`Equivalence.agda`**: `C †` (reversed, each gate inverted, S† = S³), linearity of `gateᴬ`/`applyᴬ`, `applyᴬ-++`, `†-cancelʳ`/`†-cancelˡ` (no unitarity needed), `miter` (`⟦ C₁ ⟧ ≋ ⟦ C₂ ⟧ ⇔ ⟦ C₁ ++ C₂ † ⟧ ≋ idPS`), `spec-miter` (a path-sum against a circuit, on columns); `equivalence-any`/`-syntactic`/`-decidable` for {H, S, CZ}. **`Adjoint/Gates.agda`**, **`Adjoint/Conjugate.agda`**: per-gate cancellation and conjugation facts shared by both gate sets, and `circuit-adjoint` (`amp ⟦ C † ⟧ x z ≐ conj (amp ⟦ C ⟧ z x)`, the adjoint proper). **`Compose/Apply.agda`**, **`Miter/Compose.agda`**: a circuit after any path-sum acts on its columns (`amp-∘ᴾ`); `spec-miter-∘` (`⟦ C ⟧ ≋ ξ ⇔ (⟦ C † ⟧ ∘ᴾ ξ) ≋ idPS`, section 3's miter as a composite), `miter-lemma-4-1`, `spec-miter-restriction`, `miter-∘`, `++-congˡ`/`++-cancelˡ`. **`Compose/Contraction.agda`**: `Contraction`, `WellFormed-∘ᶜ`, `WellFormed-∘-partial` (WellFormed survives any partial isometry composed after). **`CRK/Adjoint.agda`**, **`CRK/Conjugate.agda`**, **`CRK/Miter.agda`**, **`CRK/Miter/Compose.agda`**, **`CRK/Equivalence.agda`**: the same for {H, CNOT, R_k} (`_†` here is involutive on the nose, `†-involutive : C † † ≡ C`; `level-miter`), with `equivalence-gauss`/`-decidable-gauss` (Clifford equivalence by Gaussian elimination at the miter). **`CRK/Validation.agda`**: section 5.1's translation validation at any level — `validation-sound`, `validation-any`, `validation-refuted-interference` (lemma 4.2), `validation-normal-form`, `validation-clifford` (complete at level ≤ 2). **`CRK/Specification.agda`**: `spec-reduct`/`spec-sound`, a circuit against a specification through reducts of the composed miter. Closed instances of a *composed* miter do not compute (`bind` is opaque), so worked examples use the circuit miter `C₁ ++ C₂ †`.
- **`Linear.agda`**, **`CRK/`**: the paper's gate set. `Lin n m` (Z₂-linear forms), `liftᴸ`, `eval-liftᴸ`. `CRK/Circuit`: `Gate` (`H`, `CNOT` on distinct wires, `R k`, `R† k`), `State` (wires hold `Lin` forms), `⟦_⟧` (def. 2.9), `level`, `prop-2-14` (corrected to `2 ⊔ level C`; `prop-2-14-false-at-1` is the counterexample), `out-linear`. `CRK/Amp`, `CRK/Semantics`: prop. 2.10 (its own `applyᴬ`) and unit columns. `CRK/Compile`: circuits of level ≤ 2 compiled to {H, S, CZ} (`compile-≋`, corollary 4.4 by that route). `CRK/Theorems`: the package façade — its names clash with the root's, so the root imports it qualified.
- **`Polynomial/Bind.agda`**, **`Compose.agda`**, **`Compose/`**: composition. `bind` (simultaneous substitution, **opaque** — use `eval-bind`/`eval-rename`), `poly-ext`, `eval-liftᴮ`/`odd` (spelled exactly as Denotation's private `bit`, so `outBit` conversions are `refl`). `_∘ᴾ_` (def. 2.6: ξ′'s inputs fed the lifted outputs of ξ, path variables `y ++ᵃ y′`, normalisations added) and `_⊗ᴾ_`, both transparent. `Compose/Sum` (`++ᵃ`, `Σᴮ-++`, `Σᴮ-swap`, `Σᴮ-δ`), `Compose/Properties` (`eval-∘`, `outBit-∘`, `hits-∘`, `prop-2-7ʳ`, `applyᴾ`, `prop-2-7ᶜ`), `Compose/Laws` (identities, `∘ᴾ-assoc`, `∘ᴾ-cong`, up to ≋), `Compose/Matrix` (`prop-2-7` as `Σᴮ (λ w → amp ξ x w ⊛ amp ξ′ w z)`), `Compose/Gates` (`GateModel`, the compositional `⟦_⟧ᶜ`, `≋-amp`, `amp-ext`), `Compose/Clifford` and `Compose/CRK` (instances; `⟦++⟧`, `⟦⟧ᶜ≋⟦⟧`, def. 2.9's gates as printed), `Compose/WellFormed` (`Isometric-∘`, `WellFormed-∘` after an isometry), `Compose/Counterexample` (`WellFormed-∘-fails`, `PartialIsometric-∘-fails` — prop. 2.7's first half is false), `Compose/Tensor` (`amp-⊗`, `⊗ᴾ-cong`, `⊗-interchange`, `remark-2-8`), `Compose/Swap` (`swapᴾ`, `swap-natural`).
- **`Size.agda`**, **`Size/`**: corollary 2.15's size half. `Size/Sparse` (`Rep` = terms + one `Lin` form per output, `Represents`, `Small`, `size` in bits, `sparse`, `Deg≤⇔sparse`), `Size/Monomials` (`length-monomials≤ ≤ (n+m+1)^d`, exact binomial count), `Size/Submonomials`, `Size/Terms` (`liftTerms`: lemma 2.13's expansion truncated at degree k); `Size` (`repᴷ`, `corollary-2-15ᴷ`/`ᶜ`, `repᴷ-volume`, `volume-degenerate` — the literal "polynomial in n·|C|" fails at |C| = 0); `Size/Equivalence` (`repᴷ-≋`); `Size/Interpreter` (`interpᴷ` builds the representation gate by gate: `interpᴷ-correct`, `interpᴷ-length-poly`, `interpᴷ-trace-poly`, `interpᴷ-size`, `compactᴷ≡repᴷ`), `Interpreter/Clifford`, `Interpreter/Equivalence`; `Size/Example`, `Interpreter/Example` (the seven-T Toffoli at M₀ = 0). Data sizes only: no machine model, no running time.
- **`CRK/Controlled.agda`**, **`CRK/Trace.agda`**, **`QFT.agda`**, **`QFT/`**: section 5.2's QFT for every n. `CR k c t` (R_(k+1) c; R_(k+1) t; CNOT; R_(k+1)† t; CNOT) with `CR-≋` (the diagonal e^{2πi x_c x_t/2^k}, needs k + 1 ≤ M). `CRK/Trace`: `trace` (a circuit's phase and wires along one path), `run-trace`, `eval-⟦⟧`/`outBit-⟦⟧`/`amp-⟦⟧`, `trace-++`, `trace-map` (a circuit moved along a wire injection), `Σᴮ-opposite`. `QFT/Circuit` (`QFT₀`, `QFTC` = with reversal SWAPs), `QFT/Spec` (`QFTˢ n : PathSum n n n`, `bin`, `eval-QFTˢ`), `QFT` (`QFT-≋ : ∀ n → suc n ≤ M → ⟦ QFTC n ⟧ ≋ QFTˢ n`, `QFTC-matrix`), `QFT/Unitary` (`QFTˢ-Unitary⇔ : Unitary (QFTˢ n) ⇔ n ≤ M`, `χsum-rows`), `QFT/Relabel` (`QFT₀-≋` against the output-reversed spec, `QFT₀-not-spec`), `QFT/Count` (`cliffords-QFT₀ ≡ n * n`); `Examples/QFT` (table 2's sizes at their own precisions — their closed types are inferred, never written out: writing ⟦ QFTC 16 ⟧ ≋ … by hand unfolded the whole run for > 20 min).
- **`Gauss.agda`**, **`Gauss/Forms.agda`**, **`Gauss/Corollary.agda`**: corollary 4.4 by the paper's route for {H, CNOT, R_k}. `step` (pivot y_j on wire w, `y_j ← f_w ⊕ x_w ⊕ y_j`) keeps the diagonal (`step-diag`), the order (`step-Ord≤`) and solved wires; `gauss` is the elimination as a total function — `refuted x` (a vanishing diagonal entry, hence `refute`) or a `Reified` restriction (outputs `x`, `Internal`, same diagonal, `reified-≋`). `Gauss/Corollary`: `⟦_⟧ᴿ : Circuit n → Maybe …`, `not-id-gauss`, `lemma-4-1-gauss`, `lemma-4-3-gauss`, `corollary-4-4-gauss`/`-every-`/`-by-`, `decidable-gauss`. Pass `toPS {k = k}` / `ξᴿ r {k = k}` explicitly: no amplitude determines `k`.
- **`Reorder/Pair.agda`**, **`Full.agda`**, **`Full/`**: all of figure 2 at any variables. `front₂ i j`, the quarters `q₀₀ʸ`…`q₁₁ʸ` at any pair; `_⟶ᶠ_ = Anywhere (Anywhere _⟶ᴳ_)` with `elimAtᶠ`/`ωAtᶠ`/`hhAtᶠ`/`caseAtᶠ`, embeddings of the partial calculi, `⟶ᶠ-SN`, `⟶ᶠ*-bounded`, `⟶ᶠ*-length-lower`. `Full/Sound`: `⟶ᶠ-sound`. `Full/Canonical`: the quotients read off the phase (`ωQuot`, `hhQuot`, `caseX`/`caseQ`/`caseQ′`), complete whenever any witness exists. `Full/Match`: `headᴳ?`, `stepᶠ?`, `irreducibleᶠ?`, `normal-formᶠ`. `Full/Corollary`: verdicts along `⟶ᶠ` chains. `Full/Clifford`: every rule preserves `IsClifford` (Internal + order ≤ 2) — the gap in the paper's proof of corollary 4.4 — hence `corollary-4-4-normalᶠ` and `circuit-decidableᶠ`. Lemmas by recursion on a chain need a result type that does not mention the chain (`--cubical-compatible`); package the dependent part in a Σ.
- **`GlobalPhase.agda`**: `twist`, `Restriction-phase`, `lemma-4-1-phase`, `lemma-4-1-circuit-phase`, `corollary-4-4-phase` (a circuit is ζ^e·I iff its restriction reduces to `phasePS e` syntactically). **`Congruence.agda`**: `amp-≈`/`≈-≋` (syntactically congruent ⇒ ≋), `phasePS`, `renumbered-≋`, `reduces-to-id!`. **`Brute.agda`**: `≋?`, brute-force equivalence (not the paper's method). **`Ancilla.agda`**: `set0`, `_≋[_]₀_` (equivalence on the columns where a wire is |0⟩), `clean-ancilla`. **`Polynomial/Decidable.agda`**, **`Reduction/Decidable.agda`**, **`Reduction/General/Decidable.agda`**: deciders for every rule's premises and smart constructors (`hh!`, `hhᴳ!`, `caseᴳ!`, …) whose premises are `{True …}` implicits. **`Reduction/Derivation.agda`**: `Derivation` — the paper's columns of lines, each rule or rewrite checked — and `derivation-sound`.
- **`Examples.agda`**, **`Examples/`** (all at M₀ = 0; `Examples` re-exports them and the root imports it): `Base` (the shared `PathSum.Circuit 3` and `CRK` instances, circuits, literals, wrappers `circuit-id!`, `Implements`, `Literal₀`, `Clean₀`, …), `Section2` (examples 2.2, 2.12), `Section3` (§3.1), `AppendixB` (B.1: as printed it is the identity — `B1ᵖ-id`, `B1ᵖ-not-ω`; `SH³-ω`), `Clifford` (identities, refutations, `circuit-decidable` computed), `Toffoli` (3.3 by the paper's derivation; the seven-T circuit implements it), `ControlledT` (3.4 — the paper's circuit with an ancilla; the printed fourth line is a typo, `cT-line₄-typo`), `Adder` (B.2 and its circuit), `General` ([Case], (SH)³ with the general rules), `Validation` (translation validation on small miters: T;T ≡ S, T;H;H ≡ T, X ≢ I, CNOT ≢ I), `Brute` (cross-checks).
- **`Theorems.agda`**: instantiates `Clifford` at `Denotation.semantics` and states everything unconditionally — `prop-2-10`, `circuit-unit-columns`, `circuit-≋-id` (`⟦ C ⟧ ≋ idPS` iff the circuit's matrix is the identity), `lemma-4-1`, `lemma-4-1-circuit` (`⟦ C ⟧ ≋ idPS ⇔ ⟦ C ⟧ᴿ ≋ idPS`), `corollary-4-4-circuit` (about `⟦ C ⟧ᴿ`) and `corollary-4-4-⟦⟧` (about `⟦ C ⟧`: either a refutation, or a reduct with no path variables whose being the identity is exactly the circuit's), `reduct≋`, `circuit-id`/`circuit-not-id` (the verdict on a reduct, carried back to `⟦ C ⟧`), `id⇔syntactic`, **`corollary-4-4-any`** (for *any* chain `⟦ C ⟧ᴿ ⟶* ξ′` ending without path variables, `⟦ C ⟧ ≋ idPS` iff `ξ′` is syntactically the identity) and **`corollary-4-4-syntactic`** (`⟦ C ⟧ ≋ idPS` iff some such chain ends at a syntactic identity) — together the characterisation in the paper's proof of corollary 4.4; its polynomial-time claim is not formalised; and a decision procedure along that route: `id⇔′`, `decide-≋-id`, `circuit-decidable` and `matrix-decidable` (decidability as such is elementary, and the types do not record the route). Beside these, from the modules above: `lemma-2-5`/`lift-unique`, `circuit-Isometric`, the {H, CNOT, R_k} results under `-Rk` names (`prop-2-10-Rk`, `circuit-WellFormed-Rk`, `corollary-4-4-syntactic-Rk`) and `prop-2-14`/`-k`/`-deg`/`-false-at-1`, soundness and length/SN of the other calculi, `normal-form`, `lemma-4-1-partial`, `interferenceᴳ`/`interference-bool`/`lemma-4-2-as-stated-fails`, `lemma-4-3-at`, `corollary-4-4-anyᵍ`/`-anyᴳ`, `compile-≋`, `miter`, `spec-miter`, `equivalence-syntactic`/`-decidable`; and `⊛-comm`/`⊛-assoc`/`conj-⊛`/`ℤ[ζ]`, `prop-2-7`, `∘ᴾ-assoc`, `∘ᴾ-cong`, `WellFormed-∘-fails`, `PartialIsometric-∘-fails`, `Isometric-∘`, `WellFormed-∘`, `amp-⊗`, `⊗-interchange`, `swap-natural`, `circuit-Unitary`, `⟦++⟧`, `⟦⟧ᶜ≋⟦⟧-Rk`, `⟦++⟧-Rk`, `circuit-Unitary-Rk`, `⟶ᶠ-sound`/`⟶ᶠ*-sound`, `⟶ᶠ-SN`, `⟶ᶠ*-bounded`, `normal-formᶠ`, `WellFormed-strict`, `lemma-4-1-phase`, `corollary-4-4-anyᶠ`, `corollary-4-4-normalᶠ`, `circuit-decidableᶠ`, `not-id-gauss`, `corollary-4-4-gauss`, `decidable-gauss`, `corollary-4-4-phase`; and `WellFormed-∘-partial`, `circuit-adjoint`/`-Rk`, `spec-miter-∘`/`-Rk`, `spec-miter-restriction`, `miter-Rk`, `equivalence-gauss`, `equivalence-decidable-gauss`, `validation-sound`, `validation-refuted-interference`, `validation-clifford`, `spec-sound` (the {H, CNOT, R_k} ones through `KA.†`, since both `_†` are in scope); `corollary-2-15`, `corollary-2-15-volume-degenerate`, `corollary-2-15-≋`, `sparse-interpreter-correct`, `sparse-interpreter-length`; `CR-≋`, `QFT-spec-phase`, `QFT-≋`, `QFT-matrix`, `QFT-spec-Unitary⇔`, `QFT-without-permutation`, `QFT-cliffords` (ℤ's `_*_` is `_*ℤ_` there, ℕ's being in scope). It imports `PathSum.Examples` (closed facts at M₀ = 0, not restated) and reaches every module in the directory.

No theorem rests on an assumption: the hypotheses of the corollary are proved by `Circuit`, the step back from the restriction by lemma 4.1 (`Isometry` + `CircuitSemantics`), and the final test by `Decide` + `Syntactic`. What is *not* formalised: the polynomial time bounds (proposition 3.2, corollaries 2.15 and 4.4; polynomials are functions on all 2^(n+m) monomials); constant inputs beyond restricting to the columns where an ancilla is |0⟩ (`Ancilla`); a verdict-returning validation program for non-Clifford circuits (validation is stated as theorems, with no decider for lemma 4.2's pattern); the symmetric monoidal laws of remark 2.8 beyond interchange and SWAP naturality (associativity and the right unit of `⊗ᴾ` cannot be stated with the homogeneous `≋`); and the benchmarks of section 5 as runs of the tool (the QFT family is proved for every n, semantically).

**Denotation's public surface.** Its helpers are private; what other modules use is `Assign`, `amp`, `hits`, `_≋_` and its equivalence, `⟶-sound`, `interference-lemma`, `semantics`, `eval-true`/`eval-false`, `amp-idPS`, `hits-cong`, and from *Reading the outputs at a path* `outBit`, `hits-intro`/`hits-elim`, `hits-≗³`, `amp-≗`, `outBit-μ`, `eval-μ-val`, `eval-0ᴾ-val`. Private definitions still *reduce*: `outBit ξ x y w` is definitionally `not ⌊ (+ 2) ∣? eval (out ξ w) x y ⌋` even outside the module (`Syntactic.outBit-∣` relies on it). Adding to that file costs a ~5 min, ~10 GB recheck, so prototype against it, not in it.

**Pitfall (rot exponents).** Never let Agda compare `rot e A` with `rot e′ A` for syntactically different exponents — by `cong`/`subst`/`rewrite` inside the exponent, or by conversion. It unfolds the proof terms `Cyclotomic.classify` carries: one such step cost ~90 s and 3 GB. Change exponents only through `rot-exp` with both exponents written out. (`½ * [ true ]ᶻ` is not definitionally `½`: ℕ multiplication by 1 is stuck on the symbolic `H`.)

Departures from the paper (all in the module headers; the not-formalised list is in the paragraph above): a path-sum carries its normalisation `k` **and** its path-variable count `m` separately, because def. 2.1 ties them but fig. 2's rules do not; lemma 4.3's implicit side condition (the rule may cost more normalisation than the path-sum has) is *proved* here rather than assumed, via the coordinate facts above; the core corollary-4.4 development is over {H, S, CZ}, whose restriction is reified by construction (the paper's {H, CNOT, R_k} is `CRK/`, reaching the corollary by compilation and, as the paper does, by Gaussian elimination in `Gauss`); lemma 4.1 is proved under `WellFormed` (trace-form column norms), which definition 2.4 implies and which is strictly weaker; composition's laws and remark 2.8 hold up to ≋, not as equations (indices agree only propositionally, no funext); and three statements are false as printed and are proved corrected beside counterexamples — lemma 4.2 needs Q odd at some input (`lemma-4-2-as-stated-fails`), proposition 2.14's bound is max(2, k) (`prop-2-14-false-at-1`), and proposition 2.7's well-formedness claim fails for `WellFormed` and for definition 2.4 (`WellFormed-∘-fails`, `PartialIsometric-∘-fails`; it holds after an isometry). Smaller slips, each in its module header: def. 2.6 omits renaming ξ′'s path variables in the outputs (`Compose`); §4.1 substitutes `Q` for `y_i` where `x_i ⊕ Q` is meant (`Gauss`); example B.1 as printed is the identity, not ω·I (`Examples/AppendixB`); the fourth line of example 3.4 does not follow from the third (`Examples/ControlledT`). One gap is filled: the proof of corollary 4.4 reduces by arbitrary rules, which needs every rule — not only lemma 4.3's — to keep the order ≤ 2 (`Full/Clifford`).

**Pitfall (closed circuits).** A statement `⟦ C ⟧ ≋ ζ` about a *closed* circuit must use a single instance of `PathSum.Circuit 3` / `PathSum.CRK.Circuit 3` (take them from `Examples.Base`), and must not apply a general lemma (`≋-trans`, `≋-by-eval`, `renumbered-≋`) to the term `⟦ C ⟧` directly when the other side has a numeral normalisation: Agda unfolds `≋` into amplitudes to compare the types — 40–107 s per fact for (SH)³ and the seven-T Toffoli, and over 10 minutes (abandoned) for the 27-gate controlled-T. State such facts through a name whose arguments are the circuit (`circuit-id!`, `crk-renumbered-≋`, `Implements C ζ`, …); each then takes about a second.

**Pitfall (`with` over big goals).** Do not `with`-abstract when the goal mentions `≋` (or anything that unfolds to `amp`): Agda normalises the goal looking for the abstracted term. In `Decide` that cost 52 s and 8 GB; helpers taking a Boolean and its equation (`f : ∀ b → e ≡ b → Goal`, called `f e refl`) took 5 s and 0.6 GB. Likewise `hits`/`eval-cong`'s implicit assignments are not inferable through `hits` (a defined function) — pass them.

**Pitfall.** `_≋_` matches on both normalisations, so nothing can be recovered through it by unification: every statement mentioning it must be given its path-sums explicitly (`≋-trans {ξ = a} {ζ = b} {χ = d}`), including in the `Semantics` record fields.

## Key conventions

- `_===_` always means the raw relation (the axioms); `_≈_` always means the monoid congruence it generates (a congruence for `•`, closed under the monoid laws).
- `[_]ʷ` injects a generator into `Word`. `[_]ₗ`/`[_]ᵣ` are left/right embeddings in products.
- `(f ʷ)` extends `f : X → Word Y` to `Word X → Word Y` (postfix `_ʷ` = `wconcatmap`).
- `(h ᵗ)` extends a coset action `h : C → Y → Word X × C` to words, threading the coset.
- `nfp` (`NormalFormInjective`) and `nfp'` (`NormalForm`) are the standard names for normal-form witnesses.
- `by-equal-nf` proves `w ≈ v` from `nf w ≡ nf v`; `by-assoc` proves `w ≈ v` from `to-list w ≡ to-list v`; `by-passoc` re-brackets guided by pattern words built from `□`.
- Files follow the agda-stdlib style guide (see `style-guide.md`): 72-char banner headers with library line `-- Presentations of groups`, `{-# OPTIONS --safe #-}`, imports sorted with `using` lists, `private variable` blocks, sentence-case section separators.
- `MainTheorems.agda` is the style exemplar: it re-states the main theorems with definitions imported openly and proofs imported qualified, each section's banner naming the module the proof lives in. It is also the only façade — the per-development `Theorems.agda` layer was removed for the symmetric group, whose results MainTheorems now takes straight from `Presentation`, `UniqueNormalForm` and `SubPresentation/*`.

## Stdlib compatibility notes

- `Homomorphic₂` is imported from `Relation.Binary.Morphism.Definitions` (re-exported `Congruent`). That module is parameterised by `A B : Set`, so `A`/`B` are implicit at the call site — do **not** pass them explicitly.
- `IsMagmaHomomorphism` uses field `∙-homo` (renamed from `homo` in stdlib v3.0).
- `Data.Product.Relation.Binary.Pointwise.Dependent` is from stdlib master; it may not exist in older releases.
