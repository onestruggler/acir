# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## Project Overview

This is the Agda formalisation accompanying the paper *"A Complete and Natural Rule Set for Multi-Qudit Clifford Circuits in All Odd Prime Dimensions"*. It is being prepared for submission to the Agda standard library. Tested with **Agda 2.8.0 + stdlib 2.4**, which is what the tagged submission artifact is verified against (earlier versions of the library were reported to work with Agda 2.7 + stdlib 2.2).

## Typechecking

```bash
# Typecheck via WSL (Agda 2.8, resolves dependencies automatically).
# On this branch the root states only the qupit chain, so that is all it
# reaches: the symplectic development and its simplified rules, the
# ProjectivePauli factor, the semidirect construction that assembles
# them, and the Clifford rule sets on top (Simplified-V1 → Paper-V0 →
# Paper-V1, plus Shared/PauliBase).
wsl --exec /home/onest/.cabal/bin/agda MainTheorems.agda
```

Everything else in the tree is **not** reached from the root here, and needs typechecking separately if you touch it: the symmetric, trivial and cyclic groups, the wreath product, the Clifford+T and U₃(ℤ[½,i]) amalgamations, `ProjectiveClifford/Qupit/Simplified-V2`, `ProjectiveClifford/Qubit/` and `Clifford/Qupit/`.

Use WSL Agda 2.8 (`wsl --exec /home/onest/.cabal/bin/agda`) for all files. The WSL install uses its own stdlib at `/home/onest/.agda/lib/agda-stdlib/`. The `.agda-lib` file (`qupit.agda-lib`) includes `.` and depends on `standard-library`.

**Always re-typecheck `MainTheorems.agda` after any edit to library files.** From PowerShell, invoke WSL directly (Git-Bash mangles the Linux path).

## Architecture

The library is layered bottom-up:

### Layer 0 — Notations (`Notations.agda`)
Numeral patterns `₀`–`₉`, successor patterns `₁₊`/`₂₊`/`₃₊`/`₄₊` (overloaded over ℕ and Fin), and the `auto` pattern (= `Eq.refl`).

### Layer 1 — Free monoid (`Word/`)
- **`Word/Base.agda`**: the `Word X` type (free monoid over generators `X`): constructors `[_]ʷ`, `ε`, `_•_`; powers `_^_`/`_^'_`; `wmap`, `wconcat`, `wconcatmap` and its postfix notation `_ʷ` (so `(f ʷ)` extends `f : X → Word Y` to words); folds `wfoldr`/`wfoldl`; the stateful traversals `_ᵗ`/`_ᵗ'` that drive coset enumeration; conjugation helpers `_ʰ`/`_ⁿ`/`_ʰ'`/`_ⁿ'`; `WRel X = Rel (Word X) 0ℓ`.
- **`Word/Properties.agda`**: `wmap`/`wconcat` fusion laws, `lemma-ʷ-∘`, `lemma-fʷ-w^n`, `wfoldr-cong`/`wfoldl-cong`, and `≡-dec` (decidable equality of words).

### Layer 2 — Group presentations (`Presentation/`)
- **`Base.agda`**: parameterised by `Γ : WRel X`. `_===_` is the raw relation; `_≈_` the monoid congruence it generates (refl/sym/trans/cong/assoc/left-unit/right-unit/axiom); `refl'` lifts `_≡_`; combinators `cleft_`, `cright_`, `_reversed`; `Alphabet = X`.
- **`Properties.agda`**: `≈-isEquivalence`, `word-setoid`, magma/semigroup/monoid structures and bundles; the associativity solvers (`to-list`/`from-list`, `mod-assoc`, `by-assoc`, `by-assoc-and`, and the pattern-guided `Pattern-Assoc.by-passoc`); word-power lemmas (`lemma-^-+`, `lemma-^^`, `word-comm`, …); `wfoldr`/`wfoldl` congruence lemmas.
- **`Definitions.agda`**: `_IsPresentationOf_` (group), `_IsMonoidPresentationOf_`, and `module SubPresentation` (`Soundness` & `Completeness` of a semantics ⟦_⟧ : Syn → Sem, i.e. ⟦_⟧ is a setoid embedding).
- **`GroupLike.agda`**: `Grouplike` (every generator has a left inverse) and `Group-Lemmas` (`_⁻¹`, cancellation, uniqueness of inverses, the group `•-ε-group`).
- **`Morphism.agda`**: parameterised by presentations `Γ`, `Δ`. Builders turning generator-level data into `IsMonoidHomomorphism`/`Monomorphism`/`Isomorphism` and the group versions, for both `(f ʷ)` and `wmap f`.
- **`Independence.agda`**: independence of axioms. A `Select Γ` is a predicate on axiom instances and `Γ ∣ P` the sub-relation of the selected ones; `Derivable Γ P a` / `Independent Γ P a` say whether an instance `a : Γ w v` is (not) in the congruence the selection generates; `≈-mono`/`≈-∣` embed that congruence into larger ones. `Model Γ P mon ⟦_⟧₀` reads words into a monoid (via `StarInterp.Extend`) and `Model.Sound sound-ax` turns per-axiom soundness of the selected instances into `sound` for the congruence and `independent : ¬ (⟦ w ⟧ ≈ ⟦ v ⟧) → Independent Γ P a`. An axiom is shown independent by a model in which the others hold and it fails.

### Layer 3 — Constructions (`Presentation/Construct/`)
- **`Base.agda`**: amalgamated product `_⊕_` and related combinators on `WRel`.
- **`Properties/DirectProduct.agda`**, **`SemiDirectProduct.agda`**, **`NDirectProduct.agda`**, **`SugarProduct.agda`**: lift normal-form witnesses through the constructions. `SemiDirectProduct` takes a **word-valued** action `conj : H → N → Word N` (the relation `ConjRelʷ conj`); an earlier element-valued version, `conj : H → N → N`, was deleted once nothing imported it.
- **`Properties/Amalgamation.agda`**: amalgamated free product with coset normal form (`AmalDataNF`, `ANF`).

### Layer — Circuits (`Circuit/`)
- **`Base.agda`**: parameterised by `Gate : ℕ → Set`. Wire-indexed generators `Gen`, `Circuit n = Word (Gen n)`, shifts `_↑`/`_↥ᵏ_`, and `Lift-Relation` extending any gate relation with the **four** structural rules `cong↑`, `comm₁`/`comm₂` and `ω↑=ω`. `Lift-Relation` also proves `lemma-cong↑` and `comm-gate₁-w↑`/`comm-gate₂-w↑↑`, the comm rules extended from a single generator to a whole circuit. There is **no `comm₀` rule**: a scalar's centrality is the theorem `Central-Scalars.comm₀` (with `comm-gate₀-w`), derived from the other rules given the one hypothesis `scalars-comm` that scalars commute with one another — for a gate set with several 0-ary gates that is the gate-specific relation's job to state; `Single-Scalar (one : ∀ h h' → h ≡ h')` discharges it when `Gate 0` is a subsingleton. Clients with one scalar write `open LR.Central-Scalars (λ { ω-gate ω-gate → PB.refl }) public using (comm₀ ; comm-gate₀-w)` and use `comm₀ h g` where they used to write `PB.axiom (comm₀ h g)`; a function by cases on `_VRel,_===_` no longer owes a `comm₀` clause.
- **`Independence.agda`**: the four structural rules are independent of one another. `Structural SRel` names them (`data Rule`, constructors overloaded with those of `_VRel,_===_`), `Uses r a` says whether rule `r` occurs in an axiom instance (through `cong↑`'s premise), `Without r` is the selection of the others, and `a IndependentOf r` / `a DerivableWithout r` are `Presentation.Independence`'s notions at that selection; `Lift` lifts a restricted congruence one wire up. `Soundness P P-cong↑ mon ⟦_⟧₀` is the engine for an arbitrary gate relation: a monoid per width, `Rules` takes soundness of each rule guarded by the selection and gives `sound`/`independent`, and `Shift σ σ-hom ⟦↥⟧` discharges the `cong↑` obligation from a shift homomorphism. `Pure` instantiates the empty gate relation and proves, over any `Gate`, `cong↑-independent₀/₁/₂`, `comm₁-independent`, `comm₂-independent`, `ω↑=ω-independent` (models: `2 ^ depth` in (ℕ, +); depths of one-/two-wire gates in `List ℕ`; depths beyond the reach of the bottom-stated rules) and `scalars-comm-needed`: two distinct scalars need not commute in the full pure theory (the scalars in order, in `List (Gate 0)`), so `Central-Scalars`' hypothesis is not redundant. `MainTheorems` indexes these under `Structural-Theorems`.

### Layer — Normalization (`Normalization/`)
- **`NormalForm/Setoid.agda`**: setoid-valued normal-form witnesses on the stdlib `Function.Bundles` — `NormalFormInjective` = `Injection`, `BijectiveNormalForm` = `Bijection`, `NormalForm` = `RightInverse` (maps `word-setoid ⟶ₛ NF`) — plus `WeakNormalForm`. Uniqueness is **not** here.
- **`NormalForm/Uniqueness.agda`** (and `Uniqueness/Propositional.agda`, the same at the discrete setoid): `UniqueNormalForm`, `by-normalization` (soundness + unique NF ⇒ completeness) and the converse `by-completeness` (completeness + exact section `nf ∘ inv-nf ≗ id` ⇒ unique NF). `UniqueNormalForm` is indexed by the **section** `inv-nf`, not by a whole `NormalForm` record — so a client that already has the record passes `SNF.NormalForm.inv-nf nfp`, and `by-normalization` needs the record given explicitly (`{nfp}`), nothing else determining it. A second copy indexed by the record used to live in `Setoid.agda`; it is gone.
- **`NormalForm/Propositional.agda`**: `Normalization.NormalForm.Setoid Γ (setoid B)` re-exported for a plain carrier set `B` (an explicit module parameter) — the witnesses land in `≡` on `B` and all derivations are inherited. Types read `NormalForm Γ B` / `NormalFormInjective Γ B`. Because the re-export fixes the codomain to `setoid B`, the witnesses are function-aliases here, not record names: **opening or projecting a witness value goes through `Normalization.NormalForm.Setoid` directly (imported `as SNF`)** — e.g. `open SNF.NormalForm nfp renaming (…)` — while `Propositional` is used only for the `Γ B` types and record construction.
- **`Reidemeister-Schreier.agda`**: the injectivity/surjectivity engine. `Star-Injective-Simplified` proves `(f ʷ)` injective given a left inverse on generators; `Star-Injective-Full` (and its setoid variant) does coset enumeration and provides the Schreier section, right/left normal forms.
- **`CosetNF.agda`**: coset normal forms via Reidemeister–Schreier: `lemma-ᵗ-act` (letters-to-words action law), `module SingleLevel` (one level; its `Transfer.Unique` derives `nf' ∘ gg ≡ id` for the transported NF from base exactness + coset exactness of the table on sections, via cancellation and R–S injectivity), `CosetTable` / `PackedCosetTable` (coset tables with a distinguished identity coset), `CosetTower` (iterate up an ℕ-indexed family).

### Layer 4 — Specific groups (`Examples/Groups/`)
- **`Cyclic/`**: ℤ/nℤ. `Syntactics` (the relation `_Cn,_===_` over a one-element alphabet), `Presentation`, `Normalization`, `Theorems`.
- **`Trivial/`**: the trivial group, two presentations proved isomorphic. Everything is derived once from the single hypothesis that every generator is `≈ ε` (`gen≈ε`), then instantiated twice, so the directory is a **generic layer plus two instances**.
  - Generic, at the root, each parameterised by `(Γ, gen≈ε)`: `Normalization` (`w≈ε`, `nf`/`nfp`/`nfp'`), `Interpretation` (`⟦_⟧₀`, `GS`, `fʷ-cong-ax`, `grouplike`), `UniqueNormalForm` (`unfp`). `Semantics` is the shared target (`Terminal.group`, as `gp`) and takes no parameters.
  - **`Presentation1/`** is `⟨ ⊥ ∣ ⟩` (`EmptyRel`), **`Presentation2/ A`** is `⟨ A ∣ w = ε ⟩` (`TrivialRel`). Each has the same five modules: `Syntactics` supplies `pres` and `gen≈ε` (the only real content); `Normalization`, `Interpretation`, `UniqueNormalForm` just instantiate their generic namesakes; `Presentation` assembles `subpresentation`/`presentation`. Clients wanting the trivial group as a base case take `nfp`/`nfp'`/`gp`/`presentation` off `Presentation1.Presentation` (`NDirectProduct` does, at width 0).
  - `Presentation-Equivalence B` gives the monoid isomorphism between the two — a syntactic statement that never touches the semantics.
  - Note `gp` is re-exported, not abstracted over the module parameter, so it is `Presentation2.Presentation.gp`, never `.gp A`; `presentation` does take `A`.
- **`Symmetric/`**, **`Symplectic/`**, **`Clifford/`**, **`ProjectiveClifford/`**, **`Pauli/`**, **`ProjectivePauli/`**: see Layer 5.

There is no longer a `Presentation/Groups/`: it held a second Sₙ and a hand-rolled ℤ/4ℤ ≀ Sₙ, both over an inductive alphabet rather than circuit generators, and both are gone. The wreath product now comes from `Examples/Construct/SemiDirectProduct/SnD.agda`, which supplies `pres`, `nfp` and `nfp'` over the `Examples/Groups/Symmetric` alphabet.

### Layer 5 — Examples (`Examples/`)
- **`Groups/Symmetric/`**: completeness of the circuit presentation of Sₙ. There is no `Theorems.agda` façade — `MainTheorems` takes the five results (presentation, unique normal form for each semantics, soundness, completeness) straight from the modules that prove them. The layout follows the two chains: the **permutation** one is at the top level (`Semantics`, `Interpretation`, `Soundness`, `UniqueNormalForm`, `Surjectivity`, `Presentation`) and reaches a full presentation — `Semantics` is the target group alone and mentions no syntax, `Interpretation` is `⟦_⟧`/`⟦_⟧ᵍ`/`⟦↑⟧`, and `Soundness` is that `⟦_⟧` respects the relations; the **endofunction** one is `SubPresentation/` (`Semantics`, `Interpretation`, `UniqueNormalForm`, `SubPres`) and stops at a setoid embedding, since endofunctions are not all denotations. `Presentation` splits like Symplectic's: `subpresentation` needs only normalization, and `Surjectivity` is what promotes it. Shared support: `Syntactics`, `Cosets`, `Normalization`. `Completeness.agda` and `IndexedAction.agda` typecheck but nothing imports them.
- **`Groups/ProjectiveClifford/Qupit/`**: the paper's own subject. For an odd prime p, the qupit Clifford circuits read **modulo scalars** present `Pauli n ⋊ Sp(2n, ℤ/pℤ)`. Four rule sets over the same alphabet (Symplectic's `H`, `S`, `CZ`) — Simplified-V1, Simplified-V2, Paper-V0, Paper-V1 — each shown to present that group by transport from the one below it, over a base (`SemiDirect/`, on a different alphabet) that does the real work. Every layer above the base is an isomorphism plus a composition, which is why the stack is cheap to extend and why no rule set re-proves what another already has.
  - **`SemiDirect/`** is the base and defines `Pauli⋊Sp`. `Presentation` feeds the generic `SemiDirectProduct` machinery its two factors — `Symplectic.Simplified` for the symplectic part, `ProjectivePauli` for the Pauli part — together with `ConjAction`'s two well-definedness proofs for the conjugation action. Every input is a theorem, so it is unconditional.
  - **`Simplified-V1/`** is where the mathematics lives: the Pauli calculus (`Lemmas`, `LemmasXZ`, `LemmasCZ`, `SemiM`), the Ex-conjugation rules (`ExRules`), `Forward`, `Soundness`, a rewriting tactic layer (`Tactics`), and `Iso` onto the semidirect relation.
  - **`Paper-V0/`** is Figure 1 with the multiplier spelled over `R = S • Z^½`, as `RHR x x⁻¹`; 16 group-specific rules. Its `Iso` proves it isomorphic to Simplified-V1 and exports **`v1⇒pap`**, which transports Simplified-V1 theorems into its theory — which is why its `Lemmas` derives Selinger's c10–c15, the lower-wire rules and the swap calculus natively.
  - **`Paper-V1/`** is the same rules with the multiplier spelled over `S`, as `SHS' x⁻¹ x`, and 15 rules rather than 16: `(S • H) ^ 3 = ε` is a theorem here, not an axiom, because under that spelling `M₁` *is* that word and `M-power` at `k = 0` already gives it. `Iso` is the identity on words onto Paper-V0. `Lemmas/` carries only what that needs (`OneWire`, `GroupLike`, `XZ`) — the Ex-conjugation and three-wire layers it inherited from Paper-V0 were removed, since the isomorphism transports all of it.
  - **`Shared/PauliBase.agda`** is the relation-agnostic Pauli calculus, instantiated at whichever rule set needs it. `Calculus` rests on the axioms both spellings share; `BridgeCalc` adds `X • Z ≈ Z • X` as a *hypothesis* — the one fact whose proof genuinely differs between them — and builds the bridge between the R- and S-spellings of the multiplier, plus `MulZ`/`SemiMR` for carrying a Pauli across it.
  - **`Simplified-V2/`** transports the V1 theorem once more along an identity isomorphism. Nothing imports it.
- **`Groups/ProjectiveClifford/Qubit/`**: the qubit analogue (`CliffordExtension`, `Cocycle`, `ExtensionPresentation`, a `Selinger/` subtree). Self-contained — nothing outside the directory imports it.
- **`Groups/Real-Clifford+CH/`**: Clément's *A Complete Equational Theory for Real-Clifford+CH Quantum Circuits* (arXiv:2602.06644) — the gate set {H, Z, CZ, CH}, its Figure 4 with the swap rules of Remark 1, matrix semantics over ℤ[1/√2], soundness in full, completeness on 0, 1 and 2 qubits up to the one theorem the paper imports (Theorem 4.4, [17]), and completeness on n ≥ 3 qubits from the paper's Section 8 — its generators, rules, encoding and decoding all defined, the encoding proved to preserve the semantics at every width, and its three remaining results (Theorem 4.10, Lemmas 8.7 and 8.8) as hypotheses. Self-contained: nothing outside the directory imports it, and it is not reached from the root — typecheck `Examples/Groups/Real-Clifford+CH/Completeness.agda` (about ten minutes from scratch, almost all of it the relator checks and the box matrices, products of 8 × 8 integer tries decided by `refl`; run Agda under `ulimit -v 8000000` and a `timeout`), `Auxiliary/Soundness.agda`, `Auxiliary/Checks8.agda` (Figure 8 decided on three qubits: its `Base` holds the decision procedure and the Boolean checks, `Checks8/H01`…`H67` evaluate the Hadamard rules two first-indices per module — Agda frees the memory of such evaluations only between modules, and one module with all eight exhausts 10 GB — and `Checks8` the rest; about fifteen minutes in all) and `Auxiliary/DecodingChecks.agda` (Definition 8.3 decided on three qubits), which nothing imports.
  - `Syntactics`: the gates over `Circuit.Base`, the shortcuts of the paper (`°CZ`, `Ex`, `CZ₂₀`, `PP`, `CCZX`/`CCXZ`, the multi-controlled box `Λ□` of Definition 2.4), the raw relation `_SRel,_===_` (Figure 4 plus the swap rules), its lift `_VRel,_===_` through `Lift-Relation`, and `n ⊢ w ≈ v` for the congruence. Wire 0 is the paper's bottom wire; a two-wire gate sits on wires 0 and 1 with the control of CH on wire 1; bit vectors list wire 0 first.
  - `Semantics/`: `Ring` (ℤ[√2] as a `data` of two integers, ring laws by the ℤ ring solver, `√2^_`, `strict`), `Algebra` (operators `Op n = Bits n → Bits n → 𝔽` under pointwise `_≐_`; stored tries `Mat k` with `strict` entries, `mulM`/`tenM`/`idM`, `mat-dec`), `Local` (`emb` a k-wire matrix on the bottom wires, `up`/`upk` shifts, the tensor calculus), `Scaled` (`Scaled n = ℕ × Op n` read as (1/√2)^ℓ · M, the localisation relation `_~_`, `Scaled-monoid`). `Semantics.agda` re-exports these and gives the four gate matrices, each √2 times the gate.
  - `Interpretation`: `⟦_⟧ᵍ`, `len` and `⟦_⟧ₒ` by direct recursion on words, `⟦ w ⟧ = (len w , ⟦ w ⟧ₒ)`; `Ext.⟦_⟧ᴱ` is the `StarInterp` reading with `⟦⟧ᴱ-def : Ext.⟦_⟧ᴱ n w ≡ ⟦ w ⟧` and the transports `⟦⟧ᴱ-~`/`~-⟦⟧ᴱ`; the stored reading `⟦_⟧M`; `localise`, `by-matrix` and `by-relator`, which turn one equation of tries at the width a relator is drawn on into the axiom at every width.
  - `Soundness/` — `Relators` (the relators named, `Same` identities by `refl`, and the word equations `pNˡ/pNʳ : rN ↓ᵏ n ≡ …` also by `refl`), `Operators` (diagonal, permutation and controlled operators, `ctrl-⊙`), `BoxMatrices` (`abstract` stored 8 × 8 matrices and the two `refl` identities of the box's recursive step), `Box` (`Λ□-sem`: the box is (1/√2)^ℓ · a diagonal sign; `box-Z-sound`, the schema (19)) — and `Soundness` (Proposition 3.1: `axiom-sound`, `sound`, `axiom-soundᴱ`).
  - `OneQubit` (Lemma 7.1: a sixteen-state automaton normal form, uniqueness decided on stored matrices, `MonoidSem.GetSubPresentation`), `Auxiliary/Syntactics` (Figure 7 over `−1[_]`, `X[_,_]`, `H[_,_]` — the name is spelled with the Unicode minus, `-1` being a literal) and `Auxiliary/Soundness` (Figure 7 is sound for the 4 × 4 matrices, every rule at every choice of distinct indices by `refl` on tries: it pins the transcription and the written-order reading of auxiliary words), `BackAndForth` (the generic encode/decode completeness argument, by `Star-Injective-Simplified`), `TwoQubit` (Definitions 7.2 and 7.3, `e-sem` by `refl`, `by-triesY`, and `Lemma-7-6` parameterised by Theorem 4.4 and Lemmas 7.4/7.5), `Completeness` (Theorem 8.9: widths 0 and 1 outright, width 2 from `TwoQubit` and `TwoQubit/Decoding`, width ≥ 3 from `Section8.Complete` with the encoding's semantics proved and the three Section 8 results as module parameters; `subpresentation` at every width). No postulates.
  - `TwoQubit/` is Appendices B and C, proved: `Conjugation` (the `Tools` module — `front`/`back`/`mid`, the cancellation helpers, `conj-sym`/`conj-comm`/`conj-invol` — one-wire involutions, `comm-↓↑` for gates on disjoint wires, the swap moving a one-wire gate across, and `module Conj c c²`: conjugation `⟪ w ⟫ = c • w • c` by an involution, with `⟪⟫-•ₖ` distributing over k factors, `⟪⟫-≈` transporting an equation, `⟪⟫-fix`, `⟪⟫-invol`, `⟪⟫-sandwich`; instantiated as `N↑` (X on the upper wire), `N↓` (X on the lower wire) and `S` (the swap), with the action of each on every shortcut — `N↑-°CZ`, `S-°CH`, …), `Auxiliary` (Figure 12, Equations (81)–(110), each family derived once and transported: `eq84`, …, `eq110`, plus the intermediate forms named by content, `CH-°CZ`, `CX-alt : CX ≈ CH • CZ • CH`, `CH-Z↑`, …; (109) and (110) go through the one-wire facts `□-HZ`/`HZ-□` about the box Z • Z°), `Decoding` (Lemma 7.4 on the six generators; Lemma 7.5 on every rule of Figure 7 at N = 4 — the conjugation tables `N↑-dH`/`N↓-dH`/`S-dH`/`…-dZ` say how each conjugation renames a decoded generator, `cX-Z`/`cX-H`/`cX-H′` are Table 1 organised as: conjugating by D(X_[b,c]) is conjugating by CX between two renamings (`via-N↑`/`via-S`/`via-N↓`/`via-E`), and (d3*) is (110) after commuting °CH past Z° on its control). Stated at width `₂₊ n` (Decoding at 2) so the equations are available on the bottom two wires of any circuit.
  - Section 8, on n = m + 3 qubits, all `--safe`: `Auxiliary/Gray` (Definition 8.1's reflected Gray code, `code n`/`index n` mutually inverse — the width is explicit everywhere because `2 ^ n` is not invertible by unification), `Auxiliary/Bitstrings` and `BitstringsLemmas` (wire operations `lookupℕ`/`insertℕ`/`removeℕ` with their commutation lemmas under bounds, `allBits`, the Boolean membership `_∈ᵇ_` — level 6, so parenthesise it under `∧`/`∨` — and `Nodup`), `Auxiliary/Matrices` (the operators of the letters on 2ⁿ basis vectors: `negOp`, `swapOp`, `hadOp`, `⟦_⟧ᴳ`), `Auxiliary/P` (the alphabet P of Definition 4.9 — `−1−1`, `−1X`, `XX`, `HH`, with irrelevant distinctness proofs — and `⟦_⟧ᴾ`), `Auxiliary/Figure8` (rules r20–r46, side conditions as hypotheses, the paper's standing convention that an X or H letter has distinct indices made explicit), `Encoding` (Definition 8.2 through the deciding letter wrappers `zz`/`zx`/`xx`/`hh`, the basis vectors `str₁`/`str₂` of a context, `E-H`/`E-Z`/`E-CZ`/`E-CH` and `e`; Appendix A.4.1's `E-swap`, `E-X`, `ℰ` — the negation of a control, X where the bit is 1: the paper prints ℰ^{1−x}, but Checks8 decides (36)/(37) and they hold only this way round — and `W₁`/`W₂`; Definition E.1's `Σ`/`Σ′`; the H-pattern `hpat`; `view`, a generator as a gate on a wire), `MultiControlled` (Definition 2.4 by layouts: `Λ□`/`ΛZX`/`ΛXZ`/`ΛH` placed by the swap networks `shiftDown`/`shiftUp` and negated by X on the white controls), `Decoding` (Definition 8.3: `dZZ`, `dZX`, `dXX`, `dHH`, `dP`, and `d = rev ∘ dPʷ`), `Reverse` (`rev-cong`: reversal is the syntactic inverse for self-inverse generators), `Section8` (the statements `Theorem-4-10`, `E-sem`, `Lemma-8-7`, `Lemma-8-8` per width and `Complete`, an instance of `BackAndForth`). Conventions: our operator reading of a P-word is the paper's written-order matrix product (its `∘` on P-words is concatenation), the paper's circuit composition `A ∘ B` is our word `B • A`, bit vectors list wire 0 first and the paper's top-first string `x β y` is `y ++ β ∷ x`.
  - `EncodingSemantics/` proves `Section8.E-sem` at every width (`EncodingSemantics.e-sem`): `Auxiliary/WireForms` writes each gate's operator on bitstrings (`hadWire`, `chWire`, `zsgn`, `czsgn`, `form`, `gen-form`); `Products` reads a product of letters (`∏-ℓ`, `∏-M` — pass the word and `{m = m}` explicitly, the width is otherwise unsolvable) and evaluates diagonal products over an enumeration (`∏ᶠ-one`, `∏ᶠ-unique`); `Strings` reads a context and pairing bit back off a basis vector (`ctx₁`/`pr₁`/`str-ctx₁`, `ctx₂`/…); `Diagonal` does Z and CZ (sign letters, one context carries the sign at a vector); `Hadamard` does H and CH by an invariant `part p cov f` over the contexts processed so far (`Pair.step`: one two-level Hadamard on an uncovered pair; `Encode.sem`: any gate whose letters are Hadamards on pairs, instantiated twice). `Auxiliary/Checks8` decides on three qubits that `e` preserves semantics and that every rule of Figure 8 is sound at every admissible index, and `Auxiliary/DecodingChecks` that `d` preserves the semantics of every letter — the checks that pin the transcription of Definitions 8.2, 8.3 and A.4.1 (they found the missing distinctness conventions of (30), (34), (42) and the negation parity of W₁/W₂).
  - **Performance discipline.** Operators are functions, so Agda decides whether two are equal by applying them to arguments and normalising; for the product of forty gate matrices that is a sum over every intermediate index, exponential in the word, and Agda does it whenever two readings of a long word are not *syntactically* identical — which happens for reasons invisible in the source (an implicit width elaborated as `3` in one place and `₃₊ 0` in another). So `⟦_⟧ₒ` is **`abstract`**: two readings `⟦ w ⟧ₒ`/`⟦ w′ ⟧ₒ` are then compared by comparing the words, which is linear. Its defining equations are the lemmas `⟦⟧ₒ-gen`/`⟦⟧ₒ-ε`/`⟦⟧ₒ-•` (and `•₃`/`•₄`), and `localise`/`⟦⟧-ix`/`up-word`/`⟦⟧ᴱ-def` live in the same abstract blocks; proofs that need `⟦ w • v ⟧ = ⟦ w ⟧ ∙ ⟦ v ⟧` on the nose (a homomorphism law: `sound`, `BackAndForth`) use the StarInterp reading `Ext.⟦_⟧ᴱ` and transport with `⟦⟧ᴱ-~`/`~-⟦⟧ᴱ`. Three further rules: the power `√2^_` is evaluated strictly level by level (`Ring`), because the lazy `√2 ^ n` is a tree of size 2ⁿ to the conversion checker and `√2^ 27` alone exhausts memory — closed scalars are then safe to evaluate; every lemma about `_~_` gets its words explicitly (`_~_` unfolds to a scaled equation in which `√2^ ?ℓ` is not a unification pattern, so such implicits are never solved), and closed three-wire words have one spelling (`BoxMatrices.CCZX₀` etc.) so that stored-matrix identities are compared syntactically rather than by re-evaluating the tries. Stored matrices are `abstract`; the actual checks are `refl` on tries.
- **`Groups/CNOT-Dihedral/`**: Amy, Chen and Ross, *A finite presentation of CNOT-dihedral operators* (arXiv:1701.00140) — the gate set {ω, X, T, CNOT} with SWAP as the symmetry, the thirteen relations of its Figure 1, the phase-polynomial semantics, soundness, the derivable rules its existence proof rests on, and completeness assembled with the paper's imported and reordering lemmas as hypotheses. Self-contained and not reached from the root — typecheck `Examples/Groups/CNOT-Dihedral/Completeness.agda` (under a minute; the relation checks are on tables of at most sixteen entries; the one-qubit uniqueness decision needs more than 4 GB, so run under `ulimit -v 7000000`).
  - `Syntactics`: `Gate` has ω (0-ary), X, T (1-ary), CNOT, SWAP (2-ary); U and V (Definition 3.2) are words; gates on non-adjacent wires (`CNOT₂₀`, `U₀₂`, `U₁₃`, `U₀₃`, `V₀₂₃`, `V₀₁₃`) are the gate on the top-most wires conjugated by the swaps that carry its lower wires up, the paper's convention. `_SRel,_===_` is R₁ … R₁₃ plus the laws of the symmetry (an involution, the braid relation, naturality for X, T and CNOT — ω's is the theorem `comm₀`, opened from `Central-Scalars`); `Lift-Relation` adds the structural rules; `n ⊢ w ≈ v` is the congruence; `grouplike`. Wire 0 is the bottom wire; CNOT's control is wire 1, its target wire 0.
  - `Semantics/Z8` (ℤ₈ as `Fin 8`, laws by exhaustion) and `Semantics`: an operator is `Op n = Bits n → Bits n × ℤ₈`, x ↦ (f x , p x) — the phase polynomial representation; composition adds phases along the way, so there are no sums and a relation check is a `refl` on a table (`Mat k`, `matOf`, `ix`, `mat-dec`); `emb`/`up`/`split` as in Real-Clifford+CH; `Op-monoid`.
  - `Interpretation` (abstract `⟦_⟧` with `⟦⟧-gen`/`⟦⟧-ε`/`⟦⟧-•`, `Ext.⟦_⟧ᴱ` and `⟦⟧ᴱ-def`, `⟦_⟧M`, `localise`, `by-relator`), `Soundness/Relators` and `Soundness` (Proposition 3.7: `axiom-sound`, `soundᴱ`, `sound`).
  - `Derived`: the 24 rules of Figure 2 and the 4 of Lemma 5.2, each an equational derivation from the relations (`Tools` bundles the congruence, `front`/`back`/`mid` rewriting and the associativity solvers). Two lemma families do the work: naturality of the symmetry for whole circuits (`nat₁`, `nat₂`, `nat₃`, by induction from the generator laws), which yields every swap rule; and the dihedral group of the three CNOTs on three wires (`braid-CNOT`, `CNOT↑-CNOT₂₀`, …: R₆ makes `CNOT₂₀` the commutator of `CNOT` and `CNOT ↑`, which has order 2, so it is central), which lets a CNOT pass a U or V it shares a wire with (`CNOT-U↑`, `CNOT-V`, `CNOT-V↑`).
  - `OneQubit`: completeness on one wire outright (normal forms ω^k T^a X^e, an automaton respecting the relations by decision, uniqueness decided on the 128 tables, `MonoidSem.GetSubPresentation`).
  - `Completeness`: a `NormalForms n` record naming the paper's lemmas as hypotheses (Lafont's affine normal forms, Lemmas 5.1/5.3, 6.2/6.3), the separation step of Proposition 6.4 proved (D · A = D′ · A′ as operators forces A = A′ then D = D′), and the theorem and `subpresentation` assembled from them. No postulates.
- **`Amalgamations/CliffordT1.agda`**: the qubit Clifford+T gate set as an amalgamated product, ending in a monoid isomorphism.
- **`Amalgamations/QutritCliffordT1.agda`**: the qutrit Clifford+T analogue.
- **`Amalgamations/U33Di.agda`**: U₃(ℤ[½,i]) presented as a two-level amalgamated product.

## Key conventions

- `_===_` always means the raw relation (the axioms); `_≈_` always means the monoid congruence it generates (a congruence for `•`, closed under the monoid laws).
- `[_]ʷ` injects a generator into `Word`. `[_]ₗ`/`[_]ᵣ` are left/right embeddings in products.
- `(f ʷ)` extends `f : X → Word Y` to `Word X → Word Y` (postfix `_ʷ` = `wconcatmap`).
- `(h ᵗ)` extends a coset action `h : C → Y → Word X × C` to words, threading the coset.
- `nfp` (`NormalFormInjective`) and `nfp'` (`NormalForm`) are the standard names for normal-form witnesses.
- `by-equal-nf` proves `w ≈ v` from `nf w ≡ nf v`; `by-assoc` proves `w ≈ v` from `to-list w ≡ to-list v`; `by-passoc` re-brackets guided by pattern words built from `□`.
- Files follow the agda-stdlib style guide (see `style-guide.md`): 72-char banner headers with library line `-- Presentations of groups`, `{-# OPTIONS --safe #-}`, imports sorted with `using` lists, `private variable` blocks, sentence-case section separators.
- `MainTheorems.agda` is the style exemplar: it re-states the main theorems with definitions imported openly and proofs imported qualified, each section's banner naming the module the proof lives in. It is also the only façade — the per-development `Theorems.agda` layer was removed for the symmetric group, whose results MainTheorems now takes straight from `Presentation`, `UniqueNormalForm` and `SubPresentation/*`.

## Stdlib compatibility notes

- `Homomorphic₂` is imported from `Relation.Binary.Morphism.Definitions` (re-exported `Congruent`). That module is parameterised by `A B : Set`, so `A`/`B` are implicit at the call site — do **not** pass them explicitly.
- `IsMagmaHomomorphism` uses field `∙-homo` (renamed from `homo` in stdlib v3.0).
- `Data.Product.Relation.Binary.Pointwise.Dependent` is from stdlib master; it may not exist in older releases.
