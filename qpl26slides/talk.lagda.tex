% =====================================================================
%  A Complete and Natural Rule Set for Multi-Qudit Clifford Circuits
%  in All Odd Prime Dimensions  --  QPL 2026 slides
%
%  Literate Agda + Beamer (LuaLaTeX).  Every Agda code block in these
%  slides is typechecked when the slides are built.
%
%  Build:   make          (runs: agda --latex  then  latexmk -lualatex)
%  Output:  latex/talk.pdf
% =====================================================================
\documentclass[aspectratio=169]{beamer}

% ---------------------------------------------------------------------
% 1. Fonts.  Agda code is full of Unicode, so LuaLaTeX + fonts with
%    the glyphs, plus a fallback chain for the stragglers (⋊, ⟦, ↥ ...).
% ---------------------------------------------------------------------
\usepackage{fontspec}
\usepackage{unicode-math}

\directlua{luaotfload.add_fallback
  ("agdafallback",
    { "DejaVuSans:style=Regular;"
    , "FreeSerif:style=Regular;"
    }
  )}

\setmainfont{DejaVu Serif}[Scale=0.85]
\setsansfont{DejaVu Sans}[Scale=0.85]
\setmonofont{DejaVu Sans Mono}[Scale=0.85, RawFeature={fallback=agdafallback}]
\setmathfont{Latin Modern Math}

% ---------------------------------------------------------------------
% 2. Agda support (after the font setup).
% ---------------------------------------------------------------------
\usepackage{agda}

\newfontfamily{\AgdaCodeFont}{DejaVu Sans Mono}[Scale=0.62, RawFeature={fallback=agdafallback}]
\renewcommand{\AgdaFontStyle}       [1]{{\AgdaCodeFont #1}}
\renewcommand{\AgdaKeywordFontStyle}[1]{{\AgdaCodeFont #1}}
\renewcommand{\AgdaStringFontStyle} [1]{{\AgdaCodeFont #1}}
\renewcommand{\AgdaCommentFontStyle}[1]{{\AgdaCodeFont #1}}
\renewcommand{\AgdaBoundFontStyle}  [1]{\textit{\AgdaCodeFont #1}}

\setlength{\mathindent}{0.5em}
\renewcommand{\AgdaEmptySkip}{0.25\baselineskip}

% ---------------------------------------------------------------------
% 3. Circuit figures (generated by Cir2Tikz; self-contained tikzit subset)
% ---------------------------------------------------------------------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{backgrounds,arrows,shapes,shapes.geometric,shapes.misc,cd}
\pgfdeclarelayer{edgelayer}
\pgfdeclarelayer{nodelayer}
\pgfsetlayers{background,edgelayer,nodelayer,main}
\pgfkeys{/tikz/tikzit fill/.initial=0}
\pgfkeys{/tikz/tikzit draw/.initial=0}
\pgfkeys{/tikz/tikzit shape/.initial=0}
\pgfkeys{/tikz/tikzit category/.initial=0}
\tikzstyle{none}=[inner sep=0mm]
\tikzstyle{tikzfig}=[baseline=-0.25em,scale=0.38]
\newcommand{\tikzfig}[1]{{\tikzstyle{every picture}=[tikzfig]\input{#1.tikz}}}
\definecolor{normGreen}{RGB}{214, 243, 221}
\definecolor{normRed}{RGB}{249, 196, 196}
\definecolor{normYellow}{RGB}{253,246,208}
\definecolor{zxgreen}{RGB}{216,248,216}
\definecolor{zxred}{RGB}{221,165,165}
\definecolor{zxhad}{RGB}{255,255,0}
\definecolor{zxpaulibox}{RGB}{138,159,198}
\input{circuits.tikzstyles}

% Shrink a circuit to a given width iff it overflows, and vcenter it.
\newlength{\figwidth}
\AtBeginDocument{\setlength{\figwidth}{0.9\textwidth}}
\newcommand{\ket}[1]{|#1\rangle}
\newsavebox{\figbox}
\newcommand{\fitfig}[2][\figwidth]{%
  \sbox{\figbox}{\tikzfig{figures/#2}}%
  \ifdim\wd\figbox>#1
    \sbox{\figbox}{\resizebox{#1}{!}{\usebox{\figbox}}}%
  \fi
  $\vcenter{\hbox{\usebox{\figbox}}}$}

% ---------------------------------------------------------------------
% 4. Beamer theme.
% ---------------------------------------------------------------------
\usetheme{metropolis}
\setbeamertemplate{navigation symbols}{}
\setbeamerfont{title}{size=\large}

\title{A Complete and Natural Rule Set for Multi-Qudit Clifford Circuits
       in All Odd Prime Dimensions}
\author{Xiaoning Bian \and Sarah Meng Li \and Neil J.\ Ross \and
        John van de Wetering \and Yuming Zhao}
\institute{Tsinghua University \and University of Amsterdam, QuSoft \and
           Dalhousie University \and University of Copenhagen}
\date{QPL 2026}

\begin{document}

% ---------------------------------------------------------------------
% Imports needed by the module header.  Real Agda, not shown.
% ---------------------------------------------------------------------
\begin{code}[hide]
{-# OPTIONS --cubical-compatible --safe #-}

open import Data.Fin using (toℕ)
open import Data.Nat using (ℕ ; suc)
open import Data.Nat.Primality using (Prime)
open import Data.Product using (_,_ ; ∃)
open import Relation.Binary.PropositionalEquality using (_≡_)
open import Notations
open import ForStdlib.Data.Fin.Mod
open import ForStdlib.Data.Fin.Mod.Prime.Fermat
\end{code}

\maketitle

\begin{frame}{This talk}
  \begin{itemize}
    \item A \alert{presentation by generators and relations} of the
          $n$-qudit Clifford group, for every odd prime dimension $p$:
          16 relations, none involving more than 3 qudits.
    \item A \alert{unique normal form} for symplectic
          (= Clifford modulo Pauli) circuits.
    \item The computational content is \alert{formalised in Agda}.
  \end{itemize}
  \vfill
  \begin{block}{Two disclaimers, one per author group}
    \begin{itemize}
      \item Every Agda code block in these slides is \emph{real}: the slides
            are one literate Agda file, typechecked against the development
            each time they are built.
      \item These slides were written by Xiaoning Bian \emph{and Claude}:
            a $\sim$200-line outline prompt in, this deck out.
    \end{itemize}
  \end{block}
\end{frame}

\section{Introduction}

\begin{frame}{From qubit to qudit Clifford gates}
  Fix an odd prime $p$ and let $\omega = e^{2\pi i/p}$.
  The Clifford group $\mathcal{C}_n$ is generated by:
  \vfill
  \begin{center}
  \begin{tabular}{lll}
    \toprule
      & qubit ($p = 2$) & qudit (odd prime $p$) \\
    \midrule
    $H$  & $\frac{1}{\sqrt2}\begin{pmatrix}1&1\\1&-1\end{pmatrix}$
         & $\ket{j} \mapsto \tfrac{1}{\sqrt p}\sum_k \omega^{jk}\ket{k}$ \\[2ex]
    $S$  & $\begin{pmatrix}1&0\\0&i\end{pmatrix}$
         & $\ket{j} \mapsto \omega^{j(j-1)/2}\,\ket{j}$ \\[2ex]
    $CZ$ & $\mathrm{diag}(1,1,1,-1)$
         & $\ket{j}\ket{l} \mapsto \omega^{jl}\,\ket{j}\ket{l}$ \\
    \bottomrule
  \end{tabular}
  \end{center}
  \vfill
  Note the $/2$ in the qudit $S$: it is the inverse of $2$ in
  $\mathbb{Z}_p$, so odd $p$ really is a different (and more uniform) world.
\end{frame}

\begin{frame}{The setting: one Agda module}
  These slides are a single Agda module, parameterised by the prime $p$
  and by a \alert{primitive root} $g$ of $\mathbb{Z}_p^*$:

\begin{code}
module qpl26slides.talk
  (p-3 : ℕ)
  (let p-2 = ₁₊ p-3)
  (p-prime : Prime (suc (₁₊ p-2)))
  (let open PrimeModulus' p-2 p-prime)
  (g*@(g , g≠0) : ℤ* ₚ)
  (g-gen : ∀ ((x , _) : ℤ* ₚ) → ∃ \ (k : ℤ ₚ-₁) → x ≡ g ^′ toℕ k)
  where
\end{code}

  \vfill
  \begin{itemize}
    \item $p = \texttt{p-3} + 3$: every odd prime, uniformly.
    \item Everything downstream --- rule sets, normal forms, theorems ---
          is parameterised by the choice of $g$.
  \end{itemize}
\end{frame}

% ---------------------------------------------------------------------
% The real imports: the whole library, instantiated at (p, g).
% ---------------------------------------------------------------------
\begin{code}[hide]
open Primitive-Root-Modp' g* g-gen

open import Level using (Level ; _⊔_)
open import Data.Unit using (⊤ ; tt)
open import Data.Sum using (_⊎_ ; inj₁ ; inj₂)
open import Data.Product using (Σ-syntax ; _×_)
open import Data.Vec using (Vec)
open import Relation.Binary.PropositionalEquality using (_≢_)
open import Algebra.Bundles using (Group)
open import Algebra.Morphism.Structures using (module GroupMorphisms)
open import Word.Base using (Word ; WRel ; [_]ʷ ; ε ; _•_ ; _^_)
open import Presentation.Definitions using (_IsPresentationOf_)
open import Presentation.GroupLike using (Grouplike ; module Group-Lemmas)
open import Presentation.Construct.Base using (_⋄_⋄_ ; ConjRelʷ)
import Presentation.Base as PB

-- The symplectic development.
import Examples.Groups.Symplectic.Syntactics as SymSyntactics
module NSym = SymSyntactics p-2 p-prime
module Sym = NSym.Symplectic
import Examples.Groups.Symplectic.Semantics as SymSemantics
module SymSem = SymSemantics p-2 p-prime
open SymSem using (Sp-group)
import Examples.Groups.Symplectic.Normalization.Section as NSection
module Sect = NSection p-2 p-prime
import Examples.Groups.Symplectic.Normalization.Uniqueness as NUniq
module U = NUniq p-2 p-prime
import Examples.Groups.Symplectic.Simplified.Syntactics as SimSyntactics
module NSim = SimSyntactics p-2 p-prime g* g-gen
module Sim = NSim.Simplified-Relations
import Examples.Groups.Symplectic.Simplified.Lemmas as SimLemmas
module SimL = SimLemmas p-2 p-prime g* g-gen
import Examples.Groups.Symplectic.Simplified.Presentation as SimPresentation
module SimPres = SimPresentation p-2 p-prime g* g-gen

-- The Pauli development.
import Examples.Groups.ProjectivePauli.Presentation-Alt as PauliAlt
module XZ = PauliAlt p-2 p-prime
import Examples.Groups.ProjectivePauli.Semantics as PauliSemantics
module PSem = PauliSemantics p-2 p-prime
open PSem using (+ₚ-group)

-- The projective Clifford developments.
import Examples.Groups.ProjectiveClifford.Qupit.Paper-V0.Syntactics
  p-3 p-prime g* g-gen as PapSyn
import Examples.Groups.ProjectiveClifford.Qupit.Paper-V0.Presentation
  p-3 p-prime g* g-gen as PapPres
import Examples.Groups.ProjectiveClifford.Qupit.SemiDirect.Presentation
  p-3 p-prime g* g-gen as SDPres

-- The exact (with-scalars) Clifford development.
import Examples.Groups.Clifford.Qupit.Syntactics
  p-3 p-prime g* g-gen as QS
import Examples.Groups.Clifford.Qupit.Semantics p-3 p-prime as CSem
module PE = CSem.Presented-Extension g* g-gen
import Examples.Groups.Clifford.Qupit.Presentation
  p-3 p-prime g* g-gen as CQP
\end{code}

\begin{frame}{Circuits are words}
  A gate set is a family of gate types indexed by arity; a circuit on
  $n$ wires is a \alert{word} over wire-indexed generators.

\begin{code}[hide]
module Circuits-are-words where
\end{code}
\begin{code}
  data SympGate : ℕ → Set where
    H-gate   : SympGate 1
    S-gate   : SympGate 1
    CZ-gate  : SympGate 2
\end{code}

\begin{code}[hide]
module Circuit-example where
  open Sym using (Circuit ; H ; S ; CZ ; _↑)
\end{code}
\begin{code}
  example : Circuit 2
  example = CZ • H ↑ • S
\end{code}

  \begin{itemize}
    \item \AgdaFunction{Circuit}\ \AgdaBound{n}
          $=$ \AgdaDatatype{Word}\ (\AgdaDatatype{Gen}\ \AgdaBound{n}):
          free monoid; \AgdaFunction{\_↑} shifts a circuit up one wire.
    \item diagrams read in matrix-multiplication order:\quad
          \AgdaFunction{example} $=$ \fitfig[3cm]{ex-word}
  \end{itemize}
\end{frame}

\begin{frame}{Derived gates: the rest of the usual gate set}
  $X$, $Z$, the swap $\mathit{Ex}$, and the two controlled-$X$ gates are
  \emph{words}, not extra generators:

\begin{code}[hide]
module Derived-gates where
  open Sym using (Gen ; S ; S⁻¹ ; H ; CZ ; _↑ ; _↓)
\end{code}
\begin{code}
  Z X : ∀ {n} → Word (Gen (₁₊ n))
  Z = H • H • S • H • H • S⁻¹
  X = H • S • H • H • S⁻¹ • H

  Ex CX XC : ∀ {n} → Word (Gen (₂₊ n))
  Ex = CZ • H ↓ • H ↑ • CZ • H ↓ • H ↑ • CZ • H ↓ • H ↑
  CX = H ↓ ^ 3 • CZ • H ↓
  XC = H ↑ ^ 3 • CZ • H ↑
\end{code}
  \vfill
  \begin{center}
    \fitfig[0.3\textwidth]{def-CX}\qquad
    \fitfig[0.55\textwidth]{def-Ex}
  \end{center}
\end{frame}

\section{Main results}

\begin{frame}{Main result 1: a presentation of the qudit Clifford group}
  The paper's Figure~1 rule set, mod scalars, \alert{presents} the
  projective Clifford group
  $\mathcal{C}_n/\langle\omega\rangle \;\cong\;
   \mathrm{Pauli}_n \rtimes \mathrm{Sp}(2n, \mathbb{Z}_p)$:

\begin{code}[hide]
module Main-result-1 where
  open PapSyn.Clifford-Relations renaming (_QRel,_===_ to _CRel,_===_)
  open SDPres.Semidirect using (Pauli⋊Sp)
\end{code}
\begin{code}
  Theorem-1 : ∀ (n : ℕ) → (n CRel,_===_) IsPresentationOf (Pauli⋊Sp n)
  Theorem-1 = PapPres.presentation
\end{code}
  \vfill
  \begin{itemize}
    \item \AgdaDatatype{\_CRel,\_===\_} is the 16-relation rule set,
          shown on the next slides.
    \item The theorem holds \emph{at every width $n$}, for \emph{every}
          odd prime $p$ and every primitive root $g$ --- the whole deck
          is parameterised by them.
  \end{itemize}
\end{frame}

\begin{frame}{The 16 relations, in Agda (one qudit)}
\begin{code}[hide]
module The-sixteen-relations where
  open PapSyn using (Gen ; S ; S⁻¹ ; H ; CZ ; Ex ; CX ; CZ02 ; S^ ; CZ^ ; _↑ ; _↓)
  open PapSyn.Clifford-Relations using (M₋₁ ; XM ; XMg ; XMg^ ; R ; R^)
  private variable n : ℕ
  infix 4 _CRel,_===_
\end{code}
\begin{code}
  data _CRel,_===_ : (n : ℕ) → WRel (Gen n) where
    order-S      :        (₁₊ n) CRel,  S ^ p === ε
    order-H      :        (₁₊ n) CRel,  H ^ 2 === M₋₁
    M-power      : ∀ k →  (₁₊ n) CRel,  XMg^ k === XM (g^ k)
    semi-MR      :        (₁₊ n) CRel,  XMg • R^ (g * g) === R • XMg
    order-SH     :        (₁₊ n) CRel,  (S • H) ^ 3 === ε
    comm-HHSHHS  :        (₁₊ n) CRel,  H • H • S • H • H • S === S • H • H • S • H • H
\end{code}
  \vfill
  \begin{itemize}
    \item $M_x$ is the multiplier $\ket{j} \mapsto \ket{xj}$, spelled as the
          word $R^x\, H\, R^{x^{-1}} H\, R^x\, H$ with $R = S \cdot Z^{1/2}$
          (and $\mathit{XM}_x = M_{x^{-1}}$).
    \item One-qudit relations talk about the powers of a \alert{fixed
          primitive root} $g$ --- this is where odd primes differ from
          the qubit case.
  \end{itemize}
\end{frame}

\begin{frame}{The 16 relations, in Agda (two qudits)}
\begin{code}
    order-CZ     :        (₂₊ n) CRel,  CZ ^ p === ε
    order-Ex     :        (₂₊ n) CRel,  Ex ^ 2 === ε
    comm-CZ-S↑   :        (₂₊ n) CRel,  CZ • S ↑ === S ↑ • CZ
    semi-M↑CZ    :        (₂₊ n) CRel,  XMg ↑ • CZ^ g === CZ • XMg ↑
    semi-Ex-S↑   :        (₂₊ n) CRel,  Ex • S ↑ === S ↓ • Ex
    semi-Ex-H↑   :        (₂₊ n) CRel,  Ex • H ↑ === H ↓ • Ex
    blake-c12    :        (₂₊ n) CRel,  (S ^ p-1) ↑ • (S ^ p-1) ↓ • CX ^ p-1 • S ↓ • CX === CZ
\end{code}
  \vfill
  \begin{itemize}
    \item \AgdaInductiveConstructor{blake-c12} converts between the two
          entangling gates $CZ$ and $CX$.
  \end{itemize}
\end{frame}

\begin{frame}{The 16 relations, in Agda (three qudits)}
\begin{code}
    yang-baxter  :        (₃₊ n) CRel,  Ex ↑ • Ex ↓ • Ex ↑ === Ex ↓ • Ex ↑ • Ex ↓
    cz-slide     :        (₃₊ n) CRel,  Ex ↓ • Ex ↑ • CZ === CZ ↑ • Ex ↓ • Ex ↑
    semi-CX↑-CZ↓ :        (₃₊ n) CRel,  CZ ↓ • CX ↑ === CZ02 • CX ↑ • CZ ↓
\end{code}
  \vfill
  \begin{itemize}
    \item Only \alert{these 16} are axioms.  $XZ = ZX\,(\text{mod } \omega)$,
          $\mathit{Ex}$-vs-$CZ$, and both Pauli-vs-$CZ$ rules of the paper's
          Figure~1 are \emph{derived} --- each deletion is itself
          Agda-checked.
    \item Plus, for every gate set, the same three structural rules:
          congruence under $\uparrow$, and gates on disjoint wires commute.
  \end{itemize}
\end{frame}

\begin{frame}{The 16 relations, as circuits (1--2 qudits)}
  \setlength{\figwidth}{0.33\textwidth}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}\tiny
      \begin{tabular}{@{}r@{\;\,}l@{}}
        order-S & \fitfig{pv0-order-S}\\[1pt]
        order-H & \fitfig{pv0-order-H}\\[1pt]
        M-power & \fitfig{pv0-M-power}\\[1pt]
        semi-MR & \fitfig{pv0-semi-MR}\\[1pt]
        order-SH & \fitfig{pv0-order-SH}\\[1pt]
        comm-HHSHHS & \fitfig{pv0-comm-HHSHHS}\\
      \end{tabular}
    \end{column}
    \begin{column}{0.5\textwidth}\tiny
      \begin{tabular}{@{}r@{\;\,}l@{}}
        order-CZ & \fitfig{pv0-order-CZ}\\[1pt]
        order-Ex & \fitfig{pv0-order-Ex}\\[1pt]
        comm-CZ-S↑ & \fitfig{pv0-comm-CZ-Sup}\\[1pt]
        semi-M↑CZ & \fitfig{pv0-semi-Mup-CZ}\\[1pt]
        semi-Ex-S↑ & \fitfig{pv0-semi-Ex-Sup}\\[1pt]
        semi-Ex-H↑ & \fitfig{pv0-semi-Ex-Hup}\\
      \end{tabular}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{The 16 relations, as circuits (2--3 qudits)}
  \begin{center}\scriptsize
    \begin{tabular}{@{}r@{\;\;}l@{}}
      blake-c12 & \fitfig[0.42\textwidth]{pv0-blake-c12}\\[2pt]
      yang-baxter & \fitfig[0.38\textwidth]{pv0-yang-baxter}\\[2pt]
      cz-slide & \fitfig[0.38\textwidth]{pv0-cz-slide}\\[2pt]
      semi-CX↑-CZ↓ & \fitfig[0.38\textwidth]{pv0-semi-CXup-CZdn}\\
    \end{tabular}
  \end{center}
\end{frame}

\begin{frame}{What ``presents'' means}
  \begin{code}[hide]
module What-presents-means {a ℓ : Level} {X : Set} where
\end{code}
\begin{code}
  record _Presents_ (_===_ : WRel X) (G : Group a ℓ) : Set (a ⊔ ℓ) where
    field gl : Grouplike _===_
    module GL = Group-Lemmas _===_ gl
    open GroupMorphisms (Group.rawGroup GL.•-ε-group) (Group.rawGroup G)
    field
      ⟦_⟧  : Word X → Group.Carrier G
      iso  : IsGroupIsomorphism ⟦_⟧
\end{code}
  \vfill
  \begin{center}
  \begin{tikzpicture}[scale=1.0]
    \node (a) at (0,0) {$\mathsf{Cir}\;n \,/\, \approx_{\text{16 rules}}$};
    \node (b) at (7,0) {$\mathrm{Pauli}_n \rtimes \mathrm{Sp}(2n,\mathbb{Z}_p)
                         \;\cong\; \mathcal{C}_n/\langle\omega\rangle$};
    \draw[->, thick] (a) -- node[above]{{\AgdaCodeFont ⟦\_⟧}}
                          node[below]{$\cong$} (b);
  \end{tikzpicture}
  \end{center}
  \begin{itemize}
    \item \emph{Soundness}: {\AgdaCodeFont ⟦\_⟧} is well defined ---
          each rule holds semantically.
    \item \emph{Completeness}: {\AgdaCodeFont ⟦\_⟧} is injective ---
          semantically equal circuits are related by the rules.
  \end{itemize}
\end{frame}

\begin{frame}{Main result 2: a unique normal form for symplectic operators}
  Symplectic operators are Clifford mod Pauli: the ``proper'' part.
  Normal forms are \alert{data}, built from four atomic boxes:

\begin{code}[hide]
module Normal-form-boxes where
\end{code}
\begin{code}
  A : Set
  A = Σ[ ab ∈ (ℤ ₚ × ℤ ₚ) ] (ab ≢ (₀ , ₀))
  B D : Set
  B = ℤ ₚ × ℤ ₚ
  D = ℤ ₚ × ℤ ₚ
  E : Set
  E = ℤ ₚ
\end{code}
  \vfill
  \setlength{\figwidth}{0.32\textwidth}
  \begin{center}\tiny
    \begin{tabular}{@{}r@{\;\,}l@{\quad}r@{\;\,}l@{}}
      $A_{a,b}$, $a\neq0$ & \fitfig{box-A} & $D_{a,b}$, $a\neq0$ & \fitfig{box-D}\\[1pt]
      $B_{a,b}$, $a\neq0$ & \fitfig{box-B} & $E_{b}$ & \fitfig{box-E}\\
    \end{tabular}
  \end{center}
\end{frame}

\begin{frame}{Rows, and the staircase}
\begin{code}
  M L′ ML′ ML : ℕ → Set
  M 0 = ⊤
  M (₁₊ n) = Vec D n × E
  L′ 0 = ⊤
  L′ (₁₊ n) = Vec B n × A
  ML′ 0 = ⊤
  ML′ n@(₁₊ _) = M n × L′ n
  ML 0 = ⊤
  ML 1 = ML′ 1
  ML (₂₊ n) = ML′ (₂₊ n) ⊎ D × ML (₁₊ n)

  NF : ℕ → Set
  NF 0 = ⊤
  NF (₁₊ n) = NF n × ML (₁₊ n)
\end{code}
  \vfill
  A double induction: \AgdaFunction{NF} is a staircase of
  \AgdaFunction{ML} boxes, and each \AgdaFunction{ML} box is itself
  defined by recursion on the width.
\end{frame}

\begin{frame}{The normal form, as a circuit}
  Reading normal forms back as circuits is structural recursion:

\begin{code}[hide]
module NF-as-circuits where
  open Sect using (NF ; [_]ᵐˡ)
  open Sym using (Gen ; _↑)
\end{code}
\begin{code}
  [_]ⁿᶠ : ∀ {n} → NF n → Word (Gen n)
  [_]ⁿᶠ {0}     tt         = ε
  [_]ⁿᶠ {₁₊ n}  (nf , ml)  = [ nf ]ⁿᶠ ↑ • [ ml ]ᵐˡ
\end{code}
  \vfill
  \begin{center}
    \fitfig[0.3\textwidth]{nf4}\qquad
    \fitfig[0.55\textwidth]{ml-tower}
  \end{center}
\end{frame}

\begin{frame}{Everything on these slides is Agda-checked}
  \begin{itemize}
    \item $\approx 320$ files, $\approx 100\,000$ lines of Agda,
          all \texttt{--safe}, \alert{no postulates}.
    \item Agda 2.8, standard library 2.3.
    \item One root (\texttt{MainTheorems.agda}) reaches the whole library;
          these slides import the library and \emph{re-state} the main
          theorems --- if a statement on a slide did not match the
          formalisation, the slides would not build.
  \end{itemize}
  \vfill
  \begin{block}{What the formalisation covers}
    All group-theoretic computation: presentations, normal forms,
    completeness.  The two glue facts --- that the projective Clifford
    group is $\mathrm{Pauli} \rtimes \mathrm{Sp}$, and that the Clifford
    group is a central extension of it by
    $\langle\omega\rangle$ --- are classical and are \emph{not} formalised.
  \end{block}
\end{frame}

\section{Method}

\begin{frame}{Divide and conquer}
  Factor the group; present the factors; compose the presentations.
  \vfill
  \begin{center}
  \begin{tikzpicture}[scale=0.9]
    \node at (0,1.5) {$1 \to \mathrm{Pauli}_n \to
      \mathcal{C}_n/\langle\omega\rangle \to \mathrm{Sp}(2n,\mathbb{Z}_p)
      \to 1$};
    \node[anchor=west] at (7.2,1.5) {\small semidirect: it splits};
    \node at (0,0.4) {$1 \to \langle\omega\rangle \to
      \mathcal{C}_n \to \mathcal{C}_n/\langle\omega\rangle \to 1$};
    \node[anchor=west] at (7.2,0.4) {\small central extension};
  \end{tikzpicture}
  \end{center}
  \vfill
  \begin{itemize}
    \item Doesn't this just move the difficulty around?  \alert{No}: the
          symplectic factor is where the real rewriting happens, and
          keeping the Pauli part out of it pays off everywhere.
    \item Prior work (qubit: Selinger; qutrit) worked with the whole
          Clifford group at once --- the derivations drag a ``messy''
          Pauli correction through every step.
  \end{itemize}
\end{frame}

\begin{frame}{Warm-up: the symmetric group $S_n$}
  Swap circuits on $n$ wires present $S_n$.  All of $S_3$:
  \vfill
  \begin{center}
  \begin{tabular}{cccccc}
    \fitfig[0.11\textwidth]{s3-e} & \fitfig[0.11\textwidth]{s3-s0} &
    \fitfig[0.11\textwidth]{s3-s1} & \fitfig[0.13\textwidth]{s3-s01} &
    \fitfig[0.13\textwidth]{s3-s10} & \fitfig[0.15\textwidth]{s3-s010}\\
    $e$ & $\sigma_0$ & $\sigma_1$ & $\sigma_0\sigma_1$ &
    $\sigma_1\sigma_0$ & $\sigma_0\sigma_1\sigma_0$
  \end{tabular}
  \end{center}
  \vfill
  Circuits have a built-in \alert{tower of subgroups}
  \[
    \mathsf{Cir}\,0 \;<\; \mathsf{Cir}\,1 \;<\; \cdots \;<\; \mathsf{Cir}\,n
  \]
  ($\mathsf{Cir}\,k$ = circuits that only touch the top $k$ wires).
  For $S_n$: $S_0 < S_1 < \cdots < S_n$.
\end{frame}

\begin{frame}{Coset representatives, and the normal form}
  Representatives of $S_3/S_2$ (and of $S_2/S_1$): the staircases
  \vfill
  \begin{center}
  \begin{tabular}{ccc@{\qquad\qquad}cc}
    \fitfig[0.1\textwidth]{c2-0} & \fitfig[0.1\textwidth]{c2-1} &
    \fitfig[0.12\textwidth]{c2-2} &
    \fitfig[0.08\textwidth]{c1-0} & \fitfig[0.1\textwidth]{c1-1}\\
    $c^{(2)}_0$ & $c^{(2)}_1$ & $c^{(2)}_2$ & $c^{(1)}_0$ & $c^{(1)}_1$
  \end{tabular}
  \end{center}
  \vfill
  \begin{columns}[T]
    \begin{column}{0.62\textwidth}
      \begin{itemize}\small
        \item For $f \in S_3$ there is a unique $c \in C_2$ with
              $f^{-1}c$ fixing the bottom wire: $c$ must send
              $f^{-1}(0)$ to $0$.
        \item Recurse on $f^{-1}c \in S_2$: every $f$ is uniquely
              $c^{(2)} \cdot (c^{(1)}\!\uparrow)$.
        \item Group theory: given $G_0 < \dots < G_n$ and coset reps
              $C_k$ of $G_k/G_{k-1}$, elements of $G_n$ factor uniquely
              as $\prod_k c_k$ --- a \alert{mixed-radix encoding}:
              $|S_3| = 3 \cdot 2 \cdot 1$.
      \end{itemize}
    \end{column}
    \begin{column}{0.34\textwidth}
      \begin{center}\fitfig[0.9\textwidth]{sn-stair}\end{center}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Rewriting to the normal form: push a generator through}
  Multiply a normal form by a generator; push it through the staircase:
  \setlength{\figwidth}{0.34\textwidth}
  \begin{center}\small
  \begin{tabular}{r@{\qquad}l}
    \fitfig{push-commute} & disjoint wires: commute past \\
    \fitfig{push-pass}    & Yang--Baxter: pass through, shift up \\
    \fitfig{push-absorb}  & $\sigma^2 = e$: staircase shrinks \\
    \fitfig{push-extend}  & staircase grows \\
  \end{tabular}
  \end{center}
  Each case is a \emph{relation}; collecting them all gives a rewrite
  system that normalises every circuit.
\end{frame}

\begin{frame}{From $S_n$ to $\mathrm{Sp}(2n,\mathbb{Z}_p)$: change the action}
  The same framework, three ways of looking at the wires:
  \vfill
  \begin{enumerate}
    \item $S_n$ acts on $\{0, \dots, n-1\}$: a wire is a \emph{position};
          $c$ sends $f^{-1}(0)$ to $0$.
    \item $S_n$ acts on $\mathsf{Vec}\;\mathsf{Bit}\;n$: a wire is a
          \emph{bit}; $f^{-1}c$ fixes the linear subspace of the bottom wire.
    \item Adding $H, S, CZ$: the group acts on
          $\mathsf{Vec}\;\mathsf{Pauli}_1\;n$ --- a $2n$-dimensional
          \alert{symplectic} $\mathbb{Z}_p$-vector space.  A wire is a
          $\mathrm{Pauli}_1$; enlarging the coset layer from staircases to
          $\mathit{ML}$ boxes, $f^{-1}c$ fixes the symplectic subspace
          of the bottom wire.
  \end{enumerate}
  \vfill
  The framework applies to \emph{any} $\mathbb{N}$-indexed family of
  groups with such a layered structure
  (\texttt{Circuit.CosetNF}, \texttt{Circuit.Uniqueness}).
\end{frame}

\begin{frame}{Uniqueness, in Agda}
\begin{code}[hide]
module Uniqueness-of-NF where
  open Sect using (NF ; [_])
  open SymSem using (_≈ˢ_)
  open SymSem.Interpretation using (⟦_⟧)
\end{code}
\begin{code}
  Theorem-2 : ∀ n {u v : NF n} → ⟦ [ u ] ⟧ ≈ˢ ⟦ [ v ] ⟧ → u ≡ v
  Theorem-2 = U.⟦[]⟧-injective
\end{code}
  \vfill
  \begin{center}
  \begin{tikzpicture}[scale=1.1]
    \node (cir) at (0,2)   {$\mathsf{Cir}\,n\,/\,{\approx}$};
    \node (sp)  at (5.5,2) {$\mathrm{Sp}(2n,\mathbb{Z}_p)$};
    \node (nf)  at (0,0)   {$\mathit{NF}\;n$};
    \draw[->, thick] (cir) -- node[above]{{\AgdaCodeFont ⟦\_⟧}} (sp);
    \draw[->, thick, bend left=15] (cir) to node[left]{$\mathit{nf}$} (nf);
    \draw[->, thick, bend left=15] (nf) to node[right]{$[\_]$} (cir);
    \draw[->, thick, dashed] (nf) -- node[below right]{injective} (sp);
  \end{tikzpicture}
  \end{center}
  \vfill
  Soundness $+$ unique normal forms $\Rightarrow$ completeness
  (\texttt{by-normalization}); the interesting content is that the
  \emph{coset factor is determined by the action} on Pauli states.
\end{frame}

\begin{frame}{From 66 relations to 18}
  \begin{enumerate}
    \item Rewrite \emph{every} circuit to normal form; collect every
          relation the rewrite ever uses: \alert{66 relations}
          (box-vs-gate commutations, case by case).
    \item By inspection, find a small set of \alert{primitive,
          bidirectional} relations that derive all 66.
    \item The choice is not canonical --- pick the set you find natural.
  \end{enumerate}
  \vfill
  An example of a derived rule, $M_x \cdot M_y = M_{xy}$:
  \begin{center}
    \fitfig[0.3\textwidth]{mm-stmt}
  \end{center}
\begin{code}[hide]
module Multiplier-law where
  open Sym using (M)
\end{code}
\begin{code}
  M-mul : ∀ n (x y : ℤ* ₚ) → let open PB (Sim._QRel,_===_ (₁₊ n)) in
          M x • M y ≈ M (x *' y)
  M-mul n = SimL.Lemmas1.lemma-M-mul n
\end{code}
  {\small ($x = g^k$, $y = g^l$; \texttt{M-power} twice; add exponents
  mod $p-1$.)}
\end{frame}

\begin{frame}{The simplified symplectic rule set (1--2 qudits)}
  \setlength{\figwidth}{0.32\textwidth}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}\tiny
      \begin{tabular}{@{}r@{\;\,}l@{}}
        order-S & \fitfig{simp-order-S}\\[1pt]
        order-H & \fitfig{simp-order-H}\\[1pt]
        M-power & \fitfig{simp-M-power}\\[1pt]
        semi-MS & \fitfig{simp-semi-MS}\\[1pt]
        order-CZ & \fitfig{simp-order-CZ}\\[1pt]
        comm-CZ-S↓ & \fitfig{simp-comm-CZ-Sdn}\\
      \end{tabular}
    \end{column}
    \begin{column}{0.5\textwidth}\tiny
      \begin{tabular}{@{}r@{\;\,}l@{}}
        comm-CZ-S↑ & \fitfig{simp-comm-CZ-Sup}\\[1pt]
        semi-M↑CZ & \fitfig{simp-semi-Mup-CZ}\\[1pt]
        semi-M↓CZ & \fitfig{simp-semi-Mdn-CZ}\\[1pt]
        selinger-c10 & \fitfig[0.4\textwidth]{simp-selinger-c10}\\[1pt]
        selinger-c11 & \fitfig[0.4\textwidth]{simp-selinger-c11}\\[1pt]
        selinger-c12 & \fitfig{simp-selinger-c12}\\
      \end{tabular}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{The simplified symplectic rule set (3 qudits), and the theorem}
  \setlength{\figwidth}{0.95\textwidth}
  \begin{center}\scriptsize
    \begin{tabular}{@{}r@{\;\;}l@{}}
      selinger-c13 & \fitfig[0.6\textwidth]{simp-selinger-c13}\\
      selinger-c14 & \fitfig[0.75\textwidth]{simp-selinger-c14}\\
      selinger-c15 & \fitfig[0.75\textwidth]{simp-selinger-c15}\\
    \end{tabular}
  \end{center}
  \vfill
\begin{code}[hide]
module Symplectic-presentation where
  open SimPres using (_SRel,_===_)
\end{code}
\begin{code}
  Theorem-Sp : ∀ {n} → (n SRel,_===_) IsPresentationOf (Sp-group n)
  Theorem-Sp = SimPres.presentation
\end{code}
\end{frame}

\begin{frame}{Comparison with the qubit rule set (Selinger)}
  \begin{columns}[T]
    \begin{column}{0.42\textwidth}
      \vspace{2ex}
      \begin{itemize}\small
        \item \alert{identical} 3-qudit relations (C12--C15);
        \item \alert{almost identical} 2-qudit relations;
        \item \alert{more complicated} 1-qudit relations --- the odd-prime
              multiplier calculus ($M_x$, primitive root $g$) has no qubit
              counterpart.
      \end{itemize}
      \vspace{2ex}
      {\footnotesize Figure 8 of Selinger,
      \emph{Generators and relations for $n$-qubit Clifford operators} →}
    \end{column}
    \begin{column}{0.56\textwidth}
      \begin{center}
        \includegraphics[page=17, clip, trim=2.5cm 2.8cm 2.5cm 2.6cm,
                         height=0.88\textheight]{selinger-clifford.pdf}
      \end{center}
    \end{column}
  \end{columns}
\end{frame}

\section{Composition}

\begin{frame}{Composing: the Pauli factor}
  The projective Pauli group $(\mathbb{Z}_p \times \mathbb{Z}_p)^n$ has an
  easy presentation over generators $X$, $Z$ on each wire:

\begin{code}[hide]
module Pauli-relations where
  open XZ using (Gen ; X ; Z)
  private variable n : ℕ
  infix 4 _PRel,_===_
\end{code}
\begin{code}
  data _PRel,_===_ : (n : ℕ) → WRel (Gen n) where
    order-X   : (₁₊ n) PRel,  X ^ p === ε
    order-Z   : (₁₊ n) PRel,  Z ^ p === ε
    comm-Z-X  : (₁₊ n) PRel,  Z • X === X • Z

  Theorem-Pauli : ∀ {n} → (XZ._QRel,_===_ n) IsPresentationOf (+ₚ-group n)
  Theorem-Pauli = XZ.presentation
\end{code}
  \vfill
  \begin{center}
    \fitfig[0.22\textwidth]{pl-order-X}\qquad
    \fitfig[0.22\textwidth]{pl-order-Z}\qquad
    \fitfig[0.28\textwidth]{pl-comm-ZX}
  \end{center}
\end{frame}

\begin{frame}{Composing: the semidirect product}
  Semidirect product: relations of both factors, plus twisted commuting,
  $h\,n' = ({}^{h}n')\,h$, so
  $(n , h) \cdot (n' , h') = (n \cdot {}^{h}n'\, ,\; h\,h')$:

\begin{code}[hide]
module Semidirect-glue where
  private variable n : ℕ
\end{code}
\begin{code}
  conj : Sym.Gen n → XZ.Gen n → Word (XZ.Gen n)
  conj Sym.H-gen   XZ.X-gen         = XZ.Z
  conj Sym.H-gen   XZ.Z-gen         = XZ.X ^ p-1
  conj Sym.S-gen   XZ.X-gen         = XZ.X • XZ.Z
  conj Sym.S-gen   XZ.Z-gen         = XZ.Z
  conj Sym.CZ-gen  XZ.X-gen         = XZ.X • XZ.Z XZ.↑
  conj Sym.CZ-gen  (XZ.X-gen XZ.↥)  = XZ.X XZ.↑ • XZ.Z
\end{code}
\begin{code}[hide]
  conj Sym.CZ-gen  XZ.Z-gen          = XZ.Z
  conj Sym.CZ-gen  (XZ.Z-gen XZ.↥)   = XZ.Z XZ.↑
  conj Sym.H-gen   (xz XZ.↥)         = [ xz ]ʷ XZ.↑
  conj Sym.S-gen   (xz XZ.↥)         = [ xz ]ʷ XZ.↑
  conj Sym.CZ-gen  (xz XZ.↥ XZ.↥)    = [ xz ]ʷ XZ.↑ XZ.↑
  conj (sym Sym.↥) XZ.X-gen          = XZ.X
  conj (sym Sym.↥) XZ.Z-gen          = XZ.Z
  conj (sym Sym.↥) (xz XZ.↥)         = conj sym xz XZ.↑
\end{code}
\begin{code}
  Pauli⋊Symplectic : (n : ℕ) → WRel (XZ.Gen n ⊎ Sym.Gen n)
  Pauli⋊Symplectic n = XZ._QRel,_===_ n ⋄ Sim._QRel,_===_ n ⋄ ConjRelʷ conj
\end{code}
  \vfill
  The projection to the symplectic factor is a homomorphism, so
  symplectic circuits and their relations \alert{lift} unchanged.
\end{frame}

\begin{frame}{Plumbing: presentation simplification}
  Two presentations are \alert{equivalent} when the presented groups are
  isomorphic over the same interpretation --- each side's axioms derived
  from the other's.  All Agda-checked, e.g.\ \texttt{Paper-V0.Iso},
  \texttt{Simplified.Iso}.
  \vfill
  \begin{center}
  \begin{tabular}{lll}
    \toprule
      & \texttt{SemiDirect} & \texttt{Paper-V0} \\
    \midrule
    generators & $X, Z, H, S, CZ$ per wire & $H, S, CZ$ per wire\\
    relations  & Pauli + symplectic + conjugation & 16\\
    \midrule
    \multicolumn{3}{l}{\emph{generator reduction}: $X, Z$ become the derived
      words from the introduction}\\
    \multicolumn{3}{l}{\emph{relation reduction}: conjugation rules become
      derivable, e.g.\ $CZ \cdot X{\uparrow} \approx X{\uparrow} \cdot
      Z{\downarrow} \cdot CZ$}\\
    \bottomrule
  \end{tabular}
  \end{center}
\end{frame}

\begin{frame}{Getting the scalars back}
  The Clifford group itself is a central extension by
  $\langle\omega\rangle$.  On presentations: add a generator $\omega$ with
  $\omega^p = \varepsilon$, make it commute with every gate, and
  \alert{correct} each mod-scalar relation by the scalar it actually picks
  up --- only \texttt{order-SH} needs one:
  \vfill
  \begin{center}
    \fitfig[0.5\textwidth]{sc-order-SH}
  \end{center}
  \vfill
\begin{code}[hide]
module Scalars-back where
\end{code}
\begin{code}
  Theorem-Clifford : ∀ (n : ℕ) →
    (QS._Exact,_===_ n) IsPresentationOf
    (PE.Presented-group n (CQP.exact-generator-data n))
  Theorem-Clifford = CQP.presentation-exact
\end{code}
  \vfill
  {\small ($\delta_p\,$: the exponent $(p^2-1)/8$ is exact division ---
  $p$ odd --- and the correction is checked, not assumed.)}
\end{frame}

\section{Conclusion}

\begin{frame}{Conclusion}
  \begin{itemize}
    \item \alert{Main result 1}: presentations by generators and relations
          of the qudit Clifford operators (16 relations, $\leq 3$ qudits)
          and of the symplectic operators, for all odd primes at once.
    \item \alert{Main result 2}: a unique normal form for qudit Clifford
          and symplectic operators.
    \item Both are \alert{checked in Agda} --- except for the two classical
          glue facts: that projective Clifford is
          $\mathrm{Pauli} \rtimes \mathrm{Sp}$, and that Clifford is a
          central extension of projective Clifford by the scalars.
  \end{itemize}
  \vfill
  \begin{center}
    \emph{Thank you!}
  \end{center}
\end{frame}

\end{document}
